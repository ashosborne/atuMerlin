# JOURNAL — Pack B document-slices conveyor, atu-merlin

One row per run. Branch `cursor/atu-merlin-estate-discovery`. Times UTC (Ash is Europe/London, +1h in September).

| run | started (UTC) | HEAD at start | SLICE_ID | result |
| ---: | --- | --- | --- | --- |
| 1 | 2026-09-08T13:22Z | `ab342e9` | `cus-interactive` | 12/12 accepted cards written; MANIFEST → `documented`; APP_MANIFEST bumped; COVERAGE regenerated; INDEX row mirrored. `CHARACTERIZATION: deferred-waived`. |
| 2 | 2026-09-08T15:11Z | `0e32c19` | `cus-modules` | 10/10 accepted cards written (`c01`–`c09`, `c11`); `c10` stays needs-SME (inferred, no card); MANIFEST → `documented`; APP_MANIFEST bumped (documented 12→22); COVERAGE regenerated; INDEX row 2 done. `CHARACTERIZATION: deferred-waived`. |
| 3 | 2026-09-08T16:07Z | `5ac7f0d` | `ord-entry-ord100` | 12/12 accepted cards written (`c01`–`c08`, `c10`, `c12`–`c14`); `c09`, `c11` stay needs-SME (inferred, no card); MANIFEST → `documented`; APP_MANIFEST bumped (documented 22→34); COVERAGE regenerated; INDEX row 5 done. `CHARACTERIZATION: deferred-waived`. Queue left: `ord-trigger-ord700`. |
| 4 | 2026-09-08T17:45Z | `fa5b77f` | `ord-trigger-ord700` | 8/8 accepted cards written (`c02`–`c07`, `c09`, `c10`); `c01`, `c08`, `c11` stay needs-SME (inferred, no card); MANIFEST → `documented`; APP_MANIFEST bumped (documented 34→42, accepted 8→0); COVERAGE regenerated; INDEX row 11 done. `CHARACTERIZATION: deferred-waived`. **Queue empty — conveyor idle until more slices are bound.** |
| 5 | 2026-09-08T20:01Z | `02c9468` | `vat-module` | 10/10 accepted cards written (`c01`–`c10`); no needs-SME candidates in the slice; MANIFEST → `documented`; APP_MANIFEST bumped (documented 42→52, surfaces accepted 12→14); COVERAGE regenerated; INDEX row 19 done. `CHARACTERIZATION: deferred-waived`. Queue left: `dat-utils`, `cou-maintain` (FCOUNTRY `c07`–`c12` only). |
| 6 | 2026-09-08T20:26Z | `de4fa27` | `dat-utils` | 8/8 accepted cards written (`c01`–`c08`); no needs-SME candidates in the slice; MANIFEST → `documented`; APP_MANIFEST bumped (documented 52→60, surfaces accepted 14→16); COVERAGE regenerated; INDEX row 21 done. `CHARACTERIZATION: deferred-waived`. Queue left: `cou-maintain` (FCOUNTRY `c07`–`c12` only). |
| 7 | 2026-09-08T20:59Z | `c0528fc` | `cou-maintain` (FCOUNTRY half) | 6/6 accepted cards written (`c07`–`c12`); `c01`–`c06`, `c13` marked `deferred` (bind mirror, not deepened); no needs-SME candidates in the accepted set; MANIFEST → `phase: B`; APP_MANIFEST bumped (documented 60→66, deferred 0→7, surfaces accepted 16→19, deferred 2→3); COVERAGE regenerated; INDEX row 17 done / deferred. `CHARACTERIZATION: deferred-waived`. Phase A `c10` corrected (F8 divergence source-only). **Residual-wave queue empty — conveyor idle until the next ORD bind wave.** |
| 8 | 2026-09-08T21:26Z | `8386fb5` | `ord-entry-ord101` | 11/11 accepted cards written (`c01`–`c10`, `c12`); `c11` stays needs-SME (inferred, no card); MANIFEST → `documented`; APP_MANIFEST bumped (documented 66→77, surfaces accepted 19→20); COVERAGE regenerated; INDEX row 6 done. `CHARACTERIZATION: deferred-waived`. Phase A sharpened: option 6 is advertised as **`6=Deliver`** (not print); no `TOTVAT` drift on delete here (hidden subfile field); `ORD200` option 2 unreachable (precedence defect, as-is). Queue left: `ord-maintain-ord200`, `ord-maintain-ord201`, `ord-maintain-ord202`, `ord-print-ord500`. |
| 9 | 2026-09-08T21:53Z | `f7277a1` | `ord-maintain-ord200` | 12/12 accepted cards written (`c01`–`c11`, `c13`); `c12` stays needs-SME (inferred, no card); MANIFEST → `documented`; APP_MANIFEST bumped (documented 77→89, surfaces accepted 20→21); COVERAGE regenerated; INDEX row 7 done. `CHARACTERIZATION: deferred-waived`. Phase A sharpened: "already closed" / "already delivered" refusals show the generic **`Invalid Option`** text, not the closed-order message; deleted rows stay on screen with their data (`ORD201` blanks them); no `orid` tie-breaker in the sort; `F5` enabled but unlabelled and unhandled; option 3 invalid here but silently accepted in `ORD201`. Queue left: `ord-maintain-ord201`, `ord-maintain-ord202`, `ord-print-ord500`. |
| 10 | 2026-09-08T22:26Z | `1b3c363` | `ord-maintain-ord201` | 11/11 accepted cards written (`c01`–`c11`); no needs-SME candidates in the slice (all `observed-in-code`); MANIFEST → `documented`; APP_MANIFEST bumped (documented 89→100, surfaces accepted 21→22); COVERAGE regenerated; INDEX row 8 done. `CHARACTERIZATION: deferred-waived`. Phase A sharpened: the page is **7 two-line rows** (`SFLPAG(7)`, customer on line 2), 14 rows per fetch batch — not "14 per page"; `F11` (`SFLDROP`) folds/drops the customer line, invisible to the program; options typed before `PAGEDOWN` survive and run on the next Enter; a dead `3` stays typed for ever; the list is **all orders with a customer row** (inner join). Queue left: `ord-maintain-ord202`, `ord-print-ord500`. |
| 11 | 2026-09-08T23:20Z | `f193828` | `ord-maintain-ord202` | 6/6 accepted cards written (`c01`–`c06`); no needs-SME candidates in the slice (all `observed-in-code`); MANIFEST → `documented`; APP_MANIFEST bumped (documented 100→106, surfaces accepted 22→23); COVERAGE regenerated; INDEX row 9 done. `CHARACTERIZATION: deferred-waived`. Phase A corrected: **no option column exists** (every `SFL01` field is output-only); paging is **not missing** — `SFLSIZ(7) ≠ SFLPAG(6)` makes the subfile auto-extend and the display pages it (no `PAGEDOWN` keyword), so orders with more than 7 lines display fully; `SFLDROP(CF11)` means the article description line is **hidden until `F11`**; a **ghost / not-found `orid` (option 5 after a delete) raises an unmonitored date exception** at the unguarded `%date(ordate:*iso)`; a missing article carries the **previous line's description**. Queue left: `ord-print-ord500`. |
| 12 | 2026-09-08T23:50Z | `c0fbaa2` | `ord-print-ord500` | 7/7 accepted cards written (`c01`–`c03`, `c05`–`c08`); `c04` stays needs-SME (inferred, no card — `CVTSPLPDF` processing program not in tree); MANIFEST → `documented`; APP_MANIFEST bumped (documented 106→113, surfaces accepted 23→25); COVERAGE regenerated; INDEX row 10 done. `CHARACTERIZATION: deferred-waived`. Phase A corrected: **15 detail lines per page**, not 14 (`count > 14` tested before the increment); **an unknown order id does not print a blank document** — the unguarded `%date(ORDATE:*iso)` raises the same unmonitored date exception as `ORD202` before the first `write`; continuation pages **omit the customer block** (`HEADER2` written once); the PDF name truncates a 6-digit order id to 5 (`%char(orid)` into a `5 const` parameter / `&ORD LEN(5)`); VAT on the print is `tottot − totnet` of the **stored** line totals; every failure in `ORD500C` (no `MONMSG`) propagates to the caller as an unmonitored call error. **ORD-wave queue empty — all five ORD slices carded; ME drafts the new ORD Architecture pack next (`ROOM_OK` then).** |
| 13 | 2026-09-09T08:18Z | `a653c21` (RUN tip `a1ecae1`) | `par-maintain` | Row back-filled by run 14 from the run-13 notes below. 13/13 accepted cards written (`c01`–`c13`; `c08` inferred carded — bind accepted it); MANIFEST → `documented`; APP_MANIFEST bumped (documented 113→126, surfaces accepted 25→29); COVERAGE regenerated; INDEX row 18 done. `CHARACTERIZATION: deferred-waived`. Phase A corrected: the `*** Deleted ****` marker never displays (`PARM2` not a subfile field); F6 return runs the Page Down path and duplicates the last row after `Bottom`; `PATH` must end with `/` for `PRO202`/`PRO203`; getter cache on the buffer's own key — first read per job is the value for the job. Queue left: `log-programs`, `menu-cmd-shell`, `srvpgm-supporting`, `sql-objects`, `ord-batch-ord900`, `cou-maintain` COU200 half, `pro-interactive`, `pro-modules`, `pro-cobol-pro201`. |
| 14 | 2026-09-09T09:09Z | `f916270` (nudge; RUN tip `828230b`) | `log-programs` | 10/10 accepted cards written (`c01`–`c10`; `c04`/`c06`/`c09`/`c10` inferred carded — bind accepted them); MANIFEST → `documented`; APP_MANIFEST bumped (documented 126→136, surfaces accepted 29→32); COVERAGE regenerated; INDEX row 20 done. `CHARACTERIZATION: deferred-waived`. Phase A corrected: **the log fills after ≈ 35 `ORD700` lines, not 40–60** (600-byte fixed write → failure at `pos + 600 > size`) **and then fails permanently** (the failing assignment leaves `pos` unchanged; every later call in every job fails at the same offset, swallowed). Sharpened: `inz = *on` is set before the failing `QUSPTRUS`, so a missing `SAMLOG` disables logging for the whole job with no retry; `QUSCRTUS` error swallowed but `QUSPTRUS` (no error code) surfaces it — or silently resets an old log's header; `User` stamped at service-program activation, blank-padded; message > 437 chars loses the `' ***'` terminator; `EXPORT(*ALL)` exports exactly one procedure. Existing target counterpart cited (ORD vertical `samlog` table), not widened. Queue left: `menu-cmd-shell`, `srvpgm-supporting`, `sql-objects`, `ord-batch-ord900`, `cou-maintain` COU200 half, `pro-interactive`, `pro-modules`, `pro-cobol-pro201`. |
| 15 | 2026-09-09T10:54Z | `7e5a8dd` (ME DRAFT-packs commit; RUN tip `fdde069`) | `menu-cmd-shell` | Two runs fired on this job: the first (on `fdde069`, started 10:30Z) wrote all nine cards, MANIFEST, SME_BRIEF, CHARACTERIZATION and ran the inventory tools, then ended at 10:54Z without committing or pushing (nothing reached `origin`); this run (fired 10:54Z by `7e5a8dd`) recovered that work from the earlier run's transcript, re-verified every card claim against `ATU_SRC` (corrected one propagated miscount: 16 `cmd call` actions, not 17; five options with undefined help names, not six), re-ran `mark_documented.py` / `gen_coverage.py` and committed. 9/9 accepted cards written (`c01`–`c09`; `c02` inferred carded — bind accepted it); MANIFEST → `documented`; APP_MANIFEST bumped (documented 136→145, surfaces accepted 32→36); COVERAGE regenerated; INDEX row 26 done. `CHARACTERIZATION: deferred-waived`. Phase A corrected: **`ERR0001` is live** (`ART200D.DSPF:98`, `*IN40` from `ART200:287`) — the dead set is `ERR0003`/`ERR0004`/`ERR0005` (three, not four; 9 of 12 live). Sharpened: `PRO250`'s *screen* heading reads "Provider by Id" (`PRO250D.DSPF:24`) — only the `%TEXT` comments say "Display Article", so `c09` is a source-comment defect, not user-visible; help mapping has a third undefined name (panel `help=h`) and an orphan module (`SAMHELP` `ART200`, never referenced); option 3 points at the *defined but wrong* `cus200` topic; every one of the 16 in-tree targets is confirmed parameterless (no `PI`/`*ENTRY`/`USING`/`PGM PARM`) so the 16 `cmd call` actions are well-formed; `CVTSPLPDF.CMD` "no PGM" is the normal shape of command source — the program binding is a `CRTCMD PGM()` build parameter and no `CRTCMD` exists anywhere in the tree (same blind spot as `CRTORD`); `PMTCTL` only governs prompting — no `DEP`, so no cross-parameter validation. Queue left: `srvpgm-supporting`, `sql-objects`, `ord-batch-ord900`, `cou-maintain` COU200 half, `pro-interactive`, `pro-modules`, `pro-cobol-pro201`. |
| 16 | 2026-09-09T11:17Z | `f2f7e6c` (operator PASTE-convert commit; RUN tip `ed1687c`) | `srvpgm-supporting` | Three runs fired on this job (BOUND push `ad697f3` and RUN tip `ed1687c` at 11:12Z — both IDLE with nothing on `origin` — then this one on the PASTE-convert push). 9/9 accepted cards written (`c01`–`c09`; `c05` inferred carded — bind accepted it); MANIFEST → `documented`; APP_MANIFEST bumped (documented 145→154, surfaces accepted 36→37 — `bnddir:SAMPLE`; four `srvpgm:*` stay `unknown`); COVERAGE regenerated; INDEX row 25 done. `CHARACTERIZATION: deferred-waived`. **Phase A corrected: `c07` — default activation group of the fourteen un-keyworded ILE programs is `QILE`, not new-per-program → getter caches most likely job-wide (runtime-confirm).** Sharpened: `c02` (PRO202/PRO203 `/copy qprotosrc,xml|Xss` copybooks missing too — they do not compile from the tree; 7 + 16 names), `c03` (9 importers / ORD200-202 inert / ART250 `GetArtInfo` unresolvable), `c04` (`BNDSRVPGM` lists match imports exactly; PRO202 is a module), `c05` (LOG100 needs no binding; `BNDDIR(SAMPLE)` cannot explain ORD700), `c06` (`.BND` ↔ module `export` cross-check 10/4/15/4/14/4; **`EXPORT(*ALL)` = 5 / 1**; seven `Close*` prototypes unexported), `c08` (cause `ART301:164-165`), `c09` (13 ARCAD snapshots, `FGRMON_OBJ`/`FGRMON_DTA`). **One-off outside the cap-1 slice: `par-maintain-c10` corrected** (card correction block + struck text, MANIFEST summary, SME_BRIEF line) — the open item from run 13's sibling review. Queue left: `sql-objects`, `ord-batch-ord900`, `cou-maintain` COU200 half, `pro-interactive`, `pro-modules`, `pro-cobol-pro201`. |
| 17 | 2026-09-09T21:19Z | `30dd3d2` (RUN tip — "prefer sql-objects (run 17)") | `sql-objects` | Sole RUNNING run of the automation at start (one stale NOT_YET_STARTED sibling from the 20:14Z DONE push, no diff). 10/10 accepted cards written (`c01`–`c10`; `c04` inferred carded — bind accepted it); MANIFEST → `documented`; APP_MANIFEST bumped (documented 154→164; surfaces `view:ORDERCUS` / `view:ARTLSTDAT` / `table:ARTIINF` / `seq:CUSSEQ` / `sqlprc:ART801` candidate → accepted); COVERAGE regenerated; INDEX row 27 done. `CHARACTERIZATION: deferred-waived`. Sharpened: `c01` (`CUSTOME1` is `UNIQUE` on `CUID`, so the join cannot duplicate; no `CUDEL` filter — soft-deleted customers' orders stay listed, and no in-tree path physically deletes a customer, so the hidden-orphan case arises only from `ORD100`'s unchecked `ORCUID`); `c02` (`TOTVAL` is fetched into an `11Y 2` `SUMORD` — a total ≥ 10^9 fails the fetch and ends the load loop silently); `c03` (joins on `ORID = ODORID` only — `ODYEAR`/`ORYEAR` ignored, like `ART801`; no `ARDEL` / `ORDATCLO` filter; `GROUP BY ARDESC` shows the current description; view labels reproduce `SAMREF` `COLHDG`/`TEXT` incl. the `DESCRPTION` typo); `c05` (`ART200` uses the long column names, `ART302` the short ones; `ART200` stores `TRIM(:text)` from a `1520A CHECK(LC)` field and treats *any* `sqlcod <> 0` on the read as "create" — an `INSERT` into an existing key then fails unchecked; `CCSID 297` is the only explicit CCSID on a data column in the estate); `c06` (`ART200` option 4 sets `ARDEL = 'X'` and touches nothing in `ARTIINF`); `c07` (`CUID` is `5P 0` — the sequence has `NO MAXVALUE` but the column runs out at 99999); `c09` (no handler — a failing statement ends the procedure with the SQLSTATE; `DECRESULT (31,31,00)` governs the `SUM` precision); `c10` (`"ORDER"` delimited at six `ART801` lines + one each in the views; the target already names the table `orders`). Queue left: `ord-batch-ord900`, `cou-maintain` COU200 half, `pro-interactive`, `pro-modules`, `pro-cobol-pro201`. |
| 18 | 2026-09-09T22:05Z | `e81562f` (RUN tip — "prefer ord-batch-ord900 (run 18)") | `ord-batch-ord900` | Sole RUNNING run of the automation at start (one stale NOT_YET_STARTED sibling from the 20:15Z DONE push, no diff). 9/9 accepted cards written (`c01`–`c09`; `c07` inferred carded — bind accepted it); MANIFEST → `documented`; APP_MANIFEST bumped (documented 164→173; surfaces `pgm:ORD900` / `pgm:ORD901` deferred → accepted — tool now lifts a deferred surface on a re-accepting bind); COVERAGE regenerated; INDEX row 12 done (was `deferred (bind)`). `CHARACTERIZATION: deferred-waived`. Corrected: `c01` (empty file writes `LASTORDNO = 0`; `READP` does not fail). Sharpened: `c02` (guard via the `-305` `NULL` path; silent exit on any SELECT failure), `c03` (one offset, sign from data; same-day rerun no-op; `RNQ0112` on a bad date; double shift on rerun after a partial run; `ORDER3` no reader), `c04` (10-day auto-close only here, strict boundary; closed-without-delivery reachable only here; `ARCUSQTY`/`CUCREDIT` left stale, nothing sequences 81→82), `c05` (only writer of `DETORD.ODYEAR`; differential; `ORD700U` fires as `UpdArt(0)`; orphan lines skipped; `CULASTORD` statement verbatim `ART801:35-37`; `ORD701` differs), `c06` (native no-COMMIT vs SQL side with no `SET OPTION` — build option not in tree; no handler; `SQL7008` case; concurrency), `c07` (`%TEXT` blank on exactly ORD900/ORD901/ART801 + ORD700/PRO203 of 37; ARCAD-generated DTAARA), `c08` (no CL/CMD/SBMJOB/JOBSCDE/`*ENTRY`/`PI`/`AddLogEntry`; job date the only knob), `c09` (`lastdate` two meanings; `%days(10)` twice, only in ORD901). Queue left: `cou-maintain` COU200 half (runnable); `pro-interactive`, `pro-modules`, `pro-cobol-pro201` (room-held). |
| 19 | 2026-09-09T22:36Z | `a792163` (ME ord-batch pack refinement; RUN tip earlier in the same burst — "prefer cou-maintain COU200 half ONLY (run 19)") | `cou-maintain` (COU200 half) | Sole run of the automation alive at start (no RUNNING / NOT_YET_STARTED sibling). 7/7 accepted cards written (`c01`–`c06`, `c13`; all `observed-in-code`); `c07`–`c12` not re-carded (job header); MANIFEST → 13 `documented`; APP_MANIFEST bumped (documented 173→180, deferred 7→0; surface `pgm:COU200` deferred → accepted via run 18's lift); COVERAGE regenerated; INDEX row 17 done for all 13. `CHARACTERIZATION: deferred-waived` (both halves). Corrected: none. Sharpened: `c01` (`PAGEDOWN(25)` keyword dead — `N80` never true, `*IN25` never tested; controller pages; invalid option defers valid `2`s; empty file no message), `c02` (`S02CHK` empty — every Enter writes; `COISO` uppercased / `COUNTR` not; only `UF` declaration of `COUNTRY` in the tree), `c03` (deleted-row and locked-row exception paths, `G` re-raises; no in-tree trigger), `c04` (estate-wide writer / CL / SQL / trigger census; `CUCOUN` / `PRCOUN` no RI), `c05` (list F3 ≡ F12; edit F3 `GOTO` out of subroutine; edit F12 → next selected row, lock kept), `c06` (`COID`-order edits; `UPDAT SFL01` precedes the edit; permanent flag on typed `0`), `c13` (RPG/400 V2R2+; blank `%TEXT`; only `AADDSRCARC`; no `/COPY` / `CALLB` / `FCOUNTRY`; own ODP → why the cache misses renames). **Runnable queue EMPTY** — `pro-interactive`, `pro-modules`, `pro-cobol-pro201` accepted but room-held. |

## Run 1 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN`.
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD.
- Pick: `cus-interactive` (job preference; CUS-first prove path). Other accepted undocumenteds left in queue: `cus-modules`, `ord-entry-ord100`, `ord-trigger-ord700`.
- FEATURE_IDs: kept the Phase A / bind ids `cus-interactive-c01..c12` rather than renumbering to `<SLICE_ID>-NNN`. The bind record, `BIND.md` and APP_MANIFEST `behaviour_id`s all reference the `cNN` ids; renumbering would break that trace for no gain. Flagged as a Field Guide open decision, not silently changed.
- Phase A summaries corrected from source during deepen (as-is, observed): `c08` — `CUMODID` is set only at `*inzsr`; on update the `chain` reloads the stored `CUMODID`, so update refreshes `CUMOD` but **not** `CUMODID`. `c04` — update-mode duplicate check counts DB rows matching the *new* values, so changing name+phone to collide with exactly one other customer passes (`dup = 1`).
- Phase A open question on `CUCREA` answered from source: update path chains the record first, so stored `CUCREA` is preserved; create uses the `*inzsr` date (program start date, not save date).
- `APP_MANIFEST.yaml`: `notes` had been changed to a list by the bind commit; `gen_coverage.py` rendered it as a Python list. Added `overnight/tools/mark_documented.py` (status bump + pointers only) and made `gen_coverage.py` render list notes as bullets. No inventory content invented.
- Timestamps in `discovery/cus-interactive/MANIFEST.yaml` normalised to run time.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 2 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "run 2 prefer cus-modules").
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed.
- Pick: `cus-modules` (job preference; CUS-first prove path). Remaining accepted undocumenteds: `ord-entry-ord100`, `ord-trigger-ord700`.
- `srvpgm-fcustomer` folded per bind: no new feature invented; export/signature facts documented inside `c01` and `c05`.
- `c10` (inferred scaffold) kept `needs-SME`, no card written. Auto-accept policy not exercised.
- Findings beyond Phase A summaries (as-is, cited in cards): `ExistCus`/`IsCusDeleted` have no callers in `ATU_SRC` and only `GetCusName` of the getters is called; misses are not cached but hits are (stale reads possible in one activation group); `ExistCus(0)` as first call never chains (`%found` undefined); `SltCustomer` criteria persist across calls; SQL errors silent; F8 unhandled → Enter; criteria change beats selection.
- Evidence corrections: `SLTCUSTOMER` export `FCUSTOMER.BND:19`; `c10` include comment `CUSTOMER.RPGLEINC:64-67`.
- Tooling: `MANIFEST.yaml` summaries quoted (unquoted `: ` broke PyYAML before `mark_documented.py` ran; no tool changes needed). `gen_coverage.py` 0 problems.
- Pushed once at the end with `AGENT_JOB.md` already `DONE`.

## Run 3 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "run 3 prefer ord-entry-ord100").
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. The CUS PACK bound at `5ac7f0d` was not consumed (ORD is document-only per bind).
- VM had a scratch branch checked out at `5ac7f0d`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-entry-ord100` (job preference). Remaining accepted undocumented: `ord-trigger-ord700` (last in the bind).
- `c09`, `c11` (inferred) kept `needs-SME`, no card written. Auto-accept policy not exercised. `c09` evidence line corrected to `ORD100C.PGM.CLLE:12` (status unchanged).
- Findings beyond Phase A summaries (as-is, cited in cards): program starts in the add-line panel after customer selection; after `F6` a cancelled article prompt re-prompts until an article is chosen; `F3`/`F12` on `FMT02` reached via `F6` end the program (staged order lost silently), but return to the list from the first-pass add screen and the edit screen; no "F27" — 27 is `CHANGE(27)`; footer `TOTVAT` drifts on delete (`ODTOTVAT` not in `SFL01`) and footer totals are stale after an edit; `DETORD.ODYEAR` written `0` by `ORD100` (only `ORD901` backfills); no commitment control; zero-line orders confirmable; `SFLMSG` 35/36 never reset in code (persistence = INDARA runtime question); no customer/article/credit/stock guards (7 procedures, 4 files in total).
- Pointers only (not deepened): `ORD901.PGM.SQLRPGLE:42-45` (deferred `ord-batch-ord900`), `ORD500` callee signature, `ORD200`/`ORD201` call sites, `ORD700A`/`ORD701` trigger definitions.
- Tooling: `mark_documented.py --slice ord-entry-ord100` (12 bumps, 12 cards), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE`.

## Run 4 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "run 4 prefer ord-trigger-ord700", re-queued after the CUS convert station finished at `efb5e2a`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `modern/` (CUS convert) and the CUS PACK were not touched.
- VM had a scratch branch checked out at `fa5b77f`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-trigger-ord700` (only accepted undocumented slice left; matches job preference). Bind queue is now empty.
- `c01`, `c08`, `c11` (inferred) kept `needs-SME`, no card written. Auto-accept policy not exercised. Their MANIFEST summaries were sharpened with the observed facts from the cards (e.g. "order close never reaches ORD700") without changing status or confidence.
- Findings beyond Phase A summaries (as-is, cited in cards): insert path adds full `ODQTY` and ignores `ODQTYLIV` (every other path uses outstanding); only the delete event logs, message carries `ODQTY` not the subtracted quantity, `callp(e)` with `%error` unread; in-tree update firers are `ORD101` edit, `ORD200`/`ORD201` opt 8 deliver (`-ODQTY` per undelivered line) and `ORD901` `ODYEAR` backfill (nets 0); no in-tree writer changes `ODARID`; order close (opt 7) touches `ORDER` only; `UpdArt` has no error handling, ignores `ARDEL`, does not stamp `ARMOD`, can go negative; `*inlr` only on the no-parm path; `ORD701` assigns (not `MAX`), fires before the lines are written; `ART801` `WHERE EXISTS` leaves rows without open orders untouched and is the only writer of `CUCREDIT`; `AddLogEntry` binding not in source (no `bnddir`, `LOG` absent from `SAMPLE.BNDDIR`, no `ORD700.ILEPGM`).
- Evidence corrections: `ORD701` body `ORD701.SQLTRG:5-16` (was `:4-14`); menu opt 82 `SAMMNU.MENU:151-154` (was `:152-155`); `ART801` `CULASTORD` statement `:34-37` (was `:33-36`).
- Pointers only (not deepened): `ORD100`/`ORD101`/`ORD200`/`ORD201`/`ORD901` call sites; `LOG300` (what the log line contains); `PRO202`/`PRO203`/`CUS200`/`CUS250D`/`CUS300` as consumers of the maintained fields.
- Tooling: `mark_documented.py --slice ord-trigger-ord700` (8 bumps, 8 cards, 0 surface bumps — surfaces were already `accepted` from run 1), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 5 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: residual wave, "Prefer order: vat-module, then dat-utils, then cou-maintain FCOUNTRY features"; bind tip `overnight/BIND_RECORD_RESIDUAL_2026-09-08.md` @ `d24702f`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `02c9468`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `vat-module` (job preference; first of three accepted undocumenteds). Remaining accepted undocumented: `dat-utils` (8), `cou-maintain` (FCOUNTRY `c07`–`c12`; `c01`–`c06`, `c13` deferred — do not deepen).
- All 10 candidates were `observed-in-code` and accepted; no `inferred` rows, so nothing stays needs-SME for lack of a card. Auto-accept policy not exercised. Six needs-SME *target* questions carried in `MANIFEST.yaml` (`c02` unknown code → error or zero; `c04` soft-deleted code still applied; `c06` intra-day rate changes; `c07` how VATDEF is maintained; `c03` dead VAT fields on ART200 FMT02; `c05`/`c09` activation group and signature practice).
- Findings beyond Phase A summaries (as-is, cited in cards): `eval` into `11P 4` truncates before `%dech` half-adjusts (equivalent to rounding the exact quotient); misses clear the buffer so non-blank misses re-read every call while a blank code never reads at all; `ExistVATRate` is the only reader of `VATDEL` and has no caller, so soft-deleted rates still apply; `VAT300` is the only member that opens `VATDEF`; `ART200` writes `ARVATCD` as typed (validates description and family only; F4 prompts family only) and never fills the `VATRATE`/`VATDESC`/`WITHVAT` output fields on its FMT02; `GetVATRate` is called immediately after `ClcVAT` for the same code (cache hit); `FAVATCD` never applied; `DETORD` stores neither code nor rate; `'V1'` literal signature with no `*PRV` (contrast `FPROVIDER.BND`); copybook publishes five prototypes for four exports.
- Phase A corrections: `c08` — `ART250` passes `ARVATCD` directly (one hop), only `ORD100`/`ORD101` go through `GetArtVatCode` (twice per line prepare); `c10` — the module PI for `ClcVAT` also omits the `A` (`VAT300.RPGLE:40`), so the drift is between `ClcVAT` and the other three procedures, not copybook vs module.
- Pointers only (not deepened): `ART250` "with VAT" label shows the VAT amount (art-interactive); `GetArtVatCode` / `chainARTICLE1` pattern (art-modules); `ORDERCUS.VIEW` / `ART801` as consumers of `ODTOTVAT`; `LOG100` as the only install-style member.
- Bind mirror gap noted: `d24702f` did not mirror `accepted` into `APP_MANIFEST.yaml` / `INDEX.md` for `dat-utils` and `cou-maintain`; this run mirrored only its own slice (cap 1) and flagged the gap in the INDEX header and MORNING_BRIEF §4.
- Tooling: `mark_documented.py --slice vat-module` (10 bumps, 10 cards, 2 surface bumps), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 6 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "run 6 after residual bind — Prefer: dat-utils (8 accepted). Then cou-maintain FCOUNTRY half (c07–c12)").
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `de4fa27`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `dat-utils` (job preference; first of two accepted undocumenteds). Remaining accepted undocumented: `cou-maintain` (FCOUNTRY `c07`–`c12`; `c01`–`c06`, `c13` deferred — do not deepen).
- All 8 candidates were `observed-in-code` and accepted; no `inferred` rows, so nothing stays needs-SME for lack of a card. Auto-accept policy not exercised. Five needs-SME *target / out-of-tree* questions carried in `MANIFEST.yaml` (`c01`/`c07` sentinel vs NULL; `c07` collapse three implementations; `c02`/`c03` QM queries as consumers of `ISO_Num_To_Date`; `c01` `99999999` branch used by out-of-tree data; `c04` build owner: activation group / FENCED / library / function creation).
- Findings beyond Phase A summaries (as-is, cited in cards): sentinel test is exact equality on `0` and `*HIVAL` (`99999998` → NULL); no in-tree writer produces `99999999` and the `2039-12-31` value has no `MAPVAL` (would display and count as "closed"); both callers fetch the three converted dates **without null indicators** and loop on `sqlcod = 0`, so a NULL ends the list load silently (`-305`; derived: an invalid `ORDATE` sorts first under `order by datord desc` and empties the list); after deliver/close the callers refresh the subfile dates with `%date()` directly; `ORD200`/`ORD201` hold `d'1940-01-01'` to *compare* the UDF output — SQL sentinel and RPG constant must agree; the raw `0` convention is tested directly by `ORD200`/`ORD201`, `ORD202`, `ORD901`, `ART801`; `*PSSR` is a safety net (only a decimal-data error on a corrupt packed argument could reach it), message text truncated 80 → 70, `date_ind` stays `0` on that path so the statement fails rather than yielding NULL; `dat8_ind`, `Function_Name`, `Specific_Name`, `stPgmName` never read; `EXTERNAL NAME` unqualified; no build script creates the functions; nothing to go stale between rows (contrast `FVAT` / `FCUSTOMER`). Derived and flagged: `1940-01-01` / `2039-12-31` are `*LOVAL` / `*HIVAL` of RPG 2-digit-year date formats (`ORD202D` `MAPVAL(('01/01/40' *BLANK))` under `*DMY`).
- Phase A corrections: `c04` — the `PARAMETER STYLE SQL` interface is **8** parameters, not 9; `c07` — `ORD202` does not map `0 → 1940` per row; it presets the fields in `*inzsr` and skips assignment when the numeric is `0` (works because it ends with `*inlr` per order) — pointer for `ord-maintain-ord202-c01`.
- Pointers only (not deepened): `ord-maintain-ord200`/`201` (un-indicated fetch, list truncation, sentinel comparisons, `MAPVAL` on delivery/close only); `ord-maintain-ord202` (implicit sentinel, unmonitored `%date`); `ord-print-ord500` (unguarded `%date(ORDATE)`); `sql-objects` (`ORDERCUS` raw columns, `ART801` `ORDATCLO = 0`); `menu-cmd-shell` (QM options 12/13 as the only possible consumer of `ISO_Num_To_Date`).
- Bind mirror gap: `cou-maintain` still reads `candidate` in `APP_MANIFEST.yaml` / `INDEX.md` (residual bind `d24702f` did not mirror it); this run mirrored only its own slice (cap 1) and re-flagged the gap in the INDEX header and MORNING_BRIEF §4.
- Tooling: `mark_documented.py --slice dat-utils --note …` (8 bumps, 8 cards, 2 surface bumps), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 7 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "run 7 after residual bind — Prefer: cou-maintain FCOUNTRY half only (c07–c12). COU200 (c01–c06, c13) stays deferred.").
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `modern/` (including `modern/src/shared/fcountry/index.ts`, read for a pointer only), `verification/` untouched.
- VM had a scratch branch checked out at `c0528fc`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `cou-maintain` FCOUNTRY half (only accepted undocumented slice; matches job preference). Deferred half `c01`–`c06`, `c13` **not deepened**; marked `deferred` in the slice MANIFEST (schema-valid status) so the bind mirrors into APP_MANIFEST — a deviation from `ord-batch-ord900` (features left `candidate`, surfaces `deferred`), needed because this slice is split across surfaces. Residual-wave queue now empty.
- All 6 accepted candidates were `observed-in-code`; no `inferred` rows, so nothing stays needs-SME for lack of a card. Auto-accept policy not exercised. Five needs-SME *target / build* questions carried in `MANIFEST.yaml` (`c08` carry or reject `GetCountryIso3` / `COISO`; `c10` card the keyed selector once for `SltCountry` / `SltArtFam`; `c09` empty-position message and position across toggle; `c07`/`c12` activation group of three callers, signature bump history, library; `c07` cache staleness vs read-through).
- Findings beyond Phase A summaries (as-is, cited in cards): cache key is the record buffer's own `COID` — hit cached, miss clears the buffer first (never cached), blank never reads, `ExistCountry(blank)` as first call correct only via the initial `%found`; no sequence returns a wrong answer (`c07`); `COISO` is a literal `3A` outside `SAMREF`, read/written only by the deferred `COU200`; `closeCOUNTRY` unexported and uncalled, five prototypes for four exports, `COU301` opens `COUNTRY`/`COUNTR1`/`COU301D` separately and never closes them (`c08`); `SltCountry` is a native keyed read positioned at `pcod`, 20 rows per load into a 10-row page, empty frame with no message beyond the last key, `pcod` by reference and never written, plain Enter redisplays (`c09`); F8 ignored when any row option typed, F8 + option 8 toggles and discards the position (`c10`); invalid row + option 8 leaves the position unapplied with the `8` typed, valid `1` + option 8 → 42 and nothing returned, message texts are DDS literals (`c11`); `PRO200.RPGLE` has no `H` spec so it binds only via `PRO200.ILEPGM BNDSRVPGM`, literal `'V1'` with no `*PRV` (only `FPROVIDER` versions), four display files and two physical files compile against the `COUNTRY` layout (`c12`).
- Phase A correction: `c10` — the `COU301` / `FAM301` F8 key-clearing divergence is source-level only; traced through both modules it is **identical on screen** (every toggle starts the new order from the top; `pcod` position lost after the first toggle away and back). Phase A's "card separately" recommendation withdrawn in `SME_BRIEF.md`; question inverted for the room / `fam-maintain` bind. `c11` — `S01chk` and position-to branch confirmed byte-identical to `sltArtFam` by diff.
- Pointers only (not deepened): `COU200` as the direct `COUNTRY` writer / only `COISO` reader (deferred half); `FAM300`/`FAM301`/`FAM301D` (`fam-maintain`); `PRO200`/`PRO250` call sites (`pro-interactive`); `CUS301` shows raw `CUCOUN` (`cus-modules-c06`); `SAMPLE.BNDDIR` / signature policy (`srvpgm-supporting`); `modern/src/shared/fcountry/index.ts` (target-side consumer of `c07`/`c09`; not changed).
- Bind mirror gap closed: `cou-maintain` no longer reads `candidate` in `APP_MANIFEST.yaml` / `INDEX.md`.
- Tooling: `mark_documented.py` gained a split-bind rule — a `candidate` surface of an accepted slice goes `deferred` (not `accepted`) when every slice feature on it is deferred/rejected; without it `pgm:COU200` would have been bumped to `accepted`. Run: `--slice cou-maintain --note …` (13 behaviour bumps = 6 documented + 7 deferred, 6 cards, 4 surface bumps = 3 accepted + 1 deferred), `gen_coverage.py` 0 problems.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 8 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "ORD wave after bind 2026-09-08 — Prefer order: ord-entry-ord101, ord-maintain-ord200, ord-maintain-ord201, ord-maintain-ord202, ord-print-ord500"; bind tip `overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md` @ `8386fb5`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed; the ORD Architecture pack is explicitly *after* the five ORD slices are carded. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `8386fb5`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-entry-ord101` (job preference; first of five accepted undocumenteds). Remaining accepted undocumented: `ord-maintain-ord200` (12), `ord-maintain-ord201` (11), `ord-maintain-ord202` (6), `ord-print-ord500` (7).
- 11 accepted candidates all `observed-in-code`; `c11` (`inferred`, trigger attachment) stays needs-SME with no card. Auto-accept policy not exercised. Eleven needs-SME lines carried in `MANIFEST.yaml` (`c04` typed-vs-stored rules and negative delivered qty; `c03` plain-Enter re-rate, footer stale, no lower bounds; `c06` re-selected blank row drift, no confirmation; `c07` dead `6=Deliver`; `c09` `ORD200` option 2 unreachable, activation group; `c12` closed/delivered order editable inside; `c02` session-long ORDER lock; `c10` no add path; `c01` description truncation; `c11` triggers).
- Findings beyond Phase A summaries (as-is, cited in cards): `CTL01` legend is `6=Deliver` — the dead option is a per-line deliver, not print (`c07`); both quantity rules compare a typed value with the **stored** row, so a consistent raise is rejected and an inconsistent lower is stored, negative delivered qty passes (`c04`); an unmodified Enter on `FMT02` still `update fdeto`s with `ODTOTVAT` recomputed at today's VAT rate, save after change takes two Enters, `F3` and `F12` on `FMT02` both return to the list, footer not refreshed after save, `ORD101` is the only per-line `ODQTYLIV` writer (`c03`); `ODTOTVAT` **is** a hidden subfile field so no footer drift on a straight delete (unlike `ORD100`), drift on re-selected blank row (hidden fields not cleared) and after edit-then-delete, delete-by-key not-found silent (`c06`); `ORDER1` `UF` chain never updated → header row locked for the session, order-not-found silent (`c02`); `ORDATCLO`/`ORDATDEL` loaded, never tested; editing does not reopen (`c12`); `ORD200:187` precedence defect makes option 2 unreachable, `ORD201` correct, neither reloads after return (`c09`); one-shot load, `pagedown` never tested, `GetArtDesc` 50 → 30 (`c01`); `c05` guard reads the hidden subfile copy; `Prtord`, `create`, `help`, `prompt`, `confirm`, `morekeys`, `pagedown`, `count`, `lod` dead (`c08`); `F12` = `F3` on the list; only `write fdeto` in the tree is `ORD100:206`, `ORD901` only updates `ODYEAR` (`c10`).
- Phase A corrections: `c07` "print?" → DSPF says deliver. `c06` inherited the `ORD100` "TOTVAT drift" expectation; not the case here on a straight delete. `c09` activation group stated as an inference (default `QILE`), not asserted.
- Pointers only (not deepened): `ORD200` precedence defect / stale `sumord` after return (`ord-maintain-ord200`/`201`); order-level deliver `ODQTYLIV = ODQTY` on lines at 0 (`ord-maintain-ord201`); `ORD700` update/delete deltas (`ord-trigger-ord700`); `GetArtDesc` miss behaviour (`art-modules`).
- Bind mirror gap flagged: `8386fb5` set `bind_status: accepted` in the four other ORD slice MANIFESTs but did not mirror into `APP_MANIFEST.yaml` / `INDEX.md`; this run mirrored only its own slice (cap 1), set INDEX rows 7–10 to `accepted (Pack B queued)` and noted the caveat in the INDEX header.
- Tooling: `mark_documented.py --slice ord-entry-ord101 --note …` (11 bumps, 11 cards, 1 surface bump), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 9 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "ORD wave run 9 — Prefer: ord-maintain-ord200, then ord-maintain-ord201, ord-maintain-ord202, ord-print-ord500"; bind tip `overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed; the ORD Architecture pack is explicitly *after* the five ORD slices are carded. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `f7277a1`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-maintain-ord200` (job preference; first of four accepted undocumenteds). Remaining accepted undocumented: `ord-maintain-ord201` (11), `ord-maintain-ord202` (6), `ord-print-ord500` (7).
- 12 accepted candidates all `observed-in-code`; `c12` (`inferred`, stale `CULASTORD`) stays needs-SME with no card (mechanism already `ord-trigger-ord700-c08`). Auto-accept policy not exercised. Twelve needs-SME lines carried in `MANIFEST.yaml` (`c09` preserve/correct; `c06`/`c07` close-vs-deliver and partial lines; `Invalid Option` wording for state refusals; `c04`/`c13` no confirmation, ghost row, header-first residue, parity twin; `c01` tie-breaker, paging, `F5`, silent truncation; `c08` option 3 and whole-pass cancel; stale screen-copy guards; activation group / override scope; panel-id reuse and header-name source).
- Findings beyond Phase A summaries (as-is, cited in cards): "already closed" / "already delivered" refusals use the generic `Invalid Option` text (35), only 2/4-on-closed (36) and 4-with-deliveries (37) have specific wording (`c06`/`c07`/`c08`); the deleted row stays displayed with its data — `ORD201` zeroes `orid`/`oryear` — and `7`/`8` on the ghost row chain-miss then `update forde` without lock → unmonitored exception (`c04`/`c13`); header-first delete leaves orphan lines on a mid-way failure, `ORD200` hits the header lock first (`c13`); close leaves every `ODQTYLIV` at 0 so trigger-maintained `ARCUSQTY` is not reduced while `ART801` drops the order (`c06`); deliver rewrites zero-quantity lines with a zero delta, skips partial lines, does not close, and deliver-after-close is impossible because 7 always stamps `ORDATDEL` (`c07`); `ORDER BY datord DESC` with no `orid` tie-breaker, whole result in one pass, `F5` enabled by `CA05` but unlabelled and unhandled, any non-zero `SQLCOD` ends the load silently, `CUSTNM` from a native chain not the view (`c01`, `c11`); option 3 invalid here but passes in `ORD201` (`c08`); `F6` reloads unconditionally and discards typed options (`c02`); `5`/`6` have no state guard (`c05`); `F3` = `F12`; `ORD201D` reuses the `'ORD200-1'` panel literal; only caller `CUS200` (`c10`); parameter `cuid` and file field `CUID` are one RPG field; sentinel defined three times; `DATORD` has no `MAPVAL` (`c11`); dead declarations `rrs01`/`count`/`mode`/`User`/`savId`/`refresh`/`pagedown` (`c01`).
- Phase A corrections / sharpenings: none of the 13 candidate statements was wrong; the guard message texts, the ghost row, the sort tie-breaker, `F5` and the option-3 divergence were missing. Phase A's "one shared lifecycle card set for the twins" recommendation was not adopted by the bind (separate slices); full cards written here, `ORD201` cited as contrast.
- Pointers only (not deepened): `ORD201` paging / `F5` / `F11` / corrected guard / lines-first delete / row blanking (`ord-maintain-ord201`, next); `ORD202` / `ORD500` not-found handling (`ord-maintain-ord202`, `ord-print-ord500`); `ORD100C` override scope (`ord-entry-ord100`); `ORD700` deltas and `CULASTORD` asymmetry (`ord-trigger-ord700`, documented).
- Bind mirror: this run mirrored only its own slice (cap 1); `pgm:ORD201`/`ORD202`/`ORD500`/`ORD500C` still `candidate` in APP_MANIFEST until their runs. INDEX rows 8–10 keep the caveat.
- Tooling: `mark_documented.py --slice ord-maintain-ord200 --note …` (12 bumps, 12 cards, 1 surface bump), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 10 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "ORD wave run 10 — Prefer: ord-maintain-ord201, then ord-maintain-ord202, ord-print-ord500"; bind tip `overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md` + `discovery/*/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed; the ORD Architecture pack is explicitly *after* the five ORD slices are carded. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `1b3c363`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-maintain-ord201` (job preference; first of three accepted undocumenteds). Remaining accepted undocumented: `ord-maintain-ord202` (6), `ord-print-ord500` (7).
- 11 accepted candidates all `observed-in-code`; no `inferred` rows, so nothing stays needs-SME for lack of a card. Auto-accept policy not exercised. Ten needs-SME lines carried in `MANIFEST.yaml` (`c03`/`c04` parity twin for edit and delete; `c07` dead sticky option 3, options surviving `PAGEDOWN`, whole-pass cancel; `c04` no confirmation, header-without-lines residue; `c01` inner-join scope, NULL-date-empties-list inference, page geometry; `c06` pointers to the `ORD200` close/deliver questions plus the `TRGUPDCND(*CHANGE)` sharpening; `c09`/`c11` activation group, `CLOSQLCSR`, cursor-left-open inference; `c09` help map / panel id).
- Findings beyond Phase A summaries (as-is, cited in cards): page is **7 two-line rows** (`SFLPAG(7)`, customer on line 2), 14 per fetch batch; `F11` `SFLDROP` invisible to the program (`c01`); "all orders" = orders with a `CUSTOMER` row, tie-breaker `orid desc` present; inferred: a NULL `ORDATE` sorts first in DESC and empties the whole list (`c01`); option 3 passes every guard and hits an empty `other` with no `update sfl01` → stays typed and re-read on every Enter (`c07`); options typed before `PAGEDOWN` survive and run on the next Enter (`c07`); `s01chk` differs from `ORD200` on exactly two lines — option 3 and the parentheses (diff-verified, `c03`/`c07`); `7`/`8` guard and action ranges byte-identical to `ORD200` (diff-verified) → `c06` cross-references `ord-maintain-ord200-c06`/`c07`; `ORD700U` is `*CHANGE` so an unchanged zero-quantity line does not fire the trigger (`c06`); delete blanks only order/year, dates/value/customer stay; header-lock failure leaves a header with no lines (`c04`); `F5` is the only in-program re-read (`c08`); only caller `SAMMNU` opt 3 with `help=cus200`; `F3` = `F12` (`c09`); `CUSTOME1`/`ARTICLE1` dead here — `ORD200` chains `CUSTOME1` and does not declare `ARTICLE1` (`c10`); `pnl00` close needed because of paging; abnormal end → cursor left open → inferred `-502` empty list on the next call (`c11`); dead scalars `mode`/`User`/`crt`/`upd`/`help`/`prompt`/`morekeys` (`c01`).
- Phase A corrections / sharpenings: "14 rows per page" → 7 two-line rows per page, 14 per fetch (`c01`). No rule statement was wrong; the sticky 3, `PAGEDOWN`-surviving options, inner-join scope, two-line row, `F11`, partial blanking and the `*CHANGE` trigger condition were missing. Phase A's "one shared lifecycle card set" recommendation honoured by cross-reference (`c06`), not by merging.
- Pointers only (not deepened): `ORD202` / `ORD500` ghost-`orid` not-found handling (`ord-maintain-ord202`, `ord-print-ord500`, next); `ORD100C2` override scope (`ord-entry-ord100`); `ORD700` deltas and `CULASTORD` asymmetry (`ord-trigger-ord700`, documented); `SAMMNU` help map (`menu-cmd-shell`); `ORDERCUS` join (`sql-objects`). No card outside `discovery/ord-maintain-ord201/` edited (the `ORD200-c07` "zero delta" wording is sharpened here, not there).
- Bind mirror: this run mirrored only its own slice (cap 1); `pgm:ORD202`/`ORD500`/`ORD500C` still `candidate` in APP_MANIFEST until their runs. INDEX rows 9–10 keep the caveat.
- Tooling: `mark_documented.py --slice ord-maintain-ord201 --note …` (11 bumps, 11 cards, 1 surface bump), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 11 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "ORD wave run 11 — Prefer: ord-maintain-ord202, then ord-print-ord500"; bind tip `overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md` + `discovery/*/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed; the ORD Architecture pack is explicitly *after* the five ORD slices are carded. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `f193828`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-maintain-ord202` (job preference; first of two accepted undocumenteds). Remaining accepted undocumented: `ord-print-ord500` (7; `c04` needs-SME inferred).
- 6 accepted candidates all `observed-in-code`; no `inferred` rows, so nothing stays needs-SME for lack of a card. Auto-accept policy not exercised. Eight needs-SME lines carried in `MANIFEST.yaml` (`c01`/`c04` not-found id → exception, target answer; `c02`/`c05` description hidden until `F11`; `c02`/`c06` stale description on a missing article vs `FARTICLE` blank; `c02` stored sums in the footer; `c03`/`c05` any key closes incl. `F5`/`F6`; `c01` date presentation across the ORD screens; activation group / build definition / exception surface inference; `c05` page geometry as DDS contract).
- Findings beyond Phase A summaries (as-is, cited in cards): `chain id order1` / `chain orcuid custome1` untested, `datord = %date(ordate:*iso)` unconditional → a not-found id (ghost row after delete in `ORD200`/`ORD201`, concurrent delete) raises unmonitored status 00112 before any display; `ORD500:31-32` identical (`c01`, `c04`); delivery/close `> 0` guards are zero tests, the sentinel is the un-overwritten `*inzsr` preset, valid only because every call ends `*inlr` (`c01`, `c03`); `*DMY` two-digit years, `MAPVAL('01/01/40')`, list twins use `*JOB` (`c01`); every `DETORD1` line loaded in one pass, `SFLSIZ 7 ≠ SFLPAG 6` → auto-extend + display-side paging + `SFLDROP` permitted; truncated first (12/page), `F11` folds (6/page); `sflend` always on (`c02`, `c05`); missing article → previous line's `ARDESC` repeated, `FARTICLE.chainARTICLE1` clears first → blank (`c02`, `c06`); footer on `KEY01` written before `exfmt ctl01`, stored sums, `EDTCDE(2)` zero → blank `Deliver` (`c02`); `RRb01 = LRRN` dead, `SFLNXTCHG`/`SFLMSG`/`ERRSFL` residue (`c02`, `c05`); `F3` = `F12` = Enter = `F5` = `F6` → `panel 0` → `*inlr`; `F11`/roll/Print handled by the display (`c03`, `c05`); no option column — every field output-only (`c05`); exactly two call sites, no menu/CMD/CL, by-reference `orid` unchanged, no state/existence guard in either caller (`c04`); `bnddir('SAMPLE')` inert, no service-program dependency, `GetCusName` bypassed too, `ARDESC` shown at full 50 vs `ORD101D` 30, `ARDEL` untested (`c06`); dead scalars `User`/`count`/`mode`/`crt`/`upd`/`chk`/`rrs01`/`err01` + nine indicators (`c01`).
- Phase A corrections: "option field unused by program" → no option field exists (`c05`); "no paging logic … >7 lines may not display fully" → display-side paging, all lines shown, concern withdrawn (`c05`); description via `ARTICLE1` chain is right but hidden until `F11` (`c02`). No rule statement about what the program does was wrong; the not-found exception, truncated-first form, stale description and `F5`/`F6` exits were missing.
- Platform documentation consulted (not source): IBM i 7.3 DDS reference — `SFLSIZ` (auto-extend when ≠ `SFLPAG`; `SFLDROP`/`SFLFOLD` not allowed when equal), `SFLDROP` (truncated first, more records than `SFLPAG` in truncated form). Cited in `CONTEXT_GATE.md`; statements flagged runtime-confirmable on the cards.
- Pointers only (not deepened): `ORD500` unguarded `%date` + `ORD500C` (`ord-print-ord500`, next); `FARTICLE` cache/miss (`art-modules`, unbound); `GetCusName` (`cus-modules`); `ORDERCUS` inner join → orphans unreachable via the lists (`sql-objects`). No card outside `discovery/ord-maintain-ord202/` edited.
- Bind mirror: this run mirrored only its own slice (cap 1); `pgm:ORD500`/`cl:ORD500C` still `candidate` in APP_MANIFEST until the `ORD500` run. INDEX row 10 keeps the caveat.
- Tooling: `mark_documented.py --slice ord-maintain-ord202 --note …` (6 bumps, 6 cards, 1 surface bump), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 12 notes (2026-09-08)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "ORD wave run 12 — Prefer: ord-print-ord500"; bind tip `overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md` + `discovery/*/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed; the ORD Architecture pack is explicitly *after* the five ORD slices are carded — which this run completes. `atu-merlin-ts-cus-v1`, `modern/`, `verification/` untouched.
- VM had a scratch branch checked out at `c0fbaa2`; switched to `cursor/atu-merlin-estate-discovery` (same SHA) before any write.
- Pick: `ord-print-ord500` (job preference; the only accepted undocumented). 8 candidates: 7 accepted `observed-in-code`, `c04` needs-SME `inferred` (no card — `CVTSPLPDF` processing program absent). Auto-accept policy not exercised. Ten needs-SME lines carried in `MANIFEST.yaml` (`c04` provider / PDF scope; `c08`/`c05` not-found id → exception, target answer with `ORD202`; `c02`/`c03` 5-char PDF name truncation; `c02` `PATH` as configuration / cache / blank pass-through; `c03`/`c05` unmonitored escape path, print mandatory-synchronous; `c01` derived VAT from stored sums; `c01`/`c07` layout facts; `c06` lookup rule and delete flags; build definition / binding / activation group / implicit indicator; `c01` page geometry as DDS contract).
- Findings beyond Phase A summaries (as-is, cited in cards): `chain id order1` untested + `datord = %date(ORDATE:*iso)` unconditional → not-found id raises unmonitored status 00112 **before the first `write`** — no document, no PDF (`c08`; Phase A "prints blank document" corrected; twin of `ord-maintain-ord202-c01`); `count > 14` tested before the increment → **15** details per page (`c01`; Phase A said 14); page break re-writes `footer`/`header`/`header3`, **not `header2`** → customer block on page 1 only (`c01`, `c07`); eject is `HEADER`'s `SKIPB(005)` behind the current line (`c01`); `TOTVAT = TOTTOT − TOTNET` of stored sums, silent-zero VAT → blank line (`c01`); `%char(orid)` into `id 5 const` / `&ORD LEN(5)` → six-digit id right-truncated, `Custord12345.pdf` collision under `*REPLACE` (`c02`, `c03`); `getParm2` → `PAR300.chainPARAMETER` lazy open + last-key cache, `ClosePARAMETER` called by nobody, `ACTGRP(*CALLER)` → `PATH` cached per activation group; missing row → blank path passed unchecked (`c02`); `ORD500C` no `MONMSG`, `ORD500` no `monitor` → every failure to the interactive caller, in `ORD100` between the order write and the "printed" window (`c03`, `c05`); `SPLNBR(*LAST)` correct only because of the explicit `close ord500o` (load-bearing, `c07`); `TOSTMF` is `TYPE(*NAME)` (file-name case not decidable), `TODIR` `*PNAME`; defaults left `JOB(*)`, `STMFCODPAG 1250`, `TITLE *NONE` (`c03`); re-print replaces the PDF, spooled files accumulate (`c03`); three call sites, `ORD100` unconditional after confirm, `ORD200`/`ORD201` opt 6 unguarded, `ORD101` dead prototype, no menu/CMD/CL (`c05`); direct `ARTICLE1`/`CUSTOME1` chains — miss → previous line's `ARDESC` / blank customer block, `ARDEL`/`CUDEL` untested; no `bnddir` on the `H` spec yet `FPARAMETER` needed (`c06`); dead `datord` preset, undeclared/untested `oflind(overflow)`, early `*inlr`, accumulators as printer-file fields, placeholder literals (`c07`); layout: country as code, no `ODQTYLIV`, `*JOB` date format, `2016/   123`, dot at column 90 (`c01`).
- Phase A corrections: `c08` "prints with blank header fields (no error path)" → unhandled exception, nothing printed; `c01` "page break every 14 details" → 15. Phase A `c02` open question (blank `PATH`) answered up to the converter boundary. No rule statement about a *found* order's document was wrong.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards): `const` shorter-parameter truncation; implicit `OFLIND(name)` definition; `SKIPB`/`SPACEB` ordering and `EDTCDE(2)`; CL escape → RPG 00202 → caller function check; `RNQ0112` inquiry surface; command-analyser `TYPE(*NAME)` handling of a mixed-case variable value.
- Pointers only (not deepened): `PAR300` / `PAR200` (`par-maintain`, unbound); missing-`bnddir` binding (`srvpgm-supporting`, unbound); `FARTICLE` miss semantics (`art-modules`, unbound); `GetCusName` (`cus-modules`). No card outside `discovery/ord-print-ord500/` edited.
- Bind mirror: `pgm:ORD500`, `cl:ORD500C` → `accepted`; all five ORD-wave slices now mirrored in APP_MANIFEST; INDEX row 10 caveat removed.
- Tooling: `mark_documented.py --slice ord-print-ord500 --note …` (7 bumps, 7 cards, 2 surface bumps), `gen_coverage.py` 0 problems. No tool changes.
- **ORD-wave queue empty.** Conveyor idle until the next bind; ME drafts the new ORD Architecture pack (`ROOM_OK` then).
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 13 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual wave after Ash authorize — Prefer: par-maintain, then log-programs → menu-cmd-shell → srvpgm-supporting → sql-objects → ord-batch-ord900 → cou-maintain COU200 half → pro-interactive → pro-modules → pro-cobol-pro201"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md` + `discovery/*/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` present in the job body (residual night bind), recorded, not consumed — this station does not need it. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, `modern/`, `verification/` untouched.
- Trigger fired on `e311156`; branch head at start `a653c21` (ten later bind commits read). VM had a scratch branch checked out at a stale SHA; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write.
- Pick: `par-maintain` (job + bind-record preference; first of ten accepted undocumenteds). Remaining accepted undocumented after this run: `log-programs` (10), `menu-cmd-shell` (9), `srvpgm-supporting` (9), `sql-objects` (10), `ord-batch-ord900` (9), `cou-maintain` COU200 half (7), `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10).
- 13 candidates all accepted by the bind: 12 `observed-in-code`, `c08` `inferred` — carded with confidence kept (source half exact, runtime half outside the tree); flagged as a deviation from the run-12 practice of leaving `inferred` rows card-less, because here the bind accepted the row. Auto-accept policy not exercised. Ten needs-SME lines carried in `MANIFEST.yaml` (`c11` PATH as configuration; `c07`/`c11` trailing slash / file system / other rows; `c08` blank PATH target stance; `c09` cache semantics; `c05` unconfirmed delete + ghost row; `c03`/`c04` list quirks; `c02`/`c04` locks / unconditional update / 01221; `c10` exports / signature / activation group / binding; `c13` SAMLOG anchor; `c01`/`c06` DDS-contract facts).
- Findings beyond Phase A summaries (as-is, cited in cards): `PARM2` is not a field of `SFL01` (`PARM2S` is) → the `*** Deleted ****` marker never displays; ghost row shows blank key + old `2`–`5`; `4` on the ghost deletes the blank/blank row if present, `2` on it → `update` without prior read → 01221 (`c05`, `c04`); F6 return (write **or** cancel) runs the Page Down path, and at `Bottom` the saved key is the last displayed row → duplicate appended (`c03`); edited rows not rewritten → stale until F5; edit `chain` locks across the screen; unchanged panel still `update`s (`c04`); duplicate check `chain`s the `uf a` file → existing row locked and loaded while `ERRMSG` 40 shows; display upper-cases every non-`CHECK(LC)` field (why `'PATH'` matches); blank/blank key accepted (`c02`); look-ahead read makes `Bottom` exact; `s01act` one changed row per cycle pass; options typed before Page Down / F6 survive; leading zeros on `PARM4`/`PARM5`; no `H` spec; `PAR200` bypasses `FPARAMETER` (`c01`); F3 = F12 on every panel, two presses to leave from a detail panel (`c06`); `PAR201` `*TCAT '*'` → `<PATH>*` — directory contents only with a trailing `/`; `PRO202`/`PRO203` need the slash too (`%trim(path) + fileName`), `ORD500C` does not (`TODIR`); only CL with `CALLPRC`; no `MONMSG` (`c07`); blank PATH silent end to end, no seed of the row in the tree (`c08`); cache test against the record buffer's own key — hit served until a different key is asked (never, in-tree), miss never cached, blank key never reads, `QILE` shared by all four consumers (`c09`); `EXPORT(*ALL)` exports `chainPARAMETER`/`closePARAMETER`, no `.BND`, no `*PRV`; `closePARAMETER` would not reset the cache (`c10`); four literal `GetParm2('PATH':' ')` callers, everything else uncalled (`c11`); `PARAMETER` zoned `1 0`/`3 0`, packed on return, no delete flag / audit / trigger / SQL consumer (`c12`); `LOG100` INFDS 93–102 library → `SAMLOG` placement, `LOG300` `*LIBL` (`c13`).
- Phase A corrections: `c05` marker text → never displayed. Sharpened: `c03` (duplicate after `Bottom`; cancel pages too), `c04` (stale values; lock; 01221), `c02` (lock + buffer load on duplicate; upper-casing), `c09` (blank key never reads; job-wide cache), `c07`/`c11` (trailing-slash contract). No rule about a valid operation was wrong.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards): RPG-cycle looping while `*INLR` off; `READ` at EOF leaves the buffer; keyed `DELETE` no-match → `%found` off; `UPDATE` after failed `CHAIN` → 01221; record-lock lifetime; `ERRMSG` not rewriting fields; `N80 PAGEDOWN` / MDT survival on `CF`/`PAGEDOWN`; `CRTBNDRPG` defaults (no `H` spec → default activation group; `dftactgrp(*no)` → `QILE`); un-edited numeric output leading zeros; `*TCAT`; `WRKLNK` pattern; INFDS 93–102.
- Pointers only (not deepened): `LOG100` body (`log-programs-c01`, next); `SAMMNU` (`menu-cmd-shell`); `SAMPLE.BNDDIR` / `.ILEPGM` / missing `bnddir` (`srvpgm-supporting-c04`/`c05`); `PRO202`/`PRO203` file names (`pro-interactive-c05`/`c13`); `ORD500C` (`ord-print-ord500-c02`). No card outside `discovery/par-maintain/` edited.
- Bind mirror: `pgm:PAR200`, `cl:PAR201`, `srvpgm:FPARAMETER`, `mod:PAR300` → `accepted`; the other nine night-wave slices stay `candidate`/`deferred` in APP_MANIFEST until their runs (cap 1). INDEX row 18 → done; header notes the night bind.
- Tooling: `mark_documented.py --slice par-maintain --note …` (13 bumps, 13 cards, 4 surface bumps), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 14 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual wave run 14 — Prefer: log-programs, then menu-cmd-shell → srvpgm-supporting → sql-objects → ord-batch-ord900 → cou-maintain COU200 half → pro-interactive → pro-modules → pro-cobol-pro201"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md` + `discovery/log-programs/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` present in the job body (residual night bind), recorded, not consumed — this station does not need it. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, `modern/`, `verification/` untouched (read for the `samlog` citation only).
- Trigger fired on the nudge commit `f916270` ("no cards ~14m after `828230b`"). Before writing anything the run listed the automation's RUNNING agents — it was the only one; the first run-14 attempt (fired on `828230b`) had produced no cards and was gone — so this run took the slice. VM had a scratch branch checked out; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write; origin re-checked before the first commit and before the push.
- Pick: `log-programs` (job + bind-record preference; first of nine accepted undocumenteds). Remaining accepted undocumented after this run: `menu-cmd-shell` (9), `srvpgm-supporting` (9), `sql-objects` (10), `ord-batch-ord900` (9), `cou-maintain` COU200 half (7), `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10).
- 10 candidates all accepted by the bind: 6 `observed-in-code`, 4 `inferred` (`c04`, `c06`, `c09`, `c10`) — carded with confidence kept (source half exact, runtime half outside the tree), as run 13 did for `par-maintain-c08`. Auto-accept policy not exercised. Nine needs-SME lines carried in `MANIFEST.yaml` (room: preserve the user-space log at all; `c04` allocated size / observed failure; `c06` who runs `LOG100`; `c08` binding of `LOG` into `ORD700`; `c09` `ADSPUSRSPC` provider / is option 84 used; `c10` acceptable loss; `c03` activation-time `User`, line string vs `Msg` as contract; `c05` runtime confirmation; `c01`/`c02` migration of existing content).
- Findings beyond Phase A summaries (as-is, cited in cards): `data` is a **600-byte fixed** based field → every call writes 600 bytes → first failure at `pos + 600 > size` ≈ 4400 at 5000 bytes ≈ **35 `ORD700` lines of ≈ 125 bytes** (fixed part 63 + message ≈ 62), Phase A said 40–60; the failing assignment (`LOG300:31`) raises before `pos += len` (`:32`) → `pos` unchanged → **permanent** failure at the same offset in every job, all swallowed by `ORD700`'s `callp(e)` (`c04`); `init` sets `inz = *on` (`:42`) **before** `rtvusrspcptr` (`:43`) → a failed resolution is never retried in the activation group; later calls fail at `p2 = p1 + pos` on the null pointer, also swallowed (`c06`, `c03`); `QUSPTRUS` prototype has **no error code** → its failures escape: in `LOG100` unmonitored (inquiry), in `LOG300` into the caller's `(e)`; a `QUSCRTUS` failure with an old `SAMLOG` present → `QUSPTRUS` succeeds on the old object and lines 24–25 reset its header (`c05`); `User` is `inz(*USER)` static → stamped at service-program activation in the caller's AG (`ACTGRP(*CALLER)`, `ORD700` `QILE` inference), written blank-padded to 10; build buffer `500 varying` → trimmed message > 437 chars truncates and loses `' ***'`; `%trim` strips leading blanks; `%char(%timestamp())` 26 chars (`c03`); header = 4-byte big-endian cursor + `'***'`, entries from 7, `' ***'` only delimiter, 600-byte pad then `X'00'`; layout defined twice, no shared copybook (`c02`); `replace '*YES'` → re-run resets, a job holding `p1` keeps writing to the replaced copy (`c01`); `EXPORT(*ALL)` exports exactly **one** procedure — only `AddLogEntry` has `export` on its P-spec, `init` does not (P-spec rule applied consistently; contrast `FPARAMETER`); no `LOG.BND` in `QSRVSRC` (six `.BND`, none for `LOG`/`FPARAMETER`); `LOG` not in `SAMPLE.BNDDIR`; `ORD700` no `bnddir`, no `.ILEPGM` (`c08`); one caller, one event, ordered quantity logged / outstanding subtracted, `ODARID` untrimmed (`c07` → `ord-trigger-ord700-c03`); `adspusrspc samlog` unqualified, `help=nohelp`, no source, no other reader of `SAMLOG` (`c09`); read `pos` / write 600 / `pos +=` unlocked — every interleaving loses the first line and leaves a blank run; cursor corruption in the narrower load/add/store window; race disappears once full (`c10`).
- Phase A corrections: `c04` count (≈ 35, not 40–60) and permanence. Sharpened: `c05` (indirect surfacing; silent header reset), `c06` (no retry within AG), `c03` (activation-time `User`, truncation, padded write), `c08` (one export). No rule about what a valid operation does was wrong.
- Existing target counterpart cited, not widened: the ORD vertical writes the same `Msg` text + 10-char actor + timestamp to a `samlog` table per delete (`modern/db/schema.sql:132-140,170-199`; PARITY `c03` `as_is`). Recommendation to the room unchanged: replace the user-space log with target logging; only the `Msg` text is contract.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards): API error-code convention; page-rounded user-space allocation; `MCH0601`; `MCH3601`; `CPF9801`; RPG status 00202 / `RNQ0202`; static init at service-program activation; `QILE` default; `EXPORT(*ALL)` semantics; `%char(%timestamp())` form; big-endian `10i 0`; non-atomic `+=`; `QRPLOBJ` on replace.
- Pointers only (not deepened): `SAMMNU` option 84 (`menu-cmd-shell`, next); `SAMPLE.BNDDIR` / `QSRVSRC` / `QILESRC` inventory (`srvpgm-supporting`); `ORD700` (`ord-trigger-ord700-c03`); `PARAMETER` anchor (`par-maintain-c13`). No card outside `discovery/log-programs/` edited.
- Bind mirror: `pgm:LOG100`, `srvpgm:LOG`, `mod:LOG300` → `accepted`; the other eight night-wave slices stay `candidate`/`deferred` in APP_MANIFEST until their runs (cap 1). INDEX row 20 → done; header notes run 14. JOURNAL table row 13 back-filled from the run-13 notes (it had notes but no row).
- Tooling: `mark_documented.py --slice log-programs --note …` (10 bumps, 10 cards, 3 surface bumps), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 16 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual run 16 — Prefer: srvpgm-supporting, then sql-objects, ord-batch-ord900, cou-maintain COU200 half, pro-interactive, pro-modules, pro-cobol-pro201"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md` + `discovery/srvpgm-supporting/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. The trigger commit (`f2f7e6c`, five `operator/PASTE-convert-*-vertical-atu-merlin.md`) and the BOUND residual packs (`ad697f3`) were not read — Convert is a different station with its own gate. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, `modern/`, `verification/` untouched.
- Trigger fired on the operator PASTE-convert push while line 1 was `RUN`. Before writing anything the run listed the automation's RUNNING agents — it was the only one; two runs created at 11:12Z (on the BOUND push and the RUN tip) were IDLE with no diff on `origin` (one lived 8 s, the other ~5 min) and `origin` head was still `f2f7e6c` with no `document-slices: srvpgm-supporting` commit, so this run took the slice. VM had a scratch branch checked out; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write; origin re-checked before the first commit and before the push.
- Pick: `srvpgm-supporting` (job + bind-record preference; first of seven accepted undocumenteds). Remaining accepted undocumented after this run: `sql-objects` (10), `ord-batch-ord900` (9), `cou-maintain` COU200 half (7), `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10).
- 9 candidates all accepted by the bind: 8 `observed-in-code`, `c05` `inferred` — carded with confidence kept (source half exact: what each program imports; build half outside the tree: how it binds), as runs 13–15 did. Auto-accept policy not exercised. Four `unknown` surfaces stay `unknown` (`c02` records absence + the 23 expected procedure names). Nine needs-SME lines carried in `MANIFEST.yaml` (`c02` XML/XSS owner + copybooks + samples, ORDER/TXT dead?; `c05` build owner — how PRO203/ORD500/ORD700 bind; `c07` `DSPPGM DETAIL(*BASIC)` QILE vs *NEW + room staleness stance; `c06` 'V1' history, ME derive API from imports, par-maintain owner accept c10 correction; `c03` ART302/GetArtInfo (held) + ORD200-202 inert; `c01` reject-as-slice; `c09` rebuilt since 2022?).
- Findings beyond Phase A summaries (as-is, cited in cards): eleven `*LIBL` entries, six consumed via `bnddir`, `FPARAMETER` listed but all four consumers bind it otherwise, `LOG` absent, no second directory, 57 exported names all distinct so entry order is irrelevant (`c01`); `PRO202`/`PRO203` `/copy qprotosrc,xml|Xss` are the only member-style copies and the members do not exist → neither compiles from the tree; 7 + 16 distinct procedure names are the whole interface spec; `ORDER`/`TXT` appear only on the `.BNDDIR` line (`c02`); import table for the twelve `bnddir` programs — `ORD200`/`ORD201`/`ORD202` import nothing bound (extpgm + own F-specs), `ART250` imports `GetArtInfo` exported only by `ART302` which is in no `MODULE()` list (existing ART302 question restated, nothing invented), `CUS200` H-spec at line 26, three free-form `ctl-opt`, five H-spec-less programs + COU200 + CL + COBOL outside the layer (`c03`); `PRO200.ILEPGM` `BNDSRVPGM(XML FCOUNTRY FPARAMETER)` ↔ imports one-to-one (`PRO200.RPGLE` 3 FCOUNTRY procs + `prpord extproc('PRO202')`; `PRO202` `getParm2` + 7 xml*), `PAR201.ILEPGM` ↔ `CALLPRC GETPARM2`; module sources have no binding keywords; `.PGM.` infix = program, none = module, consistent across 21 + 16 members; `PRO202` is not a `*PGM` (`c04`); `dftactgrp(*no)` count 16 = 12 bnddir + 4; `LOG100` only `extpgm` (needs no binding); build-level `BNDDIR(SAMPLE)` would bind PRO203/ORD500 but not ORD700; `iproj.json` no per-object options, `.elias/hashList.json` 138 hashes status DRY (`c05`); `.BND` ↔ module `export` cross-check exact 10/4/15/4/14/4; `FPROVIDER` `*GEN` + `*PRV` (13 symbols, 25/10/16, `PRO300`/`NEWSAMPLE`, renamed `PROVIDER`→`FPROVIDER`, "01.02.13 version"); **`EXPORT(*ALL)` exports module exports only → `FPARAMETER` 5, `LOG` 1**; seven `Close*` copybook prototypes + `GetArtInfo` over-advertised; `FFAMILLY.BND` source order vs alphabetical; `FFAMILLY.ILESRVPGM` `%TEXT` "Functions Country" (`c06`); eight `*CALLER`, two `QILE`, sixteen un-keyworded → compiler default `QILE`; no `*NEW`, no `RCLACTGRP`/`RCLRSC`; caches job-wide under the default; five programs read `PATH` through one `PAR300` buffer while `PAR200` writes the file directly (`c07`); one srvpgm→srvpgm edge `FARTICLE→FFAMILLY` caused by `ART301:164-165`; graph acyclic two levels; build order FFAMILLY first (`c08`); thirteen ARCAD "Direct Object" snapshots (12 build members + `LASTORDNO.DTAARA`), `FGRMON_OBJ`/`FGRMON_DTA`, 14:54:17.006–21.181 on 2022-05-17, `&O`/`&N`; five `.BND` hand-written, `FPROVIDER.BND` ARCAD-generated; `iproj.json` 20 lines, `repository` `AdrianAtArcad/atuMerlin` (`c09`).
- Phase A corrections: `c07` (default AG `QILE`, not new-per-program — scope of cache staleness job-wide; runtime-confirm). Sharpened: `c02`, `c03`, `c04`, `c05`, `c06`, `c08`, `c09` (above). No statement about a valid build was wrong except the activation-group default.
- **Correction applied outside the slice (one-off, recorded):** `discovery/par-maintain/features/par-maintain-c10.md` — run 13 had read `EXPORT(*ALL)` as "all seven procedures exported"; only `GetPARM1`–`5` carry `export` (`PAR300.RPGLE:22,34,46,58,70`; `:82`/`:100` do not), so five. Applied as a correction block + struck statements (title corrected, id/bind/rest unchanged), `CORRECTED` prefix on the MANIFEST `c10` summary/name, one struck line in `par-maintain/SME_BRIEF.md`. APP_MANIFEST `par-maintain-c10` name/notes refreshed by `mark_documented.py --slice par-maintain`. This was the open item from run 13's sibling-run review (memory); this run owns the export-contract fact (`c06`) and was the only active runner.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards): `EXPORT(*ALL)` = module exports; `CRTBNDRPG`/`CRTSQLRPGI` `ACTGRP(*STGMDL)` → `QILE`; named-group persistence; `*CALLER` static placement; fixed-signature slot semantics, `LVLCHK(*YES)` default, `*PRV`; binding-directory search order / unresolved-only / bind-time `*LIBL`; `CRTSQLRPGI` `COMPILEOPT`; `extpgm` dynamic; unresolved import = create failure; `CRTRPGMOD` has no `DFTACTGRP`; `CALLPRC` typing; `.PGM.` convention from tree consistency.
- Pointers only (not deepened): PRO members (binding facts only — no PRO behaviour, planted defects untouched); `ART300`/`ART301`/`ART302`/`FAM300`/`FAM301` (`export` keywords, two call lines — held); `LASTORDNO.DTAARA` (header only); cache mechanism per module (`cus-modules`, `vat-module-c05/c06`, `par-maintain-c09`, `log-programs-c03/c06`). No card outside `discovery/srvpgm-supporting/` edited **except** the `par-maintain-c10` correction above.
- Bind mirror: `bnddir:SAMPLE` → `accepted`; `srvpgm:XML`/`XSS`/`ORDER`/`TXT` stay `unknown`; the other six night-wave slices stay `candidate`/`deferred` in APP_MANIFEST until their runs (cap 1). INDEX row 25 → done; header notes run 16 + the c10 correction.
- Tooling: `mark_documented.py --slice srvpgm-supporting --slice par-maintain --note …` (9 bumps, 9 cards, 1 surface bump), `gen_coverage.py` 0 problems. No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 17 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual run 17 — Prefer: sql-objects, then ord-batch-ord900, then cou-maintain COU200 half; Hold: art-* / fam-* (ART302), pro-* (room-held)"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md` + `discovery/sql-objects/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, the five residual packs, `modern/`, `verification/` untouched (`modern/db/schema.sql:4-6,68-71,142-148` and `modern/src/features/{order,customer}/*` cited read-only as the as-is `ordercus` / `cusseq` counterparts).
- Trigger fired on the RUN tip itself (`30dd3d2`). Before writing anything the run listed the automation's RUNNING agents — it was the only one; the other entry (created 20:14Z by the verify-LOG DONE push) was NOT_YET_STARTED with no branch. VM had a scratch branch checked out; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write; origin re-checked before the first commit and before the push.
- Pick: `sql-objects` (job + bind-record preference; first of six accepted undocumenteds). Remaining accepted undocumented after this run: `ord-batch-ord900` (9), `cou-maintain` COU200 half (7), `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10).
- 10 candidates all accepted by the bind: 9 `observed-in-code`, `c04` `inferred` — carded with confidence kept (the view and the menu line are source; `ARTQRY` is not in the tree), as runs 13–16 did. `c07` / `c08` bound as pointer rows — given object-contract cards that defer the behaviour to `cus-interactive-c02` / `ord-trigger-ord700-c10`. Auto-accept policy not exercised. Eleven needs-SME lines carried in `MANIFEST.yaml` (`c01` orphan orders hidden — room ORD; `c02` gross total — room ORD; `c04` retrieve ARTQRY/CUSQRY/CUSQRYFMT — source owner; `c03` report semantics after ARTQRY; `c05` ART200 lost-edit path — art-* owner, CCSID 297 mapping — room; `c06` fate of soft-deleted article text; `c07` MAXVALUE vs wider CUID — room CUS; `c08` who runs opt 82, ORD901 duplicate; `c09` atomic reset — room ORD; `c10` naming carry-over, ORD901 spelling check — ME).
- Findings beyond Phase A summaries (as-is, cited in cards): `CUSTOME1` UNIQUE on `CUID` → `ORDERCUS` cannot duplicate; no `CUDEL` / `ORDATCLO` filter; no in-tree customer delete and no `CUDEL` writer → orphan orders arise only from `ORD100`'s unchecked `ORCUID`; both readers fetch `TOTVAL` into `SUMORD 11Y 2` (`ORD200D:28`, `ORD201D:25`) → a total ≥ 10^9 ends the `dow sqlcod = 0` loop silently; view labels four of eight columns, copying `SAMREF` `COLHDG` (`c01`); `ODTOTVAT` = `odtot + vat` written by `ORD100:268,295` / `ORD101:225,260`; same field as `ART801`'s `CUCREDIT` sum; no VAT label on the screens (`c02`); `ARTLSTDAT` joins on `ORID` only (no `ODYEAR`), no `ARDEL` / `ORDATCLO` filter, `QUANTITY` = ordered qty over all orders ≠ `ARCUSQTY`, `GROUP BY ARDESC` harmless (`ARTICLE1` UNIQUE), orphan lines excluded here but counted in `TOTVAL`, `ORDATE` written by `ORD100:194` / shifted by `ORD901:21`, labels copy `SAMREF` incl. `DESCRPTION` (`c03`); `ARTLSTDAT` and `LASTORDER` each occur once in the tree; option 13 has no `QMFORM`, option 12 has; no QM source directory among the 16 (`c04`); `ARTIINF` only SQL table, long + `FOR SYSTEM NAME` / `FOR COLUMN` names, PK by `ALTER TABLE` `PRIM_ARTIINF`, no FK; `ART200` panel 3 reads with long names into `TEXT 1520A B CHECK(LC)` (19 × 80), `sqlcod <> 0` → crt, Enter always writes `TRIM(:text)` (UPDATE or INSERT), no sqlcod test after, `set option commit = *none` the only `SET OPTION` in `QRPGLESRC`; `ART302` reads short names into fixed 1520; `CCSID 297` only explicit data-column CCSID (`SAMMSGF` 297 too) (`c05`); option 4 = `ardel = 'X'` + `armod` + `armodid` + `update farti`, `ARTIINF` untouched; `ARTICLE2` no S/O so deleted rows still listed and option 3 accepted; estate record deletes: `ORD100:224`, `ORD101:191`, `ORD200:230,232`, `ORD201:235,237`, `PAR200:186`; no `DELETE FROM` anywhere (`c06`); `CUSSEQ` referenced once (`CUS200:185`, unqualified, no sqlcod check); `CUID 5P 0` → value 100 000 (98 450th) overflows (`c07`); `SPECIFIC ART801` = program name → `cmd call art801`; SQL name unused in tree; only the menu calls it; `ORD901:46-50` duplicates statement 3; `QSQLSRC` = 1 SQLPRC + 2 SQLUDF + 1 SQLTRG + 2 VIEW + 1 TABLE + 1 SQLSEQ; `%TEXT` blank (`c08`); 13 header clauses tabulated; `COMMIT *NONE` + no handler; `DECRESULT (31,31,00)`; statement 3 has no `DETORD` join (`c09`); `"ORDER"` ×10 (`QSQLSRC` ×8, `ORD901:47,49`), undelimited `from order` `ORD901:11,43,45`; `FOR SYSTEM NAME` / `FOR COLUMN` / `CONSTRAINT` only in `ARTIINF.TABLE`; `LABEL ON` in three members; no `SET OPTION NAMING`; `DESCRPTION` in `SAMREF:13`, `ARTLSTDAT:22`, `ARTICLE.RPGLEINC:5` (`c10`).
- Phase A corrections: none. Sharpened: all ten (above). No statement about a valid operation was wrong.
- Platform semantics relied on (flagged inference / catalog-confirmable on the cards): `SUM` precision / `DECRESULT`; `*LIBL` + `*SYS` naming; `LABEL ON` 20-char heading segments; `CREATE SEQUENCE` defaults (`INTEGER`, `CACHE 20`), `NEXT VALUE` not rolled back; `SPECIFIC` = program object; unhandled-error propagation in SQL PL; `FOR COLUMN` dual naming; host-variable overflow = fetch/SET error; join / grouped views read-only; duplicate-key SQLSTATE; CCSID conversion on host-variable transfer.
- Pointers only (not deepened): `ART200` / `ART302` / `ART250` (`ARTIINF` statements + option-4 soft delete; held; "Never invent ART302"); `ORD901` (`ord-batch-ord900`, next — duplicate statement + spelling); `ORD100` / `ORD101` via their cards; `menu-cmd-shell-c01` / `-c02` (options 13 / 82). No card outside `discovery/sql-objects/` edited.
- Bind mirror: `view:ORDERCUS`, `view:ARTLSTDAT`, `table:ARTIINF`, `seq:CUSSEQ`, `sqlprc:ART801` → `accepted`; the other five night-wave slices stay `candidate`/`deferred` in APP_MANIFEST until their runs (cap 1). INDEX row 27 → done; header notes run 17.
- Tooling: `mark_documented.py --slice sql-objects --note …` (10 bumps, 10 cards, 5 surface bumps), `gen_coverage.py` 0 problems (documented 154→164; surfaces accepted 37→42). No tool changes.
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 18 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual run 18 — Prefer: ord-batch-ord900, then cou-maintain COU200 half; Hold: art-* / fam-* (ART302), pro-interactive / pro-modules / pro-cobol-pro201 (room-held)"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md` + `discovery/ord-batch-ord900/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, the five residual packs, `modern/`, `verification/` untouched (`modern/db/schema.sql:59-61` and `modern/README.md:210,287,379,405` cited read-only — the `lastordno` sequence and the "ORD900 / ORD901 / ART801 not converted" statement).
- Trigger fired on the RUN tip itself (`e81562f`). Before writing anything the run listed the automation's RUNNING agents — it was the only one; the other entry (created 20:15Z by the verify-LOG DONE push) was NOT_YET_STARTED with no branch. VM had a scratch branch checked out; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write; origin re-checked before the first commit and before the push.
- Pick: `ord-batch-ord900` (job preference; first runnable of five accepted undocumenteds — `pro-*` room-held). Remaining accepted undocumented after this run: `cou-maintain` COU200 half (7, runnable), `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10) (room-held).
- 9 candidates all accepted by the bind: 8 `observed-in-code`, `c07` `inferred` — carded with confidence kept (menu wording / blank `%TEXT` / ARCAD provenance / absences are source; "demo tooling" is a reading), as runs 13–17 did. Auto-accept policy not exercised. Nine needs-SME lines carried in `MANIFEST.yaml` (`c07` real vs sample — room / product owner; `c04` 10-day auto-close — room ORD; `c04`/`c05` stale `ARCUSQTY`/`CUCREDIT`, 81→82 sequence — room / ops; `c05` fold `CULASTORD`, is `ODYEAR` needed — room ORD / ME; `c06` `PRTSQLINF ORD901` — source owner; `c06` atomic or as-is — room ORD; `c01` sequence-reset action — room ORD; `c02`/`c05` undelimited `order` — source owner; `c08` `WRKJOBSCDE` — ops).
- Findings beyond Phase A summaries (as-is, cited in cards): `ORD900` stores *last used* (`ORD100` contract `IN *LOCK` / `+1` / `OUT`); `READP` at BOF untested → `LASTORDNO` written as 0; no lock between `READP` and `IN *LOCK`; only two programs touch the data area (`c01`); `SELECT MAX INTO` with no indicator, no `sqlcod` anywhere in `ORD901` → guard via `-305` leaving the initial 0; same silent exit on any SELECT failure; `ORDER` already open `UF` before the guard (`c02`); `days = %diff(today:MAX)` `5 0`, negative if newest order is in the future; same-day rerun rewrites unchanged rows (no trigger on `ORDER`; `ORD701` is `AFTER INSERT` only); `RNQ0112` unhandled on a bad date; rerun after partial run re-shifts done rows when the newest order was not among them; `ORYEAR` recomputed from the shifted date; `ORDER3.LF` (`K ORDATE`) no reader (`c03`); delivery / close shifted independently, future → 0, `else` branch applies `ORDATCLO = ORDATDEL + 10` when `ORDATDEL < today − 10` (strict); the closed branch does not re-check delivery → `ORDATCLO > 0, ORDATDEL = 0` reachable here only (interactive close always sets both — `ORD200:246-250`); closes change `ART801`'s / `ORD700`'s open set but `ARCUSQTY` / `CUCREDIT` untouched; complete writer set of the two fields: `ORD100:195-196`, `ORD200:246-257`, `ORD201:252-263`, `ORD901:22-36` (`c04`); `UPDATE detord SET odyear … WHERE odyear <> …` differential, correlated on `ORID` only; `ODYEAR` has no other writer (ORD100 writes 0) and no reader in the tree; in `DETORD.PF`'s key but not `DETORD1`'s; each changed line fires `ORD700U` (`TRGUPDCND(*CHANGE)`) → event `'3'` → `UpdArt(0)` → `return` (`ORD700:101-103`); orphan lines skipped; `CULASTORD` statement character-for-character `ART801:35-37`; `ORD701` assigns instead of `MAX`; no `COMMIT`, no handler (`c05`); `UF E DISK` no `COMMIT` keyword; no `SET OPTION` in the member (census: `ART200:332` and `ART801` header only); build = `elias compile` (`iproj.json`), options not in tree; no `MONITOR` / `(E)` / `WHENEVER` / `*PSSR` / `sqlcod`; `SQL7008` case under `*CHG` with unjournaled files; interactive job, record lock per row, no `ALCOBJ`; an `ORD100` confirm mid-run is shifted into the future (`c06`); `:menugrp.Utilities` `SAMMNU:134-163`; `%TEXT` blank on `ORD700`, `ORD900`, `ORD901`, `PRO203` of 37 `QRPGLESRC` members, and on `ART801`; `LASTORDNO.DTAARA` "Source generated by ARCAD Software … 2022-05-17", `VALUE(60719)`; `iproj.json` description `AdrianAtArcad` (`c07`); `QCLSRC` 4 members / `QCMDSRC` 2, none for ORD9xx; no `SBMJOB` / `JOBSCDE` (only `SAMMNU:49 wrksbmjob` on F14); no `*ENTRY` / `PLIST` / `PI`; no `AddLogEntry` / `LOG300` in the two members (`c08`); `lastdate` = `MAX(ORDATE)` at `:11-17`, `today − 10` from `:18`, used at `:34`; `today` at `:24,30` only; `%days(10)` at `:18,35` — the only two in the tree (`c09`).
- Phase A corrections: `c01` ("readp fails/undefined" → writes 0, does not fail). Sharpened: all nine (above). No statement about a valid operation was wrong.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards): `-305` host variable unchanged; `READP` at BOF leaves input fields; numeric init to zero; `OUT` without `*LOCK` unlocks; `UNIQUE` LF rejects duplicate on PF `WRITE`; `%date(n:*iso)` validity → `RNQ0112`; `%diff` sign; `CRTSQLRPGI` `COMMIT(*CHG)` default and implicit commit at activation-group end; `SQL7008`; native `UF` record lock semantics; `TRGUPDCND(*CHANGE)`; `NULL` comparison unknown; UIM `cmd` runs in the interactive job; extra `CALL` parameters ignored without `*ENTRY`.
- Pointers only (not deepened): `ART801` (`sql-objects-c08` / `-c09`, `ord-trigger-ord700-c10`); `ORD700` / `ORD700U` (`ord-trigger-ord700-c04`); `ORD100` (`ord-entry-ord100-c07`); `ORD200` / `ORD201` options 7 / 8; `ORD202` / `ORD500` as readers of the shifted fields; `menu-cmd-shell-c01` (options 80 / 81). No card outside `discovery/ord-batch-ord900/` edited.
- Bind mirror: `pgm:ORD900`, `pgm:ORD901` → `accepted` (were `deferred` from the 2026-09-08 residual bind; the night bind re-accepted the slice). The other four night-wave slices stay `candidate`/`deferred` in APP_MANIFEST until their runs (cap 1). INDEX row 12 → done; header notes run 18 and the superseded deferral.
- Tooling: **`mark_documented.py` changed** (six lines) — surfaces in `deferred` are now re-evaluated on an accepted bind (candidate behaviour unchanged; nothing past `accepted` touched). Reason: the tool only lifted `candidate` surfaces, so the two `deferred` ORD9xx surfaces did not move on the first run (`surfaces status-bumped 0`). After the change: `--slice ord-batch-ord900` → `surfaces status-bumped 2`. A no-`--slice` dry run was inspected (it would have mirrored `cou-maintain` COU200 half deferred → accepted and the three `pro-*` candidate → accepted) and **reverted** — cap-1 practice leaves those for their runs. `gen_coverage.py` 0 problems (documented 164→173; surfaces accepted 42→44, deferred 3→1). `cou-maintain` COU200 half will need the same lift on its run (its seven features are `deferred` in APP_MANIFEST from the 2026-09-08 bind; check the slice MANIFEST says `accepted` before mirroring).
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).

## Run 19 notes (2026-09-09)

- `overnight/stop.txt` absent at start; `overnight/AGENT_JOB.md` line 1 `RUN` (job header: "night residual run 19 — Prefer: cou-maintain COU200 half ONLY (c01–c06 + c13); Do NOT re-card documented FCOUNTRY c07–c12; Hold: art-* / fam-* (ART302), pro-interactive / pro-modules / pro-cobol-pro201 (room-held)"; bind tip `overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md:14` + `discovery/cou-maintain/BIND.md`).
- Station: Document (Phase B). No Architecture-bind, no Convert, no Test gen, no RECORD. `ROOM_OK` not required for this station and not claimed. `atu-merlin-ts-cus-v1`, `atu-merlin-ts-ord-v1`, the five residual packs (incl. `atu-merlin-ts-cou-v1`), the ORD-batch DRAFT pack, `modern/`, `verification/` untouched (`architecture/atu-merlin-cou/PACK.yaml:30,36,67,96-97,140-141`, `modern/README.md:655,681,721,749`, `modern/db/schema.sql:324,330` cited read-only — "panel half out of scope until carded and SUPERSEDEd", "COU200 the only writer of `country`", "no maintenance path"). Carding the half meets the first supersede condition; superseding is the ME's / room's act.
- Trigger fired on `a792163` (ME ord-batch pack refinement pushed while line 1 already read `RUN`). Before writing anything the run listed the automation's agents (RUNNING / NOT_YET_STARTED / WAITING) — it was the only one alive. VM had a scratch branch checked out; switched to `cursor/atu-merlin-estate-discovery` and reset to `origin` before any write; origin re-checked before the first commit and before the push.
- Pick: `cou-maintain` COU200 half (job header names it; the only runnable of four accepted undocumenteds — `pro-*` room-held). Remaining accepted undocumented after this run: `pro-interactive` (15), `pro-modules` (14), `pro-cobol-pro201` (10) — all room-held; **runnable queue empty**.
- 7 candidates all accepted by the bind, all `observed-in-code`. Auto-accept policy not exercised. `c07`–`c12` **not re-carded, not edited** (job header) — their "the deferred `COU200` panel" wording is glossed in the SME_BRIEF, not changed. Five new needs-SME lines in `MANIFEST.yaml` (`c13` retire vs convert + ME supersede question; `c02` ISO-3 validation + unaudited edit; `c04` create / delete in target + out-of-tree loader; `c03` / `c05` / `c06` five presentation yes/nos; `c02` / `c03` build: `WAITRCD`, `CRTRPGPGM` options); the five run-7 lines kept.
- Findings beyond Phase A summaries (as-is, cited in cards): `S01PRP` runs once per call (no reload path, no F5); `PAGEDOWN(25 'dynamic subfile')` conditioned `N80` while 80 is set by the EOF read before every `EXFMT` and `*IN25` never appears → dead keyword, controller pages the extendable subfile; `SFLDSP` only when `RRN01 > 0` → empty file shows heading, no message; option check all-or-nothing per Enter (valid `2`s deferred by any invalid option); page follows first offending row (`ERR01` latch) else `LRRN` (INFDS 378–379); `SFLMSG` literal, not `SAMMSGF`; contrast `CUS200:135` / `PRO200:104` `count < 14` + `pagedown` (`c01`); `S02PRP` shows **file** values (locking `CHAIN`), not the subfile's; `S02CHK` = `MOVE 'ACT'` only; `UPDAT FCOUN` on every Enter incl. unchanged; `COISO` no `CHECK(LC)` (uppercased), `COUNTR` `CHECK(LC)`; no DDS validation keywords; 3 columns, no audit (`CUMOD` / `CUMODID` / `ARMOD` contrast), F-spec col 66 blank (no commitment control); `COISO` shown / edited only on `FMT02`, read only by uncalled `GetCountryIso3` (`c02`); indicator 98 appears once (indicator column), no `INFSR` / `*PSSR`; deleted-row path → stale panel → Enter raises (`RPG1221`-class, inquiry; `G` re-raises, `C` ends); locked-row path → raises before the panel (`WAITRCD` build unknown); changed-row path → last writer wins silently; trigger not in tree (`c03`); estate census: `COU200.RPG:7` only `UF`, `COU300:6` / `COU301:6` `IF`; no `WRITE` / `DELET` / `DELETE` / `INSERT` / `CPYF` / `CLRPFM`; `QCLSRC` 4 / `QSQLSRC` 8 / `QTRGSRC` 3 members, none touch `COUNTRY`; no `CF06` / `CF05`; `CUCOUN` (`CUSTOMER.PF:16`) / `PRCOUN` (`PROVIDER.PF:17`) `REFFLD(COID)` no RI; `CUDEL` / `PRDEL` / `ARDEL` contrast (`c04`); list `*IN03` / `*IN12` branches textually identical; edit F3 `GOTO ENDPGM` out of `S02KEY`; edit F12 → `PANEL = 1` with `STEP01` still `'ACT'` → next `2`'s panel opens at once; no write, no reload, lock held until next `CHAIN` / `LR` (only the last cancelled row stays locked through the redisplay); `CA` keys carry no data (`c05`); `UPDAT SFL01` in `ACT` precedes the edit with old values, no `UPDAT SFL01` after; `READC` yields the next `2` and overwrites the shared fields; rows edited in `COID` order; typed `0` → permanent `SFLNXTCHG` (harmless); `STEP02` left `'KEY'` after cancel (harmless) (`c06`); `QRPGSRC` = 1 member of 138 lines; `SELEC` ⇒ RPG/400 V2R2+; `%TEXT` blank (with ORD700 / ORD900 / ORD901 / PRO203 / ART801); `COU200D:2` the tree's only `AADDSRCARC`, `%%TS` 2016-11-04 V7R1; no compile spec (`QILESRC` = PAR201 / PRO200); no `/COPY`, `CALLB`, `CALLP`, `FCOUNTRY`, `*ENTRY`, `PLIST`; same PRP / LOD / DSP / KEY / CHK / ACT machine as `PRO200.RPGLE:88-174` in fixed form; OPM → `*DFTACTGRP`, own ODP → why `COU300.RPGLE:57`'s cache misses renames; immune to copybook drift / signature bump; coupled to the layout only via `REFFLD` (`c13`).
- Phase A corrections: none. Sharpened: all seven (above). No statement about a valid operation was wrong.
- Platform semantics relied on (flagged inference / runtime-confirmable on the cards and in the CONTEXT_GATE): extendable-subfile paging; `SFLEND(*MORE)` texts; inactive `PAGEDOWN` under an off indicator; `CA` keys; keyboard uppercasing without `CHECK(LC)`; `Y` field keyboard validation; INFDS 378–379; `SFLRCDNBR`; `SFLNXTCHG` / `READC` semantics; `CHAIN` lock lifetime on `UF`; `UPDAT` without prior read → exception → inquiry message replies; record-lock wait; OPM default activation group; `GOTO` out of a subroutine; `SETON LR` end-of-cycle; failed `CHAIN` leaves fields unchanged; RPG/400 `INDARA` mapping.
- Pointers only (not deepened, not edited): `c07` / `c08` / `c12` (FCOUNTRY half); `menu-cmd-shell-c01` (option 21); `pro-cobol-pro201` (COBOL outlier, room-held); `cus-interactive` / `pro-interactive` edit paths (chain-tested contrast). No card outside `discovery/cou-maintain/features/cou-maintain-c0{1..6}.md`, `-c13.md` edited.
- Bind mirror: `pgm:COU200` `deferred` → `accepted` (run 18's tool lift; `surfaces status-bumped 1`); behaviours `c01`–`c06`, `c13` `deferred` → `documented` (7). Inventory now has **no `deferred`** behaviour or surface. The three `pro-*` slices stay `candidate` in APP_MANIFEST until their runs (cap 1). INDEX row 17 → done for all 13; header notes run 19 and the superseded deferral.
- Tooling: `mark_documented.py --slice cou-maintain --note …` (7 bumps, 7 cards, 1 surface bump), `gen_coverage.py` 0 problems (documented 173→180; surfaces accepted 44→45, deferred 1→0). **No tool changes.**
- Pushed once at the end with `AGENT_JOB.md` already `DONE` (every push re-fires the automation; expect one no-op run).
