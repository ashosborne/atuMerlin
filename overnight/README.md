# overnight/

Runtime folder for Pack A / Pack B Cloud Agent runs.

- `RUN_PACK_A` / `RUN_PACK_B` — trigger files (see `../operator/atu-merlin-factory-loop/TRIGGER-CONTRACT.md`)
- `stop.txt` — abort before pack start
- `MORNING_BRIEF.md` — Pack A output
- Do not commit secrets. Never edit `ATU_SRC/**` from this tree.