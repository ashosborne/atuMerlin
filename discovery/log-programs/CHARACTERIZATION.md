# CHARACTERIZATION — log-programs

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-09 (run 14). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/log-programs/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml`. Residual-wave slices are document-only per the bind record; any Architecture pack for them is a **new** pack (never widen `atu-merlin-ts-cus-v1` or `atu-merlin-ts-ord-v1`). The ORD vertical already carries the delete-log *line* into a `samlog` table under its own waiver (`verification/ord-vertical/2026-09-09-r1/PARITY.yaml` `c03`); nothing here extends or re-verifies that.

Observable outcomes a future RECORD would need to capture (from the cards, for later stations only): `DSPOBJD SAMLOG *USRSPC DETAIL(*FULL)` after `CALL LOG100` — library (beside `PARAMETER`), attribute `LOG`, text, **actual allocated size** (`c01`, `c04`); a hex dump of the first 16 bytes after `LOG100` (`X'00000007'` `***` nulls) and after one delete (`pos` advanced by the line length; the line at offset 7; blanks after it) (`c02`, `c03`); one `ORD101` option-`4` delete and the resulting line — `User:` blank-padded to 10, `Date:` 26-character timestamp, `Msg:` text with `ODARID` untrimmed and the *ordered* quantity, `' ***'` terminator (`c03`, `c07`); `CALL LOG100` a second time — the log reset, and whether a job that already logged keeps writing to the replaced copy (`c01`); a `PARAMETER` library **not** on the writer's `*LIBL`, and a fresh library where `LOG100` never ran — deletes still succeed, nothing logged, the joblog's handled escape, and no retry within the job (`c06`); a `QUSCRTUS` failure with and without an old `SAMLOG` present (inquiry vs silent header reset) (`c05`); a scripted mass delete until the log stops — the line count at which it stops, the exception id in the joblog, `pos` unchanged afterwards, deletes still working (`c04`); two sessions deleting simultaneously (or two jobs running a scripted delete) — lost lines and blank runs in the dump (`c10`); menu option 84 — what `ADSPUSRSPC` shows, and `CPD0030` if it is not on `*LIBL` (`c09`); `DSPSRVPGM LOG DETAIL(*PROCEXP)` — one procedure; `DSPPGM ORD700 DETAIL(*SRVPGM)` — which `LOG` it is bound to (`c08`). Two facts must be settled before any RECORD: the actual allocated size of `SAMLOG` on the box (decides the overflow point) and how `ORD700` is bound to `LOG` (decides whether the trigger can be rebuilt from source at all).
