# CHARACTERIZATION — cou-maintain (both halves)

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-08 (run 7, FCOUNTRY half `c07`–`c12`) and extended on 2026-09-09 (run 19, COU200 half `c01`–`c06`, `c13`). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/cou-maintain/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml` for all thirteen behaviours.

The FCOUNTRY half has since been converted under `atu-merlin-ts-cou-v1` (`architecture/atu-merlin-cou/CONVERT_RECORD.md`) and verified under `WAIVED_PATHFINDER` (`verification/cou-vertical/`). That waiver belongs to the COU pack and its verification; nothing here extends it, and this note does not propose widening the pack to the panel half — the pack's own text keeps `COU200` out of scope until it is carded **and** the pack is superseded. Carding happened in run 19; superseding is not this station's act.

## Observable outcomes a future RECORD would need to capture (for later stations only)

FCOUNTRY half (run 7, unchanged): `GetCountryName` / `GetCountryIso3` / `ExistCountry` for a known code, an unknown code, a blank code, and a lowercase variant of a known code, in the sequences *known → same known* (no second read), *known → unknown → same unknown* (miss re-read), *start → blank* (no read, `*off`) and *known → blank* (read, blanks) — to confirm the cache and `%found` reading in `c07`; a name changed by `COU200` in another job while a caller holds a cached hit (`c07` staleness); `SltCountry` opened with a blank code, an existing code, a non-existent code between two keys, and a code beyond the last key (empty frame, `c09`); 19 / 20 / 21 / 41 remaining rows from the position to fix the "Bottom" boundary and the two-pages-per-load paging (`c09`); the F8 / option 8 sequence table in `c10` run side by side on `SltCountry` and `SltArtFam`; and the option-combination matrix in `c11`. Facts the RECORD must settle first because they are not in source: the activation group `CUS200` / `CUS250` / `PRO250` compile into (`c07`, `c12`) and whether anything outside the tree reads `COISO` (`c08`).

COU200 half (run 19):

- `c01` — `call cou200` on a populated file: every row present in `COID` order, `Bottom` on the last page, Page Down / Page Up served by the controller; Page Down on the last page → the controller's own message, program not entered (`DSPPGMREF` / a job trace or a `*IN25` probe would confirm the dead keyword); `call cou200` on an empty file (`CLRPFM` on a copy): heading only, no message. Option `3` on one row and `2` on another, Enter: `Invalid Option`, the `3` row reverse-image with the cursor, the page containing it shown, the `2` **not** actioned; fix the `3` to `0`, Enter: the `2` row's edit panel opens. Enter with nothing typed: redisplay, same page (`LRRN`).
- `c02` — option 2, Enter without typing: `UPDAT` performed (journal or `DSPFD` / `DSPOBJD` change timestamp on the file confirms a write happened); blank name, blank ISO, lowercase ISO (`abc` → `ABC` on the file), mixed-case name preserved, 1-character ISO, an ISO already used by another row — all written, no message; `DSPFD` / `DSPFFD`: three columns, no audit fields; `DSPFD` `COUNTRY` for `WAITRCD` (the build unknown).
- `c03` — while user A sits on `FMT02` for row X: user B `call cou200`, option 2 on X → B's job waits `WAITRCD` seconds then gets an RPG inquiry message **before** the edit panel (the lock case; capture the message id); separately, list a copy of the file, delete row X by SQL from another job, option 2 on X → stale values shown, Enter → RPG inquiry message (capture the id — expected "update without prior read" class), `G` reply → same message again, `C` → program ends.
- `c04` — census only; runtime check is `DSPPGMREF` for `COUNTRY` (expect `COU200`, `COU300`, `COU301` plus the four display files) and `DSPFD` for a trigger / RI constraint (expect none). Ask the data owner how rows were loaded (`DSPJRN` if the file is journaled; otherwise `DSPOBJD` create / restore dates of `COUNTRY`).
- `c05` — list F3 vs F12: both return to the menu. Edit F3: program ends, no write. Edit F12 with two rows selected: the second row's edit panel opens at once; F12 again: list, same page; `WRKOBJLCK` / `DSPRCDLCK` on `COUNTRY` from another job while user A is back on the list after the cancel: the last cancelled row is locked; after F3 the lock is gone.
- `c06` — three rows `2`, Enter, change each name, Enter ×3: edit panels in `COID` order regardless of typing order; list shows the **old** names, `Opt` cleared; option 2 again on one: `FMT02` shows the **new** name; F3, `call cou200`: list shows the new names. Type `0` on a row, Enter, Enter, Enter: no visible effect (the permanent `SFLNXTCHG` flag is only observable in a trace of `READC` counts).
- `c13` — `DSPPGM COU200`: program attribute RPG (OPM), activation group `*DFTACTGRP`; `DSPPGMREF`: `COUNTRY`, `COU200D`, no service program; `DSPOBJD` creation date vs the display file's `%%TS` stamp (2016-11-04). The cache-staleness scenario: job A `CUS200` shows customer with country X (name cached), job B `COU200` renames X, job A re-displays the same customer → old name; job A displays a customer with country Y then X again → new name.

Phase A's `SME_BRIEF` did not name a golden set for this slice; the panel half has no natural golden pair in the estate (it is the only OPM screen), and the getters remain a natural pair with the `FVAT` / `FCUSTOMER` getter cards when Test generation is unlocked for the service-program layer. That remains a recommendation for a later station; nothing here unlocks it.
