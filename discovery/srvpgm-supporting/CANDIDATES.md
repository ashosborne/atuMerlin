# CANDIDATES — srvpgm-supporting (Phase A, unbound)

Seed: `SAMPLE.BNDDIR` + cross-srvpgm binding facts. All rows `candidate`; the four sourceless service programs are recorded as **`unknown` surfaces**, not candidates.

| id | provisional name | evidence | confidence | why it belongs |
| --- | --- | --- | --- | --- |
| srvpgm-supporting-c01 | `SAMPLE.BNDDIR` lists 11 `*LIBL` service programs: `XML`, `ORDER`, `TXT`, `XSS`, `FARTICLE`, `FCUSTOMER`, `FFAMILLY`, `FPARAMETER`, `FPROVIDER`, `FVAT`, `FCOUNTRY`; **`LOG` is absent** | `QBNDSRC/SAMPLE.BNDDIR:8-14` | observed-in-code | The binding contract every `bnddir('SAMPLE')` program relies on |
| srvpgm-supporting-c02 | Four listed service programs have **no source under `ATU_SRC`**: `XML` (used by `PRO202`, bound explicitly by `PRO200.ILEPGM`), `XSS` (used by `PRO203`), `ORDER` and `TXT` (referenced by **no** program in the tree) | `SAMPLE.BNDDIR:10-11`; `QILESRC/PRO200.ILEPGM:9`; `PRO202.SQLRPGLE:11`; `PRO203.PGM.SQLRPGLE:7`; grep `ORDER`/`TXT` srvpgm use = none | observed-in-code (absence) | Blind-spot register; `ORDER`/`TXT` may be dead entries |
| srvpgm-supporting-c03 | Twelve programs bind through the directory (`bnddir('SAMPLE')` on the H-spec / `ctl-opt`): `ART200`, `ART201`, `ART202`, `ART250`, `CUS200`, `CUS250`, `ORD100`, `ORD101`, `ORD200`, `ORD201`, `ORD202`, `PRO250` | grep `bnddir` in `QRPGLESRC` | observed-in-code | Who depends on c01 |
| srvpgm-supporting-c04 | Two programs are built by explicit `CRTPGM` sources instead: `PRO200` (`MODULE(PRO200 PRO202) BNDSRVPGM(XML FCOUNTRY FPARAMETER) ACTGRP(QILE)`) and `PAR201` (`MODULE(PAR201) BNDSRVPGM(FPARAMETER) ACTGRP(QILE)`) — the only members that run in a **named** activation group | `QILESRC/PRO200.ILEPGM:8-9`; `QILESRC/PAR201.ILEPGM:8-9` | observed-in-code | Build facts with runtime consequence (shared AG between PRO200/PAR201 and their srvpgms) |
| srvpgm-supporting-c05 | Four programs have `dftactgrp(*no)` with **neither** `bnddir` nor an `.ILEPGM`: `PRO203` (needs `XSS`, `FPARAMETER`), `ORD500` (needs `FPARAMETER`), `ORD700` (needs `LOG`), `LOG100` (IBM APIs only — would link) — how the first three resolve their imports is ARCAD build metadata (`.elias`, `iproj.json`), not source | `PRO203.PGM.SQLRPGLE:4`; `ORD500.PGM.RPGLE:4`; `ORD700.PGM.RPGLE:4`; `LOG100.PGM.RPGLE:5`; `/workspace/iproj.json` (`buildCommand: elias compile`) | observed-in-code (absence) / inferred (build) | Blind spot already partially noted by Pack B run 4 for `ORD700`/`LOG` |
| srvpgm-supporting-c06 | Signature policy is inconsistent: five binders hard-code `SIGNATURE('V1')` (`FARTICLE`, `FCUSTOMER`, `FCOUNTRY`, `FFAMILLY`, `FVAT`) — exports can be added without breaking callers but also without detection; `FPROVIDER` uses `SIGNATURE(*GEN) LVLCHK(*YES)` with a `*PRV` block; `FPARAMETER` and `LOG` use `EXPORT(*ALL)` with no binder at all | `QSRVSRC/*.BND:4` (five), `FPROVIDER.BND:6,28`; `FPARAMETER.ILESRVPGM:8`; `LOG.ILESRVPGM:8` | observed-in-code | Compatibility model a target API layer should not copy blindly |
| srvpgm-supporting-c07 | All eight service programs are `ACTGRP(*CALLER)`; the eleven `bnddir` programs default to `QILE`-style `dftactgrp(*no)` without `actgrp` (compile default = new AG per program) — so each getter cache (CUS300, ART300, PRO300, …) lives per calling program's activation group, while PRO200/PAR201 share `QILE` | `QILESRVSRC/*.ILESRVPGM:8` (all); H-specs listed in c03/c04 | observed-in-code | Explains cache-staleness scope noted in every `*-modules` slice |
| srvpgm-supporting-c08 | `FARTICLE` is the only srvpgm that binds another (`BNDSRVPGM(FFAMILLY)`); no other inter-srvpgm dependency exists in source | `QILESRVSRC/FARTICLE.ILESRVPGM` (from art-modules); other `.ILESRVPGM` have no `BNDSRVPGM` | observed-in-code | Dependency graph |
| srvpgm-supporting-c09 | `.ILESRVPGM` / `.ILEPGM` / `.BNDDIR` / `.MSGF` members are **ARCAD-generated** command sources ("Source generated by ARCAD Software … 2022-05-17"), i.e. a snapshot of the build at that date; `iproj.json` declares the `elias compile` build with `objlib &verlibatu` | headers of `QILESRVSRC/*`, `QILESRC/*`, `QBNDSRC/SAMPLE.BNDDIR:4-6`, `QMSGFSRC/SAMMSGF.MSGF:4-6`; `iproj.json` | observed-in-code | Provenance; treat as metadata, not behaviour |

## Unknown surfaces (recorded, not candidates)

| surface | why unknown |
| --- | --- |
| `srvpgm:XML` | Listed in `SAMPLE.BNDDIR`, bound by `PRO200.ILEPGM`, procedures called by `PRO202` (`xmlopen`, `xmlStrTable`, `XmlStrRec`, `xmladdcol`, `xmlendrec`, `xmlEndTable`, `xmlclose`) — no source, no copybook |
| `srvpgm:XSS` | Listed; procedures called by `PRO203` (`xssopenfile`, `XssSetDocProperties`, `xsscreatestyle`, `xssSetFontSize`, `XssSetalign`, `Xsssetwrap`, `XssSetFormat`, `XsssetForcolor`, `Xssaddsheet`, `XssSetColWidth`, `xssaddrow`, `xssaddcellChar`, `XssAddCellNum`, `XssGetCellId`, `XssSetAutoFilter`, `Xssclosefile`) — no source, no copybook |
| `srvpgm:ORDER` | Listed; **no reference** anywhere in the tree |
| `srvpgm:TXT` | Listed; **no reference** anywhere in the tree |

## Deferred recommendations (prose only)

- c02: recommend the room ask the source owner whether `XML` / `XSS` are ARCAD sample libraries or third-party, and whether `ORDER` / `TXT` are dead directory entries. Until then the two XML/spreadsheet outputs (`pro-interactive` c07 / c12) cannot be characterized.
- c05 / c06 / c07 / c09: **build metadata**. Recommend `reject` as behaviour and record them in the architecture notes for the target build instead — but do not lose c07, which explains why every getter cache is per-program.
