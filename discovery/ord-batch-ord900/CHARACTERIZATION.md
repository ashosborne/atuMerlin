# CHARACTERIZATION — ord-batch-ord900

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-09 (run 18). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/ord-batch-ord900/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml`. Residual-wave slices are document-only per the bind record; any Architecture pack for them is a **new** pack (never widen `atu-merlin-ts-cus-v1` or `atu-merlin-ts-ord-v1`). The ORD pack lists `ORD900` / `ORD901` / `ART801` as *not converted* (`modern/README.md:405`, cited read-only); this slice does not change that and claims nothing about parity. The Phase A recommendation stands as a recommendation: if the room confirms `c07` (sample-refresh tooling), **defer the slice from conversion scope** and keep it in inventory as a known surface.

What a future runtime check would capture (facts for the SME / ME rather than a RECORD station): `DSPDTAARA LASTORDNO` before and after option 80, once with a populated `ORDER` and once with an empty one — confirms `MAX(ORID)` and the written-as-0 empty case (`c01`); option 81 on an empty `ORDER` — confirms the silent `-305` exit leaves no message (`c02`); `SELECT MAX(ORDATE), MIN(ORDATE), COUNT(*) FROM "ORDER"` before and after option 81 on a given job date — confirms `MAX = today` and that the spread (`MAX − MIN`) is unchanged (`c03`); `SELECT COUNT(*) FROM "ORDER" WHERE ORDATCLO > 0 AND ORDATDEL = 0` and `… WHERE ORDATCLO > 0 AND ORDATCLO = ORDATDEL + 10 days` — how many closes the 10-day rule and the future guard produced (`c04`); `SELECT COUNT(*) FROM DETORD WHERE ODYEAR = 0` before and after — whether option 81 has ever run on the box, and whether `ODYEAR` carries a value anywhere (`c05`); a diff of `CUSTOMER.CULASTORD` against `MAX(ORDATE)` per customer before option 81 — how far the incremental `ORD701` value drifts from the batch definition (`c05`); a diff of `ARTICLE.ARCUSQTY` / `CUSTOMER.CUCREDIT` before option 81, after option 81, and after option 82 — demonstrates the stale window `c04` / `c05` describe; `PRTSQLINF ORD901` / `DSPPGM ORD901` — the `COMMIT` option the object was built with and whether `from order` undelimited compiled (`c06`, `c02`); one deliberate `RNQ0112` (a row with `ORDATDEL = 20230230`) followed by a rerun — shows the partial-run state and the double shift (`c03`, `c06`); `WRKJOBSCDE` — whether anything schedules the two programs (`c08`); `DSPOBJD ORD900 *PGM` / `ORD901` — object text and last-used date, the two facts that would most directly settle `c07`. None of these is a behaviour golden; they are the checks that would confirm or refute the platform-default inferences the cards flag and give the room the `c07` answer.
