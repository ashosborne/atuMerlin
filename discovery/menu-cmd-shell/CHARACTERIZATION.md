# CHARACTERIZATION — menu-cmd-shell

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-09 (run 15). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/menu-cmd-shell/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml`. Residual-wave slices are document-only per the bind record; any Architecture pack for them is a **new** pack (never widen `atu-merlin-ts-cus-v1` or `atu-merlin-ts-ord-v1`). The shell is an adapter (menu, help stubs, message file, command definition); the Phase A recommendation to `reject` it as a conversion slice and use the cards as the navigation / resource spec stands — nothing here is behaviour to characterise in the RECORD sense.

Observable outcomes a future RECORD would need to capture (from the cards, for later stations only): a screen capture of `SAMMNU` as displayed — corner id (`ZMENU`), system name on the top separator, the three group headings and 20 option lines, the key legend across both rows, the `Selection or command` prompt (`c01`, `c03`, `c08`); Enter on an empty line and on `11`, `19`, `50`, `99` — the `CPD9817` text (`c08`); F1 with the cursor on option 1 (`srt200`), on option 6 (`ord100`), on the background (`h`) — the UIM error, and on option 2 / 3 / 4 / 10 — the "Text" / "Help not available" windows and their titles (option 3 showing "Work with Customers" over `ORD201`) (`c04`); each of the 16 `cmd call` options — that the target program starts with no parameter error and the menu redisplays on exit (`c01`); options 12, 13, 84 on a box where the objects exist — the reports' columns and the log dump — and on one where they do not — the escape ids (`c02`); F4 on an empty command line and on a typed command, F9 twice, F6, F10, F14 (`c03`); `DSPMSGD RANGE(ERR0001 ERR2002) MSGF(SAMMSGF)` — the twelve texts, severities and CCSIDs as built (`c05`); on `ART200` an unknown family code — `ERR0001` displayed on the `ARTIFA` field, proving the message live (`c06`); `DSPCMD CVTSPLPDF` — processing program, library, `ALLOW` values — and F4 on the command — the prompt layout with `STMFCODPAG` hidden until F10 and `CUSTOMPAGE` / `BMARK*` appearing only when selected (`c07`); `DSPOBJD PRO250 *PGM` — object text "Display Article" vs the screen's "Provider by Id" (`c09`). Two facts must be settled before any RECORD of the shell as a whole: whether `CUSQRY` / `ARTQRY` / `CUSQRYFMT` / `ADSPUSRSPC` exist on the box (decides whether options 12/13/84 can be recorded at all) and how users reach `SAMMNU` (initial menu / `GO` / sign-on program — decides the entry path of every interactive characterization in the estate).
