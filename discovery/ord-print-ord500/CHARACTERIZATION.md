# CHARACTERIZATION — ord-print-ord500

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-08 (run 12). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/ord-print-ord500/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml`. ORD slices are document-only per the bind record; with this slice the five ORD slices of the 2026-09-08 bind are carded and the ORD Architecture pack is a **new** pack to be drafted next (never widen `atu-merlin-ts-cus-v1`).

Observable outcomes a future RECORD would need to capture (from the cards, for later stations only): the `ORD500O` spooled file for a populated order — company block, customer block, `Order Number yyyy/   nnn` and `Order Date` in the job's date format, the two-line details with the description under the `Article` heading and the with-VAT amount highlighted, `Net` / `VAT` / `Total`, `This is the footer .` (`c01`); the same for an order of 15 lines (one page, totals directly after detail 15) and 16 lines (page 2 with company block and order line but **no customer block**) (`c01`); an empty order (blank totals) and a line with a zero quantity or price (blank cells) (`c01`); the `VAT` line for an order whose lines carry `ODTOTVAT = ODTOT` (blank — `vat-module-c02`) (`c01`); the description printed for a line whose article row is missing (inferred from source: the previous line's) and the header for an order whose customer row is missing (blank block) (`c06`); the stream file `Custord<orid>.pdf` under `PATH` — its name (including case), that a re-print replaces it, that the spooled file remains in the output queue, and what the converter does with a blank or missing `PATH` (`c02`, `c03`, `c04`); the name produced for an order id ≥ 100000 (source says: last digit dropped) (`c02`); the wait and the `Confirmation` window in `ORD100` after a confirm (`c05`); and, first of all, option `6` on a ghost row in `ORD200` and in `ORD201` and on an order deleted by another job between load and option (source says: unmonitored date exception before anything is printed, `RNQ0112` inquiry in the list program's session; fate of the already-opened empty spooled file unknown — `c08`). Three facts must be settled before any RECORD: the build definition / activation group and how `FPARAMETER` is bound (`c02`, `c06`, `c07`); which product supplies `CVTSPLPDF` and whether the PDF is in scope at all (`c04`); and the actual `PATH` value and directory on the box (`c02`).
