# CHARACTERIZATION — sql-objects

```text
CHARACTERIZATION: deferred-waived
Reason: IBM i runtime not available in this environment; documentation from source only.
No RECORD/REPLAY. No goldens. No Conversion unlock claimed by this conveyor.
```

Recorded by the Pack B document-slices conveyor on 2026-09-09 (run 17). This is a **stance note**, not a `WAIVED_*` artefact:
no Test gen pack exists for this slice, so the `waive-characterization` skill was not executed and `testexec/sql-objects/` was not created.
`legacy_green` and `parity_green` stay `false` in `inventory/atu-merlin/APP_MANIFEST.yaml`. Residual-wave slices are document-only per the bind record; any Architecture pack for them is a **new** pack (never widen `atu-merlin-ts-cus-v1` or `atu-merlin-ts-ord-v1`). Two of the five objects already have as-is counterparts inside those packs (`ordercus` view, `cusseq` sequence — `modern/db/schema.sql`, cited read-only on `c01` / `c07`); this slice does not change them and claims nothing about their parity. The Phase A recommendation stands: the two views are **data contracts** of the ORD slices, `ARTIINF` belongs to the ART pack when one exists, `CUSSEQ` / `ART801` are surfaces for behaviours already carded — `sql-objects` is not itself a conversion slice.

What a future runtime check would capture (catalog / SQL facts for the SME or ME rather than a RECORD station): `QSYS2.SYSVIEWS` / `SYSCOLUMNS` for `ORDERCUS` and `ARTLSTDAT` — column data types as materialised (the `SUM` precision `c01` / `c02` / `c03` infer; `DECRESULT`), `COLUMN_HEADING` / `COLUMN_TEXT` (the `LABEL ON` values of `c10`); `SELECT COUNT(*) FROM "ORDER" H WHERE NOT EXISTS (SELECT 1 FROM CUSTOMER WHERE CUID = H.ORCUID)` — how many orphan orders the inner join hides today (`c01`); `SELECT ORID, TOTVAL FROM ORDERCUS WHERE TOTVAL >= 1000000000` — whether the `11Y 2` host-variable cliff is anywhere near (`c01`); a diff of `ARTLSTDAT.QUANTITY` against `ARTICLE.ARCUSQTY` per article — to show the two numbers are different by design (`c03`); `RTVQMQRY ARTQRY` / `RTVQMFORM` — the query text (`c04`, the one retrieval that turns an inference into a fact); `QSYS2.SYSCOLUMNS` for `ARTIINF` — `CCSID`, `LENGTH`, long and system names (`c05`, `c10`); `SELECT COUNT(*) FROM ARTIINF I WHERE NOT EXISTS (SELECT 1 FROM ARTICLE WHERE ARID = I.ARID) OR EXISTS (SELECT 1 FROM ARTICLE WHERE ARID = I.ARID AND ARDEL = 'X')` — orphan and soft-deleted texts (`c06`); `QSYS2.SYSSEQUENCES` for `CUSSEQ` — data type, `CACHE`, `MAXVALUE`, current value vs `MAX(CUID)` (`c07`); `QSYS2.SYSROUTINES` / `SYSPARMS` for `UPDATE_ON_CUS_ORD_QTY` and `DSPOBJD ART801 *PGM` — specific name = program name, object text, compile options (`c08`, `c09`); one deliberate failure of statement 2 to observe the partial-reset behaviour (`c09`); `DSPFFD` on a program-described listing of `ORD901`'s precompiler output — whether `from order` undelimited compiles (`c10`). None of these is a behaviour golden; they are the checks that would confirm or refute the platform-default inferences the cards flag.
