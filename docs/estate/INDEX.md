# atu-merlin estate INDEX (readability mirror)

> Status mirror for Ash. **Factory SoT for radar = `inventory/atu-merlin/APP_MANIFEST.yaml`**; slice truth = `discovery/<SLICE_ID>/MANIFEST.yaml`.
> This file never grants accepts and is not permission to run Phase B. Refreshed by Pack A on 2026-09-08 (run `discovery-phase-a`); status column mirrored by Pack B run 1 (2026-09-08, `document-slices: cus-interactive`) run 2 (2026-09-08, `document-slices: cus-modules`), run 3 (2026-09-08, `document-slices: ord-entry-ord100`) and run 4 (2026-09-08, `document-slices: ord-trigger-ord700` — last slice in the 2026-09-08 bind; conveyor idle until more slices are bound). Rows 13–27 refreshed by the Pack A **residual run** (2026-09-08 19:xx UTC, `pack-a estate residual`): every remaining seed is now Phase A'd or folded; nothing bound. Residual room bind 2026-09-08 (`overnight/BIND_RECORD_RESIDUAL_2026-09-08.md`) accepted rows 19, 21 and the FCOUNTRY half of 17; row 19 status mirrored by Pack B run 5 (2026-09-08, `document-slices: vat-module`), row 21 by run 6 (2026-09-08, `document-slices: dat-utils`), row 17 by run 7 (2026-09-08, `document-slices: cou-maintain` — FCOUNTRY half `c07`–`c12` done, `COU200` half `c01`–`c06`, `c13` mirrored as `deferred`; residual-wave queue now empty, conveyor idle until the next ORD bind wave). ORD-wave room bind 2026-09-08 (`overnight/BIND_RECORD_ORD_WAVE_2026-09-08.md`) accepted rows 6–10; row 6 status mirrored by Pack B run 8 (2026-09-08, `document-slices: ord-entry-ord101`), row 7 by run 9 (2026-09-08, `document-slices: ord-maintain-ord200`), row 8 by run 10 (2026-09-08, `document-slices: ord-maintain-ord201`), row 9 by run 11 (2026-09-08, `document-slices: ord-maintain-ord202`), row 10 by run 12 (2026-09-08, `document-slices: ord-print-ord500` — last of the five ORD-wave slices; ORD-wave queue empty, conveyor idle until the ORD Architecture pack / next bind). Night residual bind 2026-09-09 (`overnight/BIND_RECORD_NIGHT_RESIDUAL_2026-09-09.md`, Ash authorized) accepted rows 12, 13, 14, 15, 18, 20, 25, 26, 27 and the `COU200` half of 17 for documentation; row 18 status mirrored by Pack B run 13 (2026-09-09, `document-slices: par-maintain`), row 20 by run 14 (2026-09-09, `document-slices: log-programs`), row 26 by run 15 (2026-09-09, `document-slices: menu-cmd-shell`), row 25 by run 16 (2026-09-09, `document-slices: srvpgm-supporting` — also applied the `par-maintain-c10` `EXPORT(*ALL)` correction to row 18's slice), row 27 by run 17 (2026-09-09, `document-slices: sql-objects`), row 12 by run 18 (2026-09-09, `document-slices: ord-batch-ord900` — the 2026-09-08 residual bind's `deferred` on this row was superseded by the night residual bind's `accepted`; surfaces `pgm:ORD900` / `pgm:ORD901` lifted deferred → accepted), the `COU200` half of row 17 by run 19 (2026-09-09, `document-slices: cou-maintain` — `c01`–`c06`, `c13` carded; the 2026-09-08 `deferred` superseded; surface `pgm:COU200` lifted deferred → accepted; row 17 now **done** for all 13). The three `pro-*` rows remain `accepted` (cards not yet written; room-held by the job header) until their runs — their rows below still read `candidate` from Pack A and are refreshed one per run.

Legend: `candidate` = Pack A proposed, unbound · `accepted` = room bind 2026-09-08, cards not yet written · `done` = Phase B cards written for every accepted behaviour (SME sign-off still pending) · `deferred` = bind deferred · `folded` = no separate Phase A; content lives in the named slice · counts are behaviours (not progress).

## Scanned in run 1 (Phase A candidates; bind + Pack B status mirrored)

| # | slice_id | domain | status | members (entry) | cand. | deps (not members) | notes |
| ---: | --- | --- | --- | --- | ---: | --- | --- |
| 1 | `cus-interactive` | CUS | **done** (12/12 cards; 3 needs-SME questions) | CUS200, CUS250 (+CUS200D, CUS250D) | 12 | CUSTOMER/CUSTOME1/2, CUSSEQ, FCOUNTRY, FCUSTOMER, ORD200, SAMMSGF | Cards `discovery/cus-interactive/features/`; CUMODID not refreshed on update; UPD duplicate gap; no delete path; CHARACTERIZATION deferred-waived |
| 2 | `cus-modules` | CUS | **done** (10/10 cards; `c10` needs-SME, no card) | CUS300, CUS301 (+CUS301D) | 11 | CUSTOMER/CUSTOME1, FCUSTOMER.ILESRVPGM/.BND, SAMPLE.BNDDIR | Cards `discovery/cus-modules/features/`; `srvpgm-fcustomer` folded in (export facts in c01/c05); ExistCus/IsCusDeleted have no callers in ATU_SRC; last-key cache stale after external updates; SltCustomer criteria persist across calls, SQL errors silent; CHARACTERIZATION deferred-waived |
| 3 | `art-interactive` | ART | candidate (skipped at bind until ART302 answered) | ART200, ART201, ART202, ART250 (+4 DSPF) | 16 | ARTICLE/1/2, ARTIPROV/1/2, ARTIINF, FARTICLE, FFAMILLY, FPROVIDER, FVAT | ART202 called from PRO side |
| 4 | `art-modules` | ART | candidate (skipped at bind until ART302 answered) | ART300, ART301, ART302 (+ART301D) | 11 | ARTICLE/1, ARTIINF, FARTICLE.ILESRVPGM/.BND, FFAMILLY | **ART302 not bound in FARTICLE source** |
| 5 | `ord-entry-ord100` | ORD | **done** (12/12 cards; `c09`, `c11` needs-SME, no card) | ORD100, ORD100C, ORD100C2, CRTORD (+ORD100D) | 14 | ORDER, DETORD (QTEMP staging), LASTORDNO, FCUSTOMER, FARTICLE, FVAT, ORD500 | Cards `discovery/ord-entry-ord100/features/`; core create transaction; starts in add-line panel; F3/F12 on F6 add screen ends program, cancelled F6 prompt re-prompts; footer TOTVAT drifts after delete, totals stale after edit; DETORD.ODYEAR written 0 (ORD901 backfills); no commitment control; no credit/stock/existence checks; CHARACTERIZATION deferred-waived |
| 6 | `ord-entry-ord101` | ORD | **done** (11/11 cards; `c11` needs-SME, no card) | ORD101 (+ORD101D) | 12 | ORDER1, DETORD1, FCUSTOMER, FARTICLE, FVAT, SAMMSGF | Cards `discovery/ord-entry-ord101/features/`; line maintenance (not "entry"); option 6 advertised **`6=Deliver`**, dead; quantity rules compare typed vs **stored** (consistent raise rejected, inconsistent lower accepted); plain Enter re-rates ODTOTVAT at today's VAT; footer stale after edit, no TOTVAT drift on straight delete (hidden field), drift on re-selected blank row; ORDER row locked for the session; no closed-order test inside (ORD201 guards, ORD200 opt 2 unreachable — as-is); no add path; F3=F12 on list; CHARACTERIZATION deferred-waived |
| 7 | `ord-maintain-ord200` | ORD | **done** (12/12 cards; `c12` needs-SME, no card) | ORD200 (+ORD200D) | 13 | ORDERCUS view, ISOTODATE40, ORDER1, DETORD1, CUSTOME1, ORD100C, ORD101, ORD202, ORD500 | Cards `discovery/ord-maintain-ord200/features/`; **option 2 unreachable (precedence defect, as-is)**; already-closed / already-delivered refused with generic **`Invalid Option`** text; deleted row stays on screen (ORD201 blanks it), 7/8 on the ghost row → unmonitored exception; header-first delete leaves orphan lines on failure (ORD201: header with missing lines); close stamps ORDATDEL but leaves ODQTYLIV at 0, deliver skips partial lines and does not close; no orid tie-breaker, whole result loaded, F5 enabled but unlabelled/unhandled; option 3 invalid here, silently accepted in ORD201; ORD201D reuses panel id `ORD200-1`; CHARACTERIZATION deferred-waived |
| 8 | `ord-maintain-ord201` | ORD | **done** (11/11 cards; no needs-SME candidates; 10 needs-SME questions) | ORD201 (+ORD201D) | 11 | ORDERCUS view, ISOTODATE40, ORDER1, DETORD1, ORD100C2, ORD101, ORD202, ORD500 (CUSTOME1/ARTICLE1 declared, unused) | Cards `discovery/ord-maintain-ord201/features/`; menu opt 3, only caller; **only working path to ORD101** (correct closed guard); page is **7 two-line rows** (14 per fetch batch), F11 drops the customer line; "all orders" = orders with a CUSTOMER row (inner join, as-is); **option 3 dead and sticky** (passes, no action, stays typed); options typed before PAGEDOWN survive and run on the next Enter; delete lines-first, row shows blank order/year but keeps dates/value/customer (ghost row → 7/8 exception, same as ORD200); 7/8 byte-identical to ORD200 (cross-referenced, not duplicated; ORD700U `*CHANGE` sharpening); F5 handled; cursor closed in pnl00; panel id `ORD200-1` reused; CHARACTERIZATION deferred-waived |
| 9 | `ord-maintain-ord202` | ORD | **done** (6/6 cards; no needs-SME candidates; 8 needs-SME questions) | ORD202 (+ORD202D) | 6 | ORDER1, DETORD1, CUSTOME1, ARTICLE1 (no service program — `bnddir` inert); callers ORD200/ORD201 opt 5 only | Cards `discovery/ord-maintain-ord202/features/`; read-only, any key closes (Enter, F5, F6 included; F3 = F12); **not-found order id (ghost row after a delete in either list, or concurrent delete) → unmonitored date exception at the unguarded `%date(ORDATE)`, nothing displayed** (ORD500 identical); **no option column** (Phase A corrected); **paging is display-side** (`SFLSIZ 7 ≠ SFLPAG 6` auto-extends — ">7 lines" concern withdrawn); description line **hidden until F11** (`SFLDROP`, 12 truncated / 6 folded per page); missing article repeats the previous line's description (`FARTICLE` would return blank); footer = stored sums; dates `dd/mm/yy` with `01/01/40` → blank; CHARACTERIZATION deferred-waived |
| 10 | `ord-print-ord500` | ORD | **done** (7/7 accepted cards; `c04` needs-SME inferred, no card; 10 needs-SME questions) | ORD500, ORD500C (+ORD500O.PRTF) | 8 | ORDER1, DETORD1, CUSTOME1, ARTICLE1 (direct chains — `FARTICLE`/`FCUSTOMER` bypassed), FPARAMETER (`GetParm2('PATH')`, no `bnddir` in source), CVTSPLPDF (definition only); callers ORD100 confirm, ORD200/ORD201 opt 6 | Cards `discovery/ord-print-ord500/features/`; **not-found order id → unmonitored date exception before the first write, no document, no PDF** (Phase A "prints blank document" corrected; twin of ORD202); **15 detail lines per page** (Phase A said 14), customer block on page 1 only; **PDF name truncates a 6-digit order id** (`5 const` / `&ORD LEN(5)`, `*REPLACE` collision); VAT = stored `TOTTOT − TOTNET` (silent-zero → blank line); `PATH` cached per activation group, blank passed through; `ORD500C` has no `MONMSG` — every failure propagates to the interactive caller; print is an unconditional synchronous side effect of `ORD100` confirm; re-print replaces the PDF; `CVTSPLPDF` processing program not in tree (`c04`); CHARACTERIZATION deferred-waived |
| 11 | `ord-trigger-ord700` | ORD | **done** (8/8 cards; `c01`, `c08`, `c11` needs-SME, no card) | ORD700, ORD700A/D/U.SYSTRG, ORD701.SQLTRG | 11 | DETORD/DETORD1, ARTICLE1, ORDER, CUSTOMER, LOG srvpgm + SAMLOG (not in tree), ART801 (related, sql-objects) | Cards `discovery/ord-trigger-ord700/features/`; hidden side effects of every order write; insert adds full ODQTY, delete/update use ODQTY−ODQTYLIV; only delete logs (SAMLOG, failure swallowed); order close never reaches ORD700; no in-tree writer changes ODARID; UpdArt silent on unknown article, no error handling, can go negative; ORD701 assigns (not MAX) CULASTORD; ART801 "Reset" leaves rows without open orders untouched, only writer of CUCREDIT; AddLogEntry binding not in source; CHARACTERIZATION deferred-waived |
| 12 | `ord-batch-ord900` | ORD | **done** (9/9 cards; `c07` inferred carded; 9 needs-SME lines; Phase A "defer from conversion scope if demo tooling" recommendation unchanged) | ORD900, ORD901 | 9 | ORDER/ORDER1 (+ORDER2/ORDER3 access paths), DETORD/DETORD1, CUSTOMER, LASTORDNO.DTAARA; neighbours ORD100 (#5, LASTORDNO consumer, ODYEAR = 0), ORD200/ORD201 (#7/#8, interactive deliver/close), ORD700U trigger (#11), ART801 (#27, duplicated CULASTORD statement) | Cards `discovery/ord-batch-ord900/features/`; **ORD900**: `SETGT *HIVAL`/`READP` on ORDER1 → `LASTORDNO = MAX(ORID)` (last used, the ORD100 contract); **empty file writes 0** (not a failure); no lock between READP and IN *LOCK; **ORD901**: guard `MAX(ORDATE) = 0` works via the `-305` NULL path (no indicator, no sqlcod anywhere); one offset `days = today − MAX(ORDATE)` for the whole file, sign from the data, same-day rerun a no-op, **rerun after a partial run double-shifts the done rows**; `ORDATDEL`/`ORDATCLO` shifted, future → 0, **hidden 10-day auto-close rule** (`ORDATCLO = ORDATDEL + 10` when delivered > 10 days ago — exists nowhere else), only in-tree path to closed-without-delivery; **ORD901 is the estate's only writer of `DETORD.ODYEAR`** (differential UPDATE; fires ORD700U as a zero-delta no-op; orphan lines skipped); `CULASTORD = MAX(ORDATE)` **verbatim `ART801:35-37`**; `ARCUSQTY`/`CUCREDIT` **left stale** after its closes until opt 82; native `UF` without COMMIT, SQL side has **no `SET OPTION COMMIT`** (build option not in tree — `PRTSQLINF` to settle), no handler; purpose = sample-data refresh (inferred: Utilities group, "Reset …" texts, blank `%TEXT` on exactly ORD900/ORD901/ART801, ARCAD-generated DTAARA); no CL/CMD/SBMJOB/JOBSCDE/parameters — two bare interactive `cmd call`s; `lastdate` reused as `today − 10` (`:18`), `%days(10)` twice; CHARACTERIZATION deferred-waived |

## Scanned in the residual run (Phase A candidates, unbound — residual, not a completeness claim)

| # | slice_id | domain | status | members (entry) | cand. | deps (not members) | notes |
| ---: | --- | --- | --- | --- | ---: | --- | --- |
| 13 | `pro-interactive` | PRO | candidate | PRO200 (+bound PRO202), PRO250, PRO203 (+PRO200D/202D/250D) | 15 | PROVIDER/PROVIDE1, ARTICLE, ARTIPROV, FCOUNTRY, FPARAMETER (PATH), FPROVIDER, ART202, **XML / XSS srvpgms (no source)** | **PRO200 edit never saves** (planted bug, `mode` never set); purchase proposal writes `Pur_Ord_*.xml` and **does not update ARPURQTY** (no writer anywhere); PRO203 spreadsheet uses a different filter (`ARCUSQTY > 0`); no provider create/delete; recommend splitting PRO203 at bind |
| 14 | `pro-modules` | PRO | candidate (folds #24) | FPROVIDER = PRO300, PRO301 (+PRO301D) | 14 | PROVIDER/PROVIDE1 | Twin of #2; only `GetProName` (ART201/202) and `SltProvider` (PRO250) have callers; F8 dead in selector; **only versioned binder** (`SIGNATURE(*GEN)` + `*PRV`) |
| 15 | `pro-cobol-pro201` | PRO | candidate | PRO201.CBL (+PRO201D) | 10 | PROVIDE1, ART202 | Only COBOL member; read-only, superseded by PRO200; **F3 on detail re-reads without repositioning (likely empty list)** — confirm on box; retire vs convert is a room call |
| 16 | `fam-maintain` | FAM | candidate | FFAMILLY = FAM300, FAM301 (+FAM301D) | 13 | FAMILLY/FAMILL1; callers ART200/250/301, FARTICLE | **No family maintenance program exists** (id is a charter misnomer); **`ExistArtFam` ignores FADEL** (unlike every other Exist*); `FAVATCD` never read; keyed-read window with by-code/by-desc toggle |
| 17 | `cou-maintain` | COU | **done** (13/13 cards: FCOUNTRY half `c07`–`c12` run 7, `COU200` half `c01`–`c06`, `c13` run 19; 10 needs-SME questions) | FCOUNTRY = COU300, COU301 (+COU301D); COU200.RPG (OPM, the only `QRPGSRC` member) + COU200D | 13 | COUNTRY/COUNTR1; callers CUS200/250, PRO200/250; SAMPLE.BNDDIR, PRO200.ILEPGM; menu opt 21 | Cards `discovery/cou-maintain/features/`. FCOUNTRY: getter cache keyed on the record buffer's own `COID` (hit cached, miss re-read, blank never reads); no delete flag → exists = row present; `GetCountryIso3` unused and `closeCOUNTRY` not exported; `SltCountry` is a **native keyed read**, 20 rows/load, empty frame with no message beyond the last key; **Phase A corrected:** the COU301/FAM301 F8 divergence is source-only; literal `'V1'` signature, `PRO200` bound via `BNDSRVPGM`. COU200: loads **every** row in one no-lock pass, `PAGEDOWN(25)` keyword **dead** (controller pages); `S02CHK` is empty — **every Enter writes**, no validation of name / ISO-3, no audit columns; `COU200.RPG:7` is the **only** update-capable declaration of `COUNTRY` in the tree — no create / delete anywhere (countries loaded out of tree, unknown how); chain not-found 98 never tested (unhandled exception if a row vanishes; no in-tree trigger); list F3 = F12; edit F12 → **next selected row** with the row still **locked**; edited subfile row never rewritten (stale list); retire-and-replace vs convert → room (`c13`); COU pack keeps the panel half out of scope until superseded; CHARACTERIZATION deferred-waived |
| 18 | `par-maintain` | PAR | **done** (13/13 cards; `c08` inferred carded; 10 needs-SME questions) | PAR200, PAR201 (CL), FPARAMETER = PAR300 (+PAR200D) | 13 | PARAMETER; consumers ORD500/PRO202/PRO203 (PATH); LOG100 (library anchor) | Cards `discovery/par-maintain/features/`; generic key/value store with **one live key (`PATH`)**, `GetPARM1/3/4/5` and `ClosePARAMETER` uncalled; **`*** Deleted ****` marker never displayed** (`PARM2` not a subfile field — Phase A corrected), ghost row can delete the blank/blank row; **F6 pages forward and duplicates the last row after `Bottom`**; edited rows stale in the list until F5; duplicate check locks and loads the existing row; **`PATH` must end with `/`** for `PRO202`/`PRO203` (`ORD500C` tolerant, `PAR201` lists `<PATH>*`); getter cache on the buffer's own key — first read per job is the value for the job, blank key never reads, `closePARAMETER` would not refresh; blank `PATH` silent end to end; `EXPORT(*ALL)`; recommend PATH as target configuration; CHARACTERIZATION deferred-waived |
| 19 | `vat-module` | VAT | **done** (10/10 cards; 6 needs-SME questions) | FVAT = VAT300 | 10 | VATDEF; callers ORD100/ORD101 via GetArtVatCode, ART250 direct; ART200 (VAT-code entry, unvalidated) | Cards `discovery/vat-module/features/`; `ClcVAT` = VAT amount, half-adjust 2 dp; **unknown VAT code → 0 silently** (blank code never reads); soft-deleted codes still applied (`ExistVATRate` uncalled); no rate maintenance; ART200 dead VAT display fields; CHARACTERIZATION deferred-waived |
| 20 | `log-programs` | LOG | **done** (10/10 cards; `c04`/`c06`/`c09`/`c10` inferred carded; 9 needs-SME questions) | LOG100, LOG = LOG300 | 10 | SAMLOG *USRSPC (runtime), PARAMETER (library only), ORD700 (only writer); target counterpart `samlog` table in the ORD pack (not widened) | Cards `discovery/log-programs/features/`; 5000-byte user space, no auto-extend, **600-byte fixed write per line → the log fills after ≈ 35 ORD700 lines (Phase A said 40–60) and then fails permanently** — the failing write leaves the cursor unchanged, every later call in every job fails at the same offset, all swallowed by `ORD700`'s `callp(e)`; **`inz` is set before the failing `QUSPTRUS`**, so a missing `SAMLOG` disables logging for the whole job with no retry; `QUSCRTUS` error swallowed but `QUSPTRUS` (no error code) surfaces it — or silently resets an old log's header; `User` stamped at service-program activation, blank-padded; message > 437 chars loses the `' ***'` terminator; `EXPORT(*ALL)` exports exactly one procedure; LOG not in BNDDIR / ORD700 no `bnddir` (build blind spot); ADSPUSRSPC reader not in tree; unlocked read-modify-write of the cursor; recommend replace with target logging (ORD vertical already has `samlog`); CHARACTERIZATION deferred-waived |
| 21 | `dat-utils` | DAT | **done** (8/8 cards; 5 needs-SME questions) | ISOTODATE40 → DAT002, ISO_Num_To_Date → DAT001 | 8 | callers ORD200/ORD201 (ISOTODATE40 only); ORDERCUS view (raw columns); CUS200 / ORD202 / ORD200 / ORD201 hold the same `1940-01-01` constant | Cards `discovery/dat-utils/features/`; pure functions, `0 → 1940-01-01`, `99999999 → 2039-12-31` (no in-tree producer, no MAPVAL), invalid → NULL; callers fetch **without null indicators** — a NULL ends the list load silently; sentinel produced 3× (SQL, CUS200 explicit, ORD202 implicit via `*inzsr` preset) and compared in ORD200/ORD201; interface is 8 parameters (Phase A said 9); ISO_Num_To_Date unused (QM queries not in tree); CHARACTERIZATION deferred-waived |
| 22 | `srvpgm-fcustomer` | SRVPGM | folded into #2 (bind 2026-09-08) | — | — | — | export facts in #2 cards c01/c05 |
| 23 | `srvpgm-farticle` | SRVPGM | folded into #4 (recommendation) | — | — | — | surface `srvpgm:FARTICLE` + #4 c06/c10 carry the export facts and the ART302 gap |
| 24 | `srvpgm-fprovider` | SRVPGM | folded into #14 (residual run) | — | — | — | #14 c11/c12 |
| 25 | `srvpgm-supporting` | SRVPGM | **done** (9/9 cards; `c05` inferred carded; 7 needs-SME questions; build contracts — reject-as-slice recommendation unchanged) + 4 `unknown` surfaces | SAMPLE.BNDDIR, PRO200/PAR201.ILEPGM | 9 | all 8 srvpgms + 12 `bnddir` programs + PRO203/ORD500/ORD700/LOG100 (binding facts only — H-specs, `/COPY`, `export` keywords, call sites) | Cards `discovery/srvpgm-supporting/features/`; 11 `*LIBL` entries in `SAMPLE`, **XML/ORDER/TXT/XSS have no source and PRO202/PRO203's `/copy qprotosrc,xml|Xss` copybooks are missing too** (7 + 16 procedure names are the only interface spec; `unknown` surfaces kept), LOG absent; six entries consumed; **12 `bnddir` programs → 9 import from 6 srvpgms, ORD200/201/202 import nothing (inert), ART250 imports `GetArtInfo` that no srvpgm exports (ART302 question restated)**; PRO200/PAR201 `CRTPGM` `BNDSRVPGM` lists match imports exactly, PRO202 is a module not a `*PGM`; **PRO203/ORD500/ORD700 bind by build metadata only, LOG100 imports nothing; a build-level `BNDDIR(SAMPLE)` cannot explain ORD700**; every `.BND` matches its modules' `export` keywords (10/4/15/4/14/4), **`EXPORT(*ALL)` = 5 (FPARAMETER) and 1 (LOG), not every procedure — par-maintain-c10 corrected**; seven `Close*` copybook prototypes have no export; **Phase A corrected: default activation group is `QILE`, not new-per-program → getter caches most likely job-wide (runtime-confirm `DSPPGM DETAIL(*BASIC)`)**; one srvpgm→srvpgm edge (FARTICLE→FFAMILLY via ART301); 13 ARCAD snapshots 2022-05-17, elias build, no compile options in tree; CHARACTERIZATION deferred-waived |
| 26 | `menu-cmd-shell` | SHELL | **done** (9/9 cards; `c02` inferred carded; 8 needs-SME questions; adapter — reject-as-slice recommendation unchanged) | SAMMNU.MENU, SAMHELP.PNLGRP, SAMMSGF.MSGF, CVTSPLPDF.CMD | 9 | every called program (cited for existence / entry declarations / `%TEXT` / headings only), ORD500C (the one CVTSPLPDF call), log-programs-c09 (option 84) | Cards `discovery/menu-cmd-shell/features/`; entry map for 20 options in three groups — **all 16 in-tree targets confirmed parameterless**, option 6 goes through `ORD100C2` because `ORD100` takes `cuid *nopass`, option 82 calls the `SPECIFIC`-named `*PGM` of SQL procedure `UPDATE_ON_CUS_ORD_QTY`, 80/81/82 unguarded resets, nothing in the tree invokes the menu; **opts 12/13/84 reach QM queries / ADSPUSRSPC not in tree** (Reports group holds zero documentable reports; menu titles are the only spec); long command line + F6/F10/F14 operator keys, no restriction in the panel; help: **three undefined names (`h`, `srt200`, `ord100`), orphan `ART200` module, option 3 mis-targeted at `cus200`**, every body "Text" / "Help not available"; **9 of 12 messages live — Phase A's "ERR0001 unused" corrected (`ART200D.DSPF:98`)**, dead = ERR0003/0004/0005, 12 `ERRMSGID` sites in 9 DSPFs, mandatory-field texts are DSPF literals; CVTSPLPDF 14-parameter contract, **processing program is a `CRTCMD PGM()` build step and no `CRTCMD` exists in the tree** (same for CRTORD), `PMTCTL` only — no `DEP`; `submsgf=sammsgf` declared but unused; `PRO250` "Display Article" is `%TEXT`-only (screen says "Provider by Id"); CHARACTERIZATION deferred-waived |
| 27 | `sql-objects` | SQL | **done** (10/10 cards; `c04` inferred carded; 9 needs-SME questions; data contracts — not-a-conversion-slice recommendation unchanged) | ORDERCUS, ARTLSTDAT (views), ARTIINF (table), CUSSEQ (sequence), ART801 (procedure) | 10 | ORDER/ORDER1, DETORD/DETORD1, CUSTOMER/CUSTOME1, ARTICLE/ARTICLE1/ARTICLE2, SAMREF; readers ORD200/ORD201 (#7/#8), writer ART200 + reader ART302 (#3/#4, held — `ARTIINF` statements only), CUS200 (#1), SAMMNU opts 13/82 (#26), ORD901 (#12, spelling only) | Cards `discovery/sql-objects/features/`; **ORDERCUS inner-joins CUSTOMER** — `CUSTOME1` UNIQUE so no duplication, no `CUDEL`/`ORDATCLO` filter, orphan orders hidden and reachable in-tree only via ORD100's unchecked `ORCUID` (no in-tree customer delete); readers fetch TOTVAL into `11Y 2`; **TOTVAL is gross (`ODTOTVAT`), the same field ART801 sums into CUCREDIT**; ARTLSTDAT = ordered qty over all orders (≠ ARCUSQTY), joins on ORID only, soft-deleted articles included, never-ordered excluded, **no reader in tree — ARTQRY (opt 13, no QMFORM) inferred**; ARTIINF: only SQL table, long + system names (ART200 uses long, ART302 short), `CCSID 297` (only explicit data CCSID), ART200 opt 3 insert-or-update of `TRIM(:text)` with **any read error → create → unchecked duplicate-key INSERT (edit lost)**, rows never deleted and still editable after opt-4 soft delete; **CUSSEQ unbounded into a `5P 0` CUID** (value 100 000 overflows, no sqlcod check); ART801: `SPECIFIC` = program name → `cmd call art801`, only the menu calls it, ORD901 duplicates statement 3 inline; `COMMIT *NONE` + no handler → partial reset on failure, `DECRESULT (31,31,00)`; naming: `"ORDER"` delimited ×10, **`from order` undelimited ×3 in ORD901**, LABEL ON headings copy SAMREF COLHDG incl. `DESCRPTION` typo, PRIM_ARTIINF the only named constraint; CHARACTERIZATION deferred-waived |

## Blind spots (see `overnight/METHOD_COVERAGE.md`)

XML/ORDER/TXT/XSS service programs and their copybooks; QM queries CUSQRY/ARTQRY/CUSQRYFMT; CVTSPLPDF and ADSPUSRSPC implementations; CMD→PGM bindings and import resolution for PRO203/ORD500/ORD700/LOG100; trigger attachment; SAMLOG capacity; who maintains PROVIDER/COUNTRY/FAMILLY/VATDEF rows and ARPURQTY; runtime-only behaviour (no IBM i available). Every `ATU_SRC` member has now been read once — that is inventory coverage, not migration progress and not completeness.
