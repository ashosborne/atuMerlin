# Verification REPORT — LOG vertical (atuMerlin), run `2026-09-09-r1`

pack_id@version: **`atu-merlin-ts-log-v1@1`** (`architecture/atu-merlin-log/PACK.yaml`, `status: BOUND`, bound 2026-09-09T11:12:14Z by the atuMerlin migration room — Field 9/10 + CTO skim; soft notes tip 4f42dcf; Agent Smith recorded; BOUND tip ad697f3)
Station: Verification · Mode: **COMPARE at the TypeScript boundary only** (plus the additive comments on the ORD `samlog` table) · Characterization: **`WAIVED_PATHFINDER`** (ADR 0001)
Authorisation: Verification `ROOM_OK` in `overnight/AGENT_JOB.md` (line 4: "ROOM_OK: verification LOG only (atu-merlin-ts-log-v1@1 BOUND; Convert DONE tip 666330d)"; Field + CTO Verify ROOM_OK batch of five 2026-09-09), separate from BIND and from the convert `ROOM_OK`. Paste: `PASTE-verify-log-vertical-atu-merlin.md` @ 744ac29 (job body @ `1e41dc3`).
Branch `cursor/atu-merlin-estate-discovery` @ `1e41dc3` · PR [#1](https://github.com/ashosborne/atuMerlin/pull/1) → `master` · 2026-09-09 19:50–20:10 UTC · Cloud Agent (atuMerlin factory job runner, sole run)

## 1. Verdict

**`parity: TS_BOUNDARY_GREEN`** — see `PARITY.yaml`.

What that means, exactly:

- `npm run typecheck` is clean and `npm test` is 306/306 against live PostgreSQL 16.15 (24 LOG: `test/samlog.test.ts`; the 55 CUS, 56 ORD, 27 VAT, 63 DAT, 41 COU and 40 PAR tests unchanged and green).
- Every export of `modern/src/shared/samlog/index.ts` behaves the way the ten `log-programs` cards and the README LOG section say it should, checked by a probe whose oracles were written from the cards and the cited source lines, not from the module or its vitest suite. `LOG100` + `LOG300` are replayed in JavaScript as a **byte machine over a user space** — the 7-byte header with a big-endian `pos`, the `entry 500A value` parameter through `%trim`, the line built in a `500 varying` field, the 600-byte fixed write that fails when `pos + 600 > size` *before* `pos` is advanced, and the unlocked read-modify-write of `pos` — and the module is compared with it on the line string (3 000 triples including the 437 / 438 boundary and the card's own example line), the stored message (3 000 `%trim` entries, 500 module writes), the timestamp form (1 500 timestamps round-tripped through the table), and the decoder's output over the machine's bytes (200 spaces, 2 820 lines, plus a CP037-encoded copy). The `ORD700` message is rebuilt from `ORD700.PGM.RPGLE:78-81` and compared on 300 `DETORD` deletes with what the ORD trigger writes **and** with what the LOG module writes for the same event — the two rows are indistinguishable on `msg`, `user_id` and the rebuilt legacy line. **18/18 cases, stable over three runs** (`evidence/results.json`).
- The three deliberate deltas the README declares are observed *as deltas*, not assumed: the machine at 5 000 bytes accepts **35** ORD700 lines and then fails silently and permanently while the table keeps 60 of 60 (CR-L5); a failing insert **throws 42P01** to the caller and the next call writes again, where the legacy swallowed the error and never retried (CR-L6); **200 of 200** concurrent appends are kept where the machine under the card's interleaving loses one line per racing pair and leaves blank runs (CR-L8).
- The planted behaviours and known risks are present and asserted as-is, not fixed (§4, "Preserved").
- Every card is either matched at the boundary or carries a reason that traces to the pack; there are **no unexplained card gaps**. Two **observations** were recorded (§4, G-L1, G-L2): the `Db = pg.Pool` parameter type rejects the transaction client the documented actor alignment depends on (it works at runtime), and the width constants count characters where the box counted single-byte EBCDIC bytes. Neither changes an answer for any input an in-tree caller can produce.

What it does **not** mean:

- **Not `PARITY=GREEN` against IBM i.** No IBM i / RPG goldens exist for this pathfinder and none were invented. `REPLAY_GREEN` is not claimed; `legacy_green` and `parity_green` stay `false`; `replay_green_run_ids` stays `[]`. The oracle here is the Discovery cards (read from source, never executed on the box) and the README's stated contract.
- Not "Merlin migrated", and not "LOG migrated": the `SAMLOG` user space itself — `LOG100` (c01, c05, c06), the 5 000-byte capacity (c04), the in-band cursor (c02 storage, c10) and the reader `ADSPUSRSPC` (c09) — is residual by design and stays on IBM i; the LOG pack's deliverable is one shared module writing to the table the ORD vertical already writes. **Whether the user-space log is to be preserved at all** (pack `known_risks`: "whole-slice may be non-functional side effect") and **whether the legacy line string is a contract** (SME_BRIEF item 7) are the room's questions and stay open. No business value is claimed for the slice; the module has no caller under `modern/src` (P07) and that is documented as fine.
- CUS, ORD, VAT, DAT, COU and PAR are not re-verified as this job's claim. The ORD trigger `ord700_detord_delete` is *exercised* by the probe (P05, P06, P07) only to compare its row with the LOG module's row — the trigger's own behaviour was verified under the ORD pack (`verification/ord-vertical/2026-09-09-r1`, `ord-trigger-ord700-c03` `as_is`) and is not widened here.

**Talk-track:** LOG residual verified at TS API under waiver — not Merlin migrated; the `SAMLOG` user space (`LOG100`, its reader `ADSPUSRSPC`) stays on IBM i, and the room still decides whether the user-space log is preserved at all and whether the line string is a contract.

## 2. Gate checks

| Gate | Result |
| --- | --- |
| `PACK.yaml` status | `BOUND` — proceed |
| Verification `ROOM_OK` in job body | present (`overnight/AGENT_JOB.md` line 4, "ROOM_OK: verification LOG only") |
| Convert complete | yes — `architecture/atu-merlin-log/CONVERT_RECORD.md`, code `2b0fa73`, docs `4b2edc2`, DONE stamp `666330d`, handoff `PARITY=UNVERIFIED` |
| Another job `RUN` (Smith gate) | no — `list-cloud-agents` showed this run as the only active automation run at start and again before the first commit; branch head = trigger head `1e41dc3`; `AGENT_JOB.md` line 1 `RUN` on origin both times |
| `overnight/stop.txt` | absent |
| Scope | LOG only (`log-programs` c01–c10: `shared/samlog`). CUS / ORD / VAT / DAT / COU / PAR not re-verified; ART untouched; `features/order/**` not rewritten (the trigger is compared with, not changed) |
| Edit surface | written: `verification/log-vertical/2026-09-09-r1/**`, `modern/README.md` (the LOG status sentences only — header line and the LOG section's waiver paragraph — as the VAT / DAT / COU / PAR verify runs did; this job body carries no STRICT line) and `overnight/AGENT_JOB.md` line 1. Untouched: `ATU_SRC/**`, `discovery/**`, `inventory/**`, `architecture/**` (this pack's `PACK.yaml` stays `verification: DEFERRED` — BOUND is immutable), every sibling architecture directory, `modern/src/**`, `modern/db/**`, `modern/test/**`, `modern/openapi/**` |
| CUS / ORD packs not widened | `git diff --stat fc39676..HEAD -- modern/src/features/customer modern/src/features/order modern/openapi modern/src/db modern/src/app.ts modern/src/server.ts architecture/atu-merlin architecture/atu-merlin-ord architecture/atu-merlin-vat architecture/atu-merlin-dat architecture/atu-merlin-cou architecture/atu-merlin-par` is empty (`fc39676` = the convert RUN tip, the last commit before the LOG code); the first 371 lines of `modern/db/schema.sql` (CUS + ORD + VAT + DAT + COU + PAR objects) are byte-identical to `fc39676`; the LOG section is four `COMMENT ON` statements (P16); CUS / ORD suites 55 / 56 unchanged; nothing under `modern/src` imports `shared/samlog` (P07) |
| `ATU_SRC/**` | `git diff origin/master..HEAD -- ATU_SRC` is empty |
| Goldens | none exist; nothing RECORDed, nothing rewritten |

## 3. Commands run

Environment: Node v22.14.0 (pack minimum 20), PostgreSQL 16.15 (`modern/scripts/local-pg.sh`, port 54329; server binaries installed on the runner for this session), fresh `npm ci`.

| Command | Result | Evidence |
| --- | --- | --- |
| `cd modern && npm run typecheck` | `tsc --noEmit` exit 0, no diagnostics | `evidence/typecheck-and-vitest.log` |
| `cd modern && npm test` | vitest 3.2.7: **11 files, 306 tests, 306 passed** (`dat.test.ts` 63, `order.api.test.ts` 47, `fcountry.test.ts` 41, `customer.api.test.ts` 29, `fvat.test.ts` 27, `par.api.test.ts` 27, `samlog.test.ts` 24, `fcustomer.test.ts` 16, `parm.test.ts` 13, `customer.web.test.ts` 10, `order.web.test.ts` 9), 4.50 s | `evidence/typecheck-and-vitest.log` |
| independent COMPARE probe (`DATABASE_URL=... npx tsx evidence/probe.mjs`) | **18 PASS / 0 FAIL** (run three times; counts stable, random messages / users / rows differ) | `evidence/results.json` |
| `tsc --strict` on a two-line file calling `createSamlog(pool)` and `createSamlog(client)` (outside the repo, deleted) | first accepted, second **TS2345** — the G-L1 fact | §4, G-L1 |

The vitest suite is modern's own; on its own it never sets a green flag (Field Guide anti-greenwash). The probe is the independent check. Its oracles are written from the cards: (1) the fixed-length alpha assignment (cut right, pad blanks) and `%trim` with no character list (blanks only, both ends) for `entry 500A value` and `User 10`; (2) `LOG300.AddLogEntry` as `'User: ' + User + ' * ' + 'Date: ' + ts + ' * ' + 'Msg: ' + %trim(entry) + ' ***'` into a `500 varying`, then a 600-byte fixed write at `p1 + pos` and `pos += %len(data2)`; (3) `LOG100`'s header (`pos = 7`, `'***'`, `X'00'` fill) and `QUSCRTUS` size 5 000 with no auto-extend, so a write fails when `pos + 600 > size` and `pos` is not advanced (c04); (4) the c10 "Interleaving A" (A27 B27 A31 A32 B31 B32) as two writers sharing one `pos`; (5) the `ORD700` message with `%char` of `ODORID` / `ODLINE` / `ODQTY` (no leading zeros, `-` only when negative) and `ODARID` untrimmed at 6; (6) `%char(%timestamp())` as `YYYY-MM-DD-HH.MM.SS.UUUUUU` from components; (7) CP037 byte values for the line's charset, to exercise the c02 "an EBCDIC copy needs the box's CCSID" note rather than assume it. It builds a throw-away schema through `modern/test/helpers/db.ts`, drives the module against it, fires the ORD trigger by deleting `detord` rows written directly (no ORD feature code), boots `buildApp()` once to check the HTTP surface for absence, and drops the schema.

Two probe cases failed on their first runs, both probe-script faults, neither a module fault: the "nothing named `init`" filter matched the constant `SAMLOG_INITIAL_SIZE` (fixed to test function names only), and the "ten blank entries" assertion did not allow for the random generator also producing empty entries (fixed to a lower bound). No module or server behaviour was changed. The committed `results.json` is a clean run.

## 4. Card COMPARE — cards vs exports vs tests vs probe

Legend: export = function or method in `modern/src/shared/samlog/index.ts`; test = vitest block in `test/samlog.test.ts`; probe = case id in `evidence/results.json`.

### log-programs (c01 … c10)

| Card | README status | Export | Test | Probe | COMPARE outcome |
| --- | --- | --- | --- | --- | --- |
| c01 LOG100 creates `SAMLOG` (5 000 bytes, `replace *YES`, beside `PARAMETER`), writes the 7-byte header | **residual** | — (`applySchema`) | — | P13, P15 | **residual explained**: the table exists once `applySchema` has run (idempotent `CREATE TABLE IF NOT EXISTS` in the ORD section), the first call in a fresh schema writes (P13); nothing named `reset` / `truncate` / `init` in the module and its only SQL is the `INSERT` (P15). No reset path (CR-L1); the library anchor (`par-maintain-c13`) has no meaning in one schema |
| c02 Layout: big-endian cursor at 0, `'***'` at 4, entries from 7, `' ***'` the only delimiter, 600-byte pad | converted (decoder) / **residual** (storage) | `decodeSamlogUserSpace` | c02 / c09 (6) | P09, P10, P11 | match: 200 machine-built spaces (sizes 5 000 … 65 536, 2 820 lines) decode to the lines written — `pos` = 7 + the sum of *logical* lengths (the 600-byte pads are not counted), marker seen, no terminator on any line, empty tail; a fresh space → `pos 7`, no lines; the c09 reader rule applied by hand agrees. A **CP037 copy** is unreadable with the default `latin1` text (no marker, no split) and exact with the injected text and terminator bytes — the "runtime CCSID" note exercised. Storage is rows, not bytes (CR-L2); no migration performed |
| c03 AddLogEntry: `init` once per activation group; `'User: ' + User(10) + ' * Date: ' + %char(%timestamp()) + ' * Msg: ' + %trim(entry) + ' ***'` in a `500 varying`; 600-byte write; `User` stamped at activation, padded; `entry` 500 by value; > 437 loses the terminator | converted (**as-is**) / needs-SME | `addLogEntry`, `normaliseLogEntry`, `formatLegacyLine`, `toRpgTimestamp` | c03 (11) | P01–P05, P17, P18 | match: `formatLegacyLine` == the line machine on 3 000 triples (430 of them over 437 lose `' ***'` in both; 437 keeps it at exactly 500, 438 does not) and rebuilds the card's example line character for character (P17); `normaliseLogEntry` == `500A` + `%trim` on 3 000 entries — cut **before** trim (499 blanks + `ZQ` → `Z`; 500 blanks + `Z` → `''`), leading blanks lost, tabs / NBSP / newlines kept (P02); `toRpgTimestamp` == `%char(%timestamp())` on 1 500 timestamps read back through the pool with fractions 0 / ms / tenths / random micro (P03); 500 module writes store the oracle `msg` and the named user cut to 10 with case kept, `logged_at` inside the call window, blank entries still write (P04). Actor: the role through the pool; `atu.user` when handed the transaction's client, where the trigger row of the same transaction names the same user; the setting does not leak to the next pool call; a named `''` is stored `''`, not defaulted (P05). Per-event actor is CR-L4 (SME confirmation open); the stored `msg` is not cut at 437 (CR-L3). **G-L1**, **G-L2** below |
| c04 No capacity check: silent, permanent stop once `pos + 600 > size` (≈ 35 ORD700 lines at 5 000) | **residual** (`inferred` kept) | — | c04 (1) | P12 | **residual explained, delta observed**: the machine with 127-byte ORD700 lines accepts **35** calls (`7 + 35 × 127 + 600 = 5 052 > 5 000` on the 36th — the card's "≈ 35"), `pos` then stays at 4 452 and the remaining 25 calls all fail; at an 8 192-byte allocation **60** lines (the card's "≈ 60"). The table keeps 60 of 60 (CR-L5). `TODO(log-programs-c04)` present; nothing caps or rotates; no SME answer invented |
| c05 LOG100: `QUSCRTUS` error swallowed; `QUSPTRUS` escapes one line later or silently resets an old log's header | **residual** | — | — | P13, P15 | **residual explained**: no create step, no header — nothing to swallow or reset; the module has one SQL statement and no `catch`, so a failing insert throws (P13). Runtime confirmation of the two legacy paths stays needs-SME (SME_BRIEF item 8) |
| c06 LOG100 has no caller (install step); if never run, every AddLogEntry in the job fails silently and `init` is never retried | **residual** (`inferred` kept) | — | c06 (1) | P13, P15 | **residual explained, delta observed**: fresh schema → 0 rows → first call writes; table renamed away → **42P01 thrown** to the caller (legacy: swallowed by `callp(e)`); table back → next call writes (legacy: `inz` set before the failed resolution, never retried) (CR-L6). `TODO(log-programs-c06)` present; the install runbook stays an ops question |
| c07 ORD700 event `'2'` is the only writer; `'ORD700:Order Line deleted <orid> <line> article : <arid 6> quantity : <odqty>'` | converted (reuse) | (the ORD trigger; `addLogEntry` aligned) | c07 (2) | P06, P07 | match: 300 `DETORD` deletes (orid / line incl. 0, article ids 0–6 characters incl. `X9` and blank, negative / zero / positive quantities) — the trigger row == the message oracle (`%char`: no leading zeros, `-` only when negative; `ODARID` padded to 6) and the module's row for the same event is indistinguishable on `msg`, `user_id` and the rebuilt line; 20 `addLogEntry` calls change nothing but `samlog` (article quantities, `detord`, `orders` untouched); events `'1'` / `'3'` log nothing; **nothing under `modern/src` imports `shared/samlog`** — the trigger keeps its own `INSERT` (reuse, not rewiring; ORD pack not widened) |
| c08 `EXPORT(*ALL)` exports exactly `AddLogEntry`; `ORD700`'s binding to `LOG` is not in the tree | converted / n/a | `Samlog` interface; module exports | c08 (1) | P08 | match: instance keys are exactly `['addLogEntry']`; module exports are `createSamlog`, `normaliseLogEntry`, `formatLegacyLine`, `toRpgTimestamp`, `decodeSamlogUserSpace` and eight width constants with the cards' values (500, 10, 500, 63, `' ***'`, 600, 7, 5 000); nothing named `init` / `reset` / `truncate` / `read` / `list` / `clear`. Signature / `BNDDIR` / `ACTGRP(*CALLER)` have no TS equivalent (CR-L7, explained) |
| c09 Reader = menu option 84 `ADSPUSRSPC` (no source); a reader would decode offset 0, take `7 .. pos−1`, split on `' ***'` | **residual** / needs-SME (`inferred` kept) | (`decodeSamlogUserSpace` over a copy; `formatLegacyLine` as the one string) | c02 / c09 (6) | P09, P11, P15, P17 | **residual explained**: **0** log / samlog routes in the Fastify tree (35 routes), 8 candidate URLs 404, no OpenAPI, no web page, `app.ts` does not reference `samlog` (P15). The rule the card spells out is what the decoder applies (P09), and it shows the c03 "glued lines" consequence: a 450-character message is written as 500 bytes with no terminator, the decoder then sees the cut line glued to the next one, or as the tail when it is last (P11). `TODO(log-programs-c09)` present; nothing answered |
| c10 Unsynchronised shared cursor: concurrent writers overwrite one another's line, blank runs, lost lines | **residual** (`inferred` kept) | — | c10 (1) | P14 | **residual explained, delta observed**: 200 concurrent `addLogEntry` calls through the pool → 200 rows, 200 distinct messages, ids unique and increasing; the machine under the card's Interleaving A loses every `A` line of 40 racing pairs, keeps the `B` lines, leaves 39 blank runs and over-advances `pos` by the lost lines' lengths — exactly the card's description (CR-L8). Acceptable-loss question stays with the room (SME_BRIEF item 6) |

**Unexplained gaps: none.** Every card is either matched at the boundary or carries a reason that traces to the pack (`anti_corruption` "prefer aligning with existing ORD samlog table without widening", `contract_paths: []`, "HTTP only if Convert needs it", `data.notes` reuse) or to a bind-time needs-SME.

### Schema — `samlog` reuse (pack mapping rule "PF / samlog → reuse or align with existing ORD samlog table; additive shared helpers; document residual vs reuse")

| Check | Probe | Observed |
| --- | --- | --- |
| The ORD pack's table, unchanged | P16 | `id bigint` from a sequence, `logged_at timestamp NOT NULL DEFAULT LOCALTIMESTAMP`, `user_id varchar(10) NOT NULL DEFAULT ''`, `msg text NOT NULL`; PK the only index; no trigger on `samlog` (the ORD700 / ORD701 triggers are on `detord` / `orders`) |
| Additive | §2, P16 | first 371 lines of `schema.sql` byte-identical to `fc39676`; the LOG section (from line 374) is **four `COMMENT ON`** statements — the table and its `user_id` / `logged_at` / `msg` columns — and nothing else: no `CREATE`, `ALTER`, `DROP`, `INSERT` |
| Comments carry the cards | P16 | the table comment cites the pack id, c04 and c09; `user_id` cites c03 and the activation-group `User`; `logged_at` the 26-character Date; `msg` CR-L3 |
| No new object | P15, P16 | no table, view, function, sequence, index, trigger, route or module beyond `shared/samlog/index.ts`; `src/shared` has no `logging/` directory |

### Preserved (planted / known_risk / as-is), asserted, not fixed

| Behaviour | Where asserted | Observed |
| --- | --- | --- |
| Cut to 500 first, then `%trim` of blanks at both ends (c03) | P02, P04 | 499 blanks + `ZQ` → `Z`; leading blanks lost; tabs / NBSP / newlines kept |
| `User` padded to 10 in the line, never trimmed; over-long user cut, case kept (c03) | P01, P04, P05 | `User: ASH        *`; `abcdefghijklmnop` → `abcdefghij` |
| > 437 characters lose `' ***'` in the rebuilt line; stored `msg` not cut (c03, CR-L3) | P01, P11 | 430 of 3 000 random lines; the cut line glues to the next for a separator reader |
| Blank / empty entry still writes a row (c03) | P04 | 10 deliberate blanks → 10 empty-`msg` rows |
| Capacity silent permanent stop (c04) — **not** reproduced | P12 | machine 35 then silence; table 60 of 60 |
| Swallowed error / silent reset (c05), never-retried `init` (c06) — **not** reproduced | P13 | 42P01 thrown; next call writes |
| Unlocked cursor losing lines (c10) — **not** reproduced | P14 | machine loses 40 of 80; table 200 of 200 |
| Activation-time `User` (c03) — **not** reproduced | P05 | per-event actor; aligned with `ord700_user()` |
| No reader / reset / HTTP / OpenAPI / second module (c01, c05, c06, c09) | P15 | 0 routes, 8 × 404, `openapi/` = CUS + ORD, no `shared/logging` |
| The trigger keeps its own `INSERT`; module imported by nothing (c07) | P07 | 0 importers under `modern/src` |

### Observations found (not behaviour drift on any card for an input reachable through an in-tree caller)

| Id | Gap | Severity | Suggested owner |
| --- | --- | --- | --- |
| G-L1 | `createSamlog(db: Db)` with `Db = pg.Pool`. The module documents that a caller *inside an ORD transaction* logs as the same user the trigger would — `atu.user` is transaction-local, set by `order.repository.ts` on its own `PoolClient`. P05 shows the alignment holds at runtime when `createSamlog` is handed that client (module row and trigger row both `OPERATOR12`). But `pg.PoolClient` is not assignable to `pg.Pool` (`tsc --strict`: TS2345, missing `totalCount`, `idleCount`, `waitingCount`, `expiredCount` and three more), so through the **typed** API the only reachable default actor is the database role, and `test/samlog.test.ts` exercises the in-transaction actor with a hand-written `INSERT … COALESCE($1, ord700_user())`, not through the module. No card behaviour is affected: c07's alignment is between two writers of one table and both name the same user given the same transaction; the README's CR-L4 sentence ("`atu.user` on the transaction (ORD writers)") describes the trigger's path, which is what the LOG module *aligns with*, not a path a typed LOG caller has today | low | Conversion at the next pack version: widen the parameter to a query-only interface (`Pick<pg.Pool \| pg.PoolClient, "query">`, or export a `Queryable` type from `db/pool.ts` — the latter touches `src/db/**`, which is deny-listed for this pack, so the local form is the one within the edit surface). One line; no behaviour change |
| G-L2 | The `500A` entry, the `10A` user and the `500 varying` line buffer were single-byte EBCDIC on the box; the module counts UTF-16 code units. P18: 600 × `é` stores 500 characters (1 000 UTF-8 bytes); a 12-character umlaut user is cut to 10 characters; the rebuilt line is cut at 500 characters. For the ASCII charset every in-tree caller produces (the ORD700 message, fixture users) the two counts coincide exactly (P01, P02, P04, P06), and no RPG caller can pass a multi-byte character into a `500A` parameter. Recorded so `ADDLOGENTRY_ENTRY_LENGTH` and friends are not mistaken for byte counts | low | Conversion documentation at the next README touch (one clause on the constants' doc comments: "characters, not bytes"). Nothing to change in behaviour unless the room wants byte semantics, which would also mean choosing a CCSID |

## 5. CONTRACT_RISK CR-L1..CR-L8 and pack `known_risks` — accept-for-demo or defer

No line below claims an IBM i match. "Accept-for-demo" means the delta is acceptable for the pathfinder demo as documented in `modern/README.md`; "defer" means the room or an SME must decide before anything beyond a demo. No SME answer is invented here.

| Id | Delta (from `modern/README.md`) | Disposition | One sentence |
| --- | --- | --- | --- |
| CR-L1 | No `LOG100`: the log is created by `applySchema`, is never "replaced", and has no reset path; `db:migrate` is the install step | **accept-for-demo** | `replace *YES` and the 7-byte header are properties of the user space, the table exists in every schema `applySchema` has built (P13) and a `TRUNCATE samlog` helper would be a mass delete on a table the ORD pack owns with no in-tree caller — nothing to reset is the right demo answer, and the install runbook question (c06) is the ops team's, not the code's. |
| CR-L2 | Storage is one row per line, not a byte buffer with an in-band cursor | **accept-for-demo** | It is the pack's own mapping rule ("reuse or align with existing ORD samlog table"), the table is the ORD pack's shape unchanged (P16), and the byte layout survives as a decoder that reads a machine-built copy exactly, CP037 included (P09, P10) — the only thing lost is a migration of existing content, which c02 recommends against and the room has not asked for. |
| CR-L3 | `msg` is stored in full (cut to 500 then trimmed); the 437-character cut that drops `' ***'` applies only when the legacy string is rebuilt with `formatLegacyLine` | **accept-for-demo** | The cut is a property of the `500 varying` line buffer, not of the message, `formatLegacyLine` reproduces it exactly including the glued-line consequence (P01, P11), ORD700's ≈ 64-character messages never reach it (P06), and storing a cut message would lose text the legacy had no reader for; whether the line *string* is a contract at all is the room's (SME_BRIEF item 7), listed under "deferred decisions", and either answer is served by a row plus the formatter. |
| CR-L4 | Actor per event: `user_id` = `atu.user` on the transaction, the database role, or the user named by `addLogEntry`; case kept, cut to 10 | **accept-for-demo** (code) — SME confirmation **deferred** | The ORD pack already chose per-event (`ord700_user()`) and had it verified `as_is`, this module applies the same function so the two writers name the same user in the same transaction (P05), a process-wide "first user" in a request-serving Node process would be wrong, and the legacy activation-time stamp is an inference on timing the SME brief still asks about (item 7); G-L1 notes the typed path to the transaction actor is one type widening away. |
| CR-L5 | No capacity: the log grows without bound; nothing stops silently | **accept-for-demo** | c04 is a legacy defect (`inferred`), the machine shows it exactly as the card describes — 35 lines at 5 000 bytes, then silence forever (P12) — and reproducing a silent permanent stop would be reproducing a bug with no reader to notice; the box facts (allocated size, has it ever filled) stay needs-SME and are recorded, not answered. |
| CR-L6 | No per-activation-group `init`, no resolution failure, no never-retried state: every call writes or throws | **accept-for-demo** | The failure mode exists only because `SAMLOG` is a separately created object on `*LIBL`; here a missing table throws 42P01 to the caller and the next call after the cause is removed writes (P13), which is the same stance the ORD trigger takes (a failed `INSERT` fails the delete's transaction — no `callp(e)` there either); an application-level decision to swallow logging errors, if ever wanted, belongs to the caller, and there is none. |
| CR-L7 | No binder signature, no `BNDDIR`, no `ACTGRP(*CALLER)`, no `*LIBL`; the export surface is a TypeScript interface with one method | **accept-for-demo** | A TypeScript import resolves `addLogEntry` at build time (P08: exactly one method, nothing to open or close), which is stricter than the generated signature with no `*PRV` that c08 describes; the build-owner question (how `LOG` is bound into `ORD700`) is real but about the box, shared with `ord-trigger-ord700-c03` and `srvpgm-supporting`, and has no TS counterpart — same stance as CR-P7. |
| CR-L8 | Concurrent appends never lose a line or corrupt a cursor | **accept-for-demo** | The insert is transactional and `id` is a sequence, 200 of 200 concurrent appends are kept (P14) where the machine under the card's own interleaving loses half, and reproducing a lost-line race would be reproducing a defect the card calls "real but rare, unobservable without a reader"; the room's acceptable-loss question (item 6) is about whether the legacy loss ever mattered, not about what the target should do. |

Totals: 8 accept-for-demo, 0 deferred CONTRACT_RISKs. **Deferred to the room / SME** (not risks of the code, decisions the code waits on): preserve the user-space log at all (pack `known_risk`, SME_BRIEF item 1); the line string vs `Msg` as the contract (item 7); activation-time vs per-event actor confirmation (item 7 / CR-L4); an ORD pack version if the room wants one writer through `shared/samlog`.

Pack `known_risks` (from `PACK.yaml`):

| Known risk | Disposition | Evidence / one sentence |
| --- | --- | --- |
| Pathfinder waiver: no IBM i goldens; COMPARE only at TypeScript API | accept-for-demo (structural) | This whole run; `PARITY.yaml` `legacy_replay: SKIPPED`, `ibm_i_parity: NOT_CLAIMED`. |
| Whole-slice may be non-functional side effect — do not invent business value claims | **defer** (room) — verified not invented | No business value is claimed anywhere (README, CONVERT_RECORD, this report); the module is imported by nothing under `modern/src` (P07) and the ORD trigger writes the log without it — the LOG pack's deliverable is an alignment helper plus the residual-vs-reuse record, and whether the log is preserved at all is SME_BRIEF item 1. |
| Capacity silent fail (c04) | accept-for-demo — verified preserved as residual | P12: the machine reproduces the card (35 lines, then silence, `pos` unchanged); the table does not; `TODO(log-programs-c04)` present; nothing "fixed", nothing invented. |
| ORD already has samlog table — prefer align without widening `atu-merlin-ts-ord-v1`; extend shared logging or document residual vs reuse | accept-for-demo — verified | P06 / P07 / P16: same table, same actor function, trigger and module rows indistinguishable; `features/order/**`, `openapi/order.yaml`, `architecture/atu-merlin-ord/**` untouched since `fc39676`; the residual-vs-reuse table is in the README LOG section. |
| Never widen ORD pack | accept-for-demo — verified | §2: empty `git diff` on every ORD path; ORD suite 56 green without edit; the module is not wired into ORD (P07). |
| Architecture DRAFT/BOUND does not authorize Convert; separate ROOM_OK required | n/a (procedural) | Chain honoured: BIND `ad697f3` → convert `ROOM_OK` (`fc39676`) → convert `2b0fa73` / `4b2edc2` / `666330d` → verification `ROOM_OK` (`1e41dc3`) → this run. |
| CUS and ORD modern already exist; accidental re-scope of those packs is a fail | accept-for-demo — verified | §2: `features/customer/**`, `features/order/**`, both OpenAPI files, `architecture/atu-merlin/**`, `architecture/atu-merlin-ord/**`, `src/app.ts`, `src/server.ts` untouched since `fc39676`; CUS / ORD suites 55 / 56 green without edit. |
| `modern/db/**` additive / shared reuse only — do not reshape CUS or ORD schema non-additively | accept-for-demo — verified | §2 + P16: first 371 lines of `schema.sql` byte-identical to `fc39676`; the LOG section is four `COMMENT ON` and nothing else; `samlog` is the ORD pack's shape exactly. |

## 6. Explicitly not claimed / refused

- IBM i parity, `PARITY=GREEN`, `REPLAY_GREEN`, `legacy_green`, `parity_green`, behaviour `status: verified` — none. `inventory/atu-merlin/APP_MANIFEST.yaml` was not touched (pack `edit_surface.deny` and Field Guide: under a waiver the maximum is `converted` + `parity: WAIVED`).
- `PACK.yaml` `quality_gates.verification: DEFERRED` was not edited — BOUND is immutable; this run is the evidence the room reads next to it.
- Whole-estate verification — CUS / ORD / VAT / DAT / COU / PAR not re-verified as this job's claim; ART untouched.
- A reader (route, page, CLI, OpenAPI), a reset / truncate / rotate path, a migration of `SAMLOG` content, an EBCDIC CCSID table, a `shared/logging` module, wiring the ORD trigger through `shared/samlog` — not modelled, not invented (the room's preserve-at-all decision comes first).
- "Repo fully migrated" or "LOG fully migrated" — no; see §1.
- Golden creation or RECORD — no `tests/characterization/**` exists and none was created.
- Discovery card edits — none: no harness bug blocked COMPARE.
- Fixing G-L1 / G-L2 or deciding SME_BRIEF items 1 / 7 — not this station's edit; recorded for Conversion (G-L1, G-L2) and the room (items 1, 7, an ORD writer version).
- Any edit under `ATU_SRC/**`, `discovery/**`, `inventory/**`, `architecture/**`, `modern/src/**`, `modern/db/**`, `modern/test/**`, `modern/openapi/**`. `modern/README.md` was touched for its two LOG status sentences only (the job body carries no STRICT line; VAT / DAT / COU / PAR precedent), so the README stops saying "Verification deferred" for LOG.
- SME answers — none invented; `discovery/log-programs/SME_BRIEF.md` stays unsigned with its nine open questions.

## 7. Reproduce

```bash
cd modern && npm ci && ./scripts/local-pg.sh start
export DATABASE_URL=$(./scripts/local-pg.sh url)
npm run typecheck && npm test
npx tsx ../verification/log-vertical/2026-09-09-r1/evidence/probe.mjs
```

The probe needs only a live `DATABASE_URL`: it builds a throw-away schema through `modern/test/helpers/db.ts` (schema + CUS seed + ORD seed), boots `buildApp()` against it for the route-absence check, runs every case there (P13 builds and drops a second fresh schema of its own) and drops it. The machine replays, the schema and export checks and the absence checks are deterministic; the random messages, users, timestamps and `DETORD` rows (P01–P04, P06, P09) are drawn fresh each run (`crypto.randomBytes`), so `results.json` counts are stable but the sampled values are not.

## 8. Follow-ups (not done here)

- Conversion (next pack version, one line, no behaviour change): G-L1 — widen `createSamlog`'s parameter to a query-only type so the documented in-transaction actor path is typable; then let `test/samlog.test.ts` exercise that path through the module rather than by hand.
- Conversion (next README touch, documentation only): G-L2 — "characters, not bytes" on the width constants.
- Room: decide SME_BRIEF item 1 (preserve the user-space log at all — if "no", the module stays an alignment helper nothing calls and the pack is complete as documentation; if "yes", decide item 7: is the line string a contract, and is per-event actor the intended one); decide whether ORD should log through `shared/samlog` (an `atu-merlin-ts-ord-v1` version bump — not a LOG edit).
- SME: sign off `discovery/log-programs/SME_BRIEF.md` — the nine open questions (c04 allocated size / observed failure, c06 install runbook, c08 binding of `LOG` into `ORD700`, c09 `ADSPUSRSPC` provider and use of option 84, c10 acceptable loss, c03 actor timing and line-string contract, c05 runtime confirmation, c01 / c02 migration).
- README (COU pack, documentation only, not this station's edit): the header sentence still says "COU: **PARITY=UNVERIFIED** (Verification deferred)" and the COU section's waiver paragraph likewise, although `verification/cou-vertical/2026-09-09-r1/PARITY.yaml` records `TS_BOUNDARY_GREEN` — that verify run's edit surface was STRICT (`verification/cou-vertical/**` only), so the README was left stale by design. One sentence at the next COU README touch.
- With this run every residual pack is verified under the waiver (VAT, DAT, COU, PAR, LOG) alongside CUS and ORD; the conveyor is idle until Ash sets a new `AGENT_JOB` RUN. Pack B night-residual queue still held (sql-objects, ord-batch-ord900, cou-maintain COU200 half, pro-interactive, pro-modules, pro-cobol-pro201).
- If the room ever wants parity against IBM i: legacy RECORD on the box first (Test execution station), then a real COMPARE — this run cannot be upgraded into that.
