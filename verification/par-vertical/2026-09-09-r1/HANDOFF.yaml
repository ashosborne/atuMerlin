run_id: 2026-09-09-r1
slice_id: par-maintain             # the pack's single slice — all thirteen accepted cards in scope
station: Verification
mode: COMPARE
target: modern
characterization: WAIVED_PATHFINDER
authorisation: "Verification ROOM_OK carried in overnight/AGENT_JOB.md line 4 (ROOM_OK: verification PAR only; Field + CTO Verify ROOM_OK batch of five 2026-09-09; paste PASTE-verify-par-vertical-atu-merlin.md @ 744ac29), separate from BIND and from the convert ROOM_OK"

pack_id: atu-merlin-ts-par-v1
pack_version: 1
pack_status_at_run: BOUND
pack_path: architecture/atu-merlin-par/PACK.yaml
waiver_record: architecture/atu-merlin-par/ADR/0001-par-maintain-ts-postgres.md

conversion_handoff:
  record: architecture/atu-merlin-par/CONVERT_RECORD.md
  parity_at_handoff: UNVERIFIED
  branch: cursor/atu-merlin-estate-discovery
  pr: https://github.com/ashosborne/atuMerlin/pull/1
  commits:
    - 19609fc   # convert PAR vertical: shared parm (FPARAMETER getters + getPath) and PAR200 maintain API
    - 0724e00   # README PAR section + CONVERT_RECORD
    - cb8142d   # AGENT_JOB convert DONE
  head_at_verification: ce4e68ec962ea5a1f61f1768e262259b3eadb8d4

feature_ids:
  par-maintain: [c01, c02, c03, c04, c05, c06, c07, c08, c09, c10, c11, c12, c13]
  residual_explained: [c03, c06, c13]          # subfile snapshot / key routing (CR-P2); LOG100 anchor (LOG pack)
  residual_half: [c07]                         # WRKLNK panel not carried; the pattern string is

oracle:
  kind: discovery-cards-and-readme    # NOT IBM i goldens
  paths:
    - discovery/par-maintain/features/   # c01..c13
    - modern/README.md                   # PAR card coverage + CONTRACT_RISK CR-P1..CR-P7
    - architecture/atu-merlin-par/CONVERT_RECORD.md
  legacy_goldens: none                # tests/characterization/** does not exist for this pathfinder
  replay_green_run_ids: []
  probe_oracle: "c09 PAR300.chainPARAMETER replayed as a buffer machine incl. its cache (request vs the record buffer's own key; clear + chain on a differing key; miss never cached), compared on values and on SQL counts through a query-counting proxy; c01 PAR200 s01lod keyed read replayed in JavaScript (plain SELECT, Buffer.compare sort on (pacode, pasubcode), 14 rows, one look-ahead read, PARM2S = first 32); c02 / c04 / c05 FMT03 / FMT02 / option 4 as display folding of every field without CHECK(LC) + duplicate-check-only + unconditional update with the key output-only + keyed delete; c07 PAR201 `*TCAT '*'` on a *CHAR 100; 10A by-value contract as truncate-right / ignore-trailing-blanks. Independent of the module's SQL and of test/parm.test.ts / test/par.api.test.ts"

modern_boot:
  runtime: "Node v22.14.0 (pack min node 20)"
  database: "PostgreSQL 16.15 via modern/scripts/local-pg.sh (server binaries installed on the runner for this session); the probe builds a throw-away schema through modern/test/helpers/db.ts (schema + CUS seed + ORD seed) and drops it"
  server: "buildApp() booted once against the throw-away schema; the HTTP surface (GET list / GET path / GET, PUT, DELETE key / POST) driven with inject"

scope_guards:
  sibling_packs_untouched: "git diff ae99393..HEAD -- modern/src/features/customer modern/src/features/order modern/openapi modern/src/db architecture/atu-merlin architecture/atu-merlin-ord architecture/atu-merlin-vat architecture/atu-merlin-dat architecture/atu-merlin-cou architecture/atu-merlin-log shows only architecture/atu-merlin-log/CONVERT_RECORD.md (the LOG pack's own later convert); modern/src/app.ts diff is the additive PAR registration + error-handler chain"
  schema_additive: "first 332 lines of modern/db/schema.sql (CUS + ORD + VAT + DAT + COU) byte-identical to ae99393; PAR section is one CREATE TABLE IF NOT EXISTS + four COMMENT ON, alters / drops nothing (P22)"
  atu_src_untouched: "git diff origin/master..HEAD -- ATU_SRC is empty"
  order_interop: "features/order/** makes no PATH read (P08); ORD500 PDF step stays needs-SME under the ORD pack; interop documented as reuse of getPath() / GET /api/parameters/path, order not rewritten"
  edit_surface_this_run: "verification/par-vertical/2026-09-09-r1/**, modern/README.md (PAR status sentences only: header line + PAR section waiver paragraph, VAT / DAT precedent; this job body carries no STRICT line) and overnight/AGENT_JOB.md line 1"

open_after_this_run:
  contract_gaps: [G-P1, G-P2, G-P3, G-P4]
  deferred_risks: []                            # CR-P1..CR-P7 all accept-for-demo
  deferred_decisions: "c11 PATH-as-configuration (room; pack known_risk); c07 trailing-slash data fact (SME); PAR200 web page (room, with c11); estate list/edit lock template CR-8 / CR-O8 / CR-P2 / CR-P5 (room)"
  needs_sme: "discovery/par-maintain/SME_BRIEF.md — unsigned: c11 configuration vs maintained row + other rows on the box, c07 / c11 trailing slash and file system, c08 blank PATH stance, c09 cache staleness, c05 unconfirmed delete, c02 / c04 lock template, c10 / c12 build-owner questions"
  downstream_packs: "ORD pack version: ORD500 PDF step reusing getPath() (ord-print-ord500-c02 / c04 needs-SME first); PRO202 / PRO203 (pro-interactive, unbound); LOG pack: c13 anchor (CR-L1)"

forbid_record: true
goldens: n/a-read-only
