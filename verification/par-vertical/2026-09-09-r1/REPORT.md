# Verification REPORT — PAR vertical (atuMerlin), run `2026-09-09-r1`

pack_id@version: **`atu-merlin-ts-par-v1@1`** (`architecture/atu-merlin-par/PACK.yaml`, `status: BOUND`, bound 2026-09-09T11:12:14Z by the atuMerlin migration room — Field 9/10 + CTO skim; soft notes tip 4f42dcf; Agent Smith recorded; BOUND tip ad697f3)
Station: Verification · Mode: **COMPARE at the TypeScript boundary only** (plus the additive `parameter` mapping) · Characterization: **`WAIVED_PATHFINDER`** (ADR 0001)
Authorisation: Verification `ROOM_OK` in `overnight/AGENT_JOB.md` (line 4: "ROOM_OK: verification PAR only (atu-merlin-ts-par-v1@1 BOUND; Convert DONE tip cb8142d)"; Field + CTO Verify ROOM_OK batch of five 2026-09-09), separate from BIND and from the convert `ROOM_OK`. Paste: `PASTE-verify-par-vertical-atu-merlin.md` @ 744ac29 (job body @ `ce4e68e`).
Branch `cursor/atu-merlin-estate-discovery` @ `ce4e68e` · PR [#1](https://github.com/ashosborne/atuMerlin/pull/1) → `master` · 2026-09-09 19:18–19:40 UTC · Cloud Agent (atuMerlin factory job runner, sole run)

## 1. Verdict

**`parity: TS_BOUNDARY_GREEN`** — see `PARITY.yaml`.

What that means, exactly:

- `npm run typecheck` is clean and `npm test` is 306/306 against live PostgreSQL 16.15 (40 PAR: `test/parm.test.ts` 13 + `test/par.api.test.ts` 27; the 55 CUS, 56 ORD, 27 VAT, 63 DAT, 41 COU and 24 LOG tests unchanged and green).
- Every export of `modern/src/shared/parm/index.ts` and every route of `modern/src/features/par/` behaves the way the thirteen `par-maintain` cards and the README PAR section say it should, checked by a probe whose oracles were written from the cards, not from the module or its vitest suite. `PAR300.chainPARAMETER` is replayed in JavaScript as a buffer machine — the request compared with the record buffer's own key, clear + chain on a differing key, a miss never cached — and the module is compared with it on values (1 810 getter calls over 62 rows plus 300 random keys, 2 000 `10A`-contract pairs) and on SQL statement counts through a query-counting proxy, which is where the deliberate no-cache delta (CR-P1) is expected to show and does. `PAR200`'s `s01lod` keyed read (14 rows + one look-ahead, byte-order key) is replayed over a plain `SELECT` and compared with `GET /api/parameters` page by page on ten table sizes; `FMT03` / `FMT02` / option 4 are modelled as display folding + duplicate-check-only + unconditional update + keyed delete and compared with `POST` / `PUT` / `DELETE` on 400 random creates, 200 random edits and a 24-way create race; `PAR201`'s `*TCAT '*'` pattern is compared on 3 000 values — **22/22 cases, stable over three runs** (`evidence/results.json`).
- The buffer rules the cards describe are observed, not inferred: **0** SQL statements for a blank/blank key from all five getters with a blank/blank row present (c09 kept), **5** reads for 5 `getPath` calls where the legacy machine reads once (CR-P1 — the deliberate no-cache delta, seen as such), and a `PATH` change or delete seen by the next call while the legacy buffer holds it until a key switch.
- The planted behaviours and known risks are present and asserted as-is, not fixed (§4, "Preserved").
- Every card is either matched at the boundary or carries a reason that traces to the pack; there are **no unexplained card gaps**. Four **observations** were recorded (§4, G-P1 … G-P4): a wrong sentence in the README's CR-P3 rationale, three input corners a 5250 field could not produce, a lower-case key written by another tool being unaddressable by the folded keyed routes, and a nuance in the c09 card's "never reads" that the module's unconditional short-circuit does not share. None changes an answer for any input reachable through the modern writer.

What it does **not** mean:

- **Not `PARITY=GREEN` against IBM i.** No IBM i / RPG goldens exist for this pathfinder and none were invented. `REPLAY_GREEN` is not claimed; `legacy_green` and `parity_green` stay `false`; `replay_green_run_ids` stays `[]`. The oracle here is the Discovery cards (read from source, never executed on the box) and the README's stated contract.
- Not "Merlin migrated", and not "PAR migrated": `PAR201`'s `WRKLNK` panel (c07, the interactive IFS browser) is not carried — only the pattern string it would open; the 5250 subfile mechanics of `PAR200` (c03, c06) are residual by design; `LOG100`'s library anchor (c13) belongs to the LOG pack; and the **PATH-as-configuration decision (c11)** is open — the table is the mapping and `getPath()` the seam, nothing more. No `PATH` row is seeded. There is no web page and no OpenAPI for PAR.
- CUS, ORD, VAT, DAT, COU and LOG are not re-verified as this job's claim. `features/order/**` is *read* by one probe case (P08) only to confirm it makes no `PATH` read — the ORD500 PDF step stays needs-SME under the ORD pack, and the interop is documented as reuse of the `getPath()` / `GET /api/parameters/path` config surface, not as a rewrite of order.

**Talk-track:** PAR residual verified at TS API under waiver — not Merlin migrated; PAR201's IFS panel and the PATH-as-configuration decision (c11) are still on IBM i / open.

## 2. Gate checks

| Gate | Result |
| --- | --- |
| `PACK.yaml` status | `BOUND` — proceed |
| Verification `ROOM_OK` in job body | present (`overnight/AGENT_JOB.md` line 4, "ROOM_OK: verification PAR only") |
| Convert complete | yes — `architecture/atu-merlin-par/CONVERT_RECORD.md`, code `19609fc`, docs `0724e00`, DONE stamp `cb8142d`, handoff `PARITY=UNVERIFIED` |
| Another job `RUN` (Smith gate) | no — `list-cloud-agents` showed this run as the only active automation run at start and again before the first commit; branch head = trigger head `ce4e68e`; `AGENT_JOB.md` line 1 `RUN` on origin both times |
| `overnight/stop.txt` | absent |
| Scope | PAR only (`par-maintain` c01–c13: shared `parm` + `features/par`). CUS / ORD / VAT / DAT / COU / LOG not re-verified; ART untouched; `features/order/**` not rewritten (interop = config reuse) |
| Edit surface | written: `verification/par-vertical/2026-09-09-r1/**`, `modern/README.md` (the PAR status sentences only — header line and the PAR section's waiver paragraph — as the VAT / DAT verify runs did; this job body carries no STRICT line) and `overnight/AGENT_JOB.md` line 1. Untouched: `ATU_SRC/**`, `discovery/**`, `inventory/**`, `architecture/**` (this pack's `PACK.yaml` stays `verification: DEFERRED` — BOUND is immutable), every sibling architecture directory, `modern/src/**`, `modern/db/**`, `modern/test/**`, `modern/openapi/**` |
| CUS / ORD packs not widened | `git diff ae99393..HEAD -- modern/src/features/customer modern/src/features/order modern/openapi modern/src/db architecture/atu-merlin architecture/atu-merlin-ord architecture/atu-merlin-vat architecture/atu-merlin-dat architecture/atu-merlin-cou architecture/atu-merlin-log` shows only `architecture/atu-merlin-log/CONVERT_RECORD.md` (the LOG pack's own later convert, not PAR's); `modern/src/app.ts` diff is the additive PAR registration + error-handler chain; the first 332 lines of `modern/db/schema.sql` (CUS + ORD + VAT + DAT + COU objects) are byte-identical to the pre-convert tip `ae99393`; CUS / ORD suites 55 / 56 unchanged |
| `ATU_SRC/**` | `git diff origin/master..HEAD -- ATU_SRC` is empty |
| Goldens | none exist; nothing RECORDed, nothing rewritten |

## 3. Commands run

Environment: Node v22.14.0 (pack minimum 20), PostgreSQL 16.15 (`modern/scripts/local-pg.sh`, port 54329; server binaries installed on the runner for this session), fresh `npm ci`.

| Command | Result | Evidence |
| --- | --- | --- |
| `cd modern && npm run typecheck` | `tsc --noEmit` exit 0, no diagnostics | `evidence/typecheck-and-vitest.log` |
| `cd modern && npm test` | vitest 3.2.7: **11 files, 306 tests, 306 passed** (`dat.test.ts` 63, `order.api.test.ts` 47, `fcountry.test.ts` 41, `customer.api.test.ts` 29, `fvat.test.ts` 27, `par.api.test.ts` 27, `samlog.test.ts` 24, `fcustomer.test.ts` 16, `parm.test.ts` 13, `customer.web.test.ts` 10, `order.web.test.ts` 9), 7.85 s | `evidence/typecheck-and-vitest.log` |
| independent COMPARE probe (`DATABASE_URL=... npx tsx evidence/probe.mjs`) | **22 PASS / 0 FAIL** (run three times; counts stable, random rows / bodies differ) | `evidence/results.json` |

The vitest suite is modern's own; on its own it never sets a green flag (Field Guide anti-greenwash). The probe is the independent check. Its oracles are written from the cards: (1) the fixed-length `10A` parameter / field (truncate right, ignore trailing blanks) and the 5250 folding of every input field without `CHECK(LC)`; (2) `PAR300.chainPARAMETER` as a buffer machine with its cache (c09 steps 2–4), used both for the values every getter must return and to characterise CR-P1 and G-P4 by read counts; (3) `PAR200.s01lod` — every `parameter` row fetched with a plain `SELECT` (no `ORDER BY`), sorted by `Buffer.compare` on `(pacode, pasubcode)`, 14 rows from the saved position, one look-ahead read for More / Bottom, `PARM2S` = first 32 of `PARM2`; (4) `FMT03` / `FMT02` / option 4 from cards c02 / c04 / c05 — `clear` then the typed values, absent = blank / zero, duplicate-key check only with the DDS text, unconditional update with the key output-only, keyed delete with `%found` off on a miss; (5) `PAR201`'s `CHGVAR &PATH (&PATH *TCAT '*')` on a `*CHAR 100`. It builds a throw-away schema through `modern/test/helpers/db.ts`, wraps the pool in a query-counting proxy for the getters, boots `buildApp()` once and drives the HTTP surface with `inject`.

Seven probe cases failed on their first runs, all probe-script faults, none a module fault: the random-row generator could produce the blank/blank key a case inserts deliberately (23505); the equality helper compared JSON with key order significant (the list row's `parm2s` is emitted last); random rows for the edit / delete cases had lower-case keys, which the keyed routes fold — the very behaviour P19 records as G-P3 — so they were regenerated as PAR200 would have written them; the route tree is printed collapsed, so the surface is now checked by request; the `PATH` width check ran after the random edits had overwritten `PATH`; and a "duplicate" `POST` followed the case's own `DELETE` of that key. No module or server behaviour was changed. The committed `results.json` is a clean run.

## 4. Card COMPARE — cards vs exports / routes vs tests vs probe

Legend: export = function, method or route in `modern/src/shared/parm/index.ts` / `modern/src/features/par/`; test = vitest block in `test/parm.test.ts` / `test/par.api.test.ts`; probe = case id in `evidence/results.json`.

### par-maintain (c01 … c13)

| Card | README status | Export / route | Test | Probe | COMPARE outcome |
| --- | --- | --- | --- | --- | --- |
| c01 List in key order, 14 per load, exact Bottom by look-ahead, options 2 / 4 only, `PARM2S` 32, no filter, empty file → headings only | converted / residual | `GET /api/parameters[?offset]` (`ParRepository.list`, `ParService.list`) | c01 (6) | P11–P13 | match: pages equal the `s01lod` oracle on 10 table sizes (0, 1, 13, 14, 15, 27, 28, 29, 42, 57 rows — blank/blank, lower-case and digit-led keys included); exactly 14 / 28 rows show Bottom on the last full page; blank/blank row first; rows carry `parm2s` and never `parm2`; empty file → `rows: []`, no message; offset beyond the end → empty page; `-1` / `x` / `1.5` / `1e400` / `NaN` → 400. Byte order == module order, characterised against EBCDIC (**G-P1** below; CR-P3). Subfile mechanics residual (CR-P2). Observation: `?offset=` and `?offset=1e1` are coerced to 0 and 10 — harmless |
| c02 F6 create: duplicate-key check only; blank code / sub-code accepted; values unvalidated; display folds every field but `PARM2`; no message | converted (as-is) | `POST /api/parameters` (`ParService.create`) | c02 (7) | P14, P15, P19–P21 | match: 400 random bodies equal the `FMT03` oracle — 201 with the answer == the stored row (keys / `PARM1` / `PARM3` upper-cased and right-trimmed, `PARM2` as typed, absent or `null` → blank / zero) or 400 `VALIDATION` / `DUPLICATE_KEY` on `pacode` with `This code/sub-code already exist.` and the existing row untouched; the blank/blank row creatable; 24 concurrent creates of one key → one 201 + 23 × the same duplicate error (CR-P5); over-long / non-numeric → 400 per field, nothing written; negatives accepted (CR-P6). The lock on the existing row during the error has no HTTP equivalent (CR-P2). **G-P2** corners below |
| c03 List not refreshed after create; duplicate row after Bottom | **residual** | — (the list is re-read per call) | c03 / c06 (2) | P18 | **residual explained, delta observed**: after a create at Bottom the next two pages hold 15 distinct keys including the new one; no duplicate. A property of the subfile snapshot and the saved look-ahead key (CR-P2) |
| c04 Option 2 edit: chain with lock, `FMT02`, unconditional `update`, no validation / audit; list keeps pre-edit values; vanished row → 01221 | converted (as-is) / residual | `GET` + `PUT /api/parameters/:pacode/:pasubcode` (`ParService.get` / `update`) | c04 (6) | P16, P18, P19 | match: 200 random edits equal the `FMT02` oracle — entry returns the full row (`PARM2` at 100), update is unconditional (unchanged values still update), a partial body blanks / zeros the absent fields, the key is output-only (a body key is ignored and no `OTHER` row appears), the URL key is folded like the display; vanished row → 404 `PARAMETER_NOT_FOUND` with the key on `PUT` and `GET` (CR-P5); the list shows the new values at once, no lock (CR-P2). **G-P3** below. Detail: a key containing `/` is addressable as `%2F` (200) and not as a raw slash (404) |
| c05 Option 4: immediate keyed delete, no confirmation, no in-use check, not-found silent; `*** Deleted ****` never displayed; blank/blank key deletes the blank/blank row | converted (as-is) / residual | `DELETE /api/parameters/:pacode/:pasubcode` (`ParService.remove`) | c05 (4) | P17, P18, P21 | match: 204 with an empty body for an existing row, for the same key again and for a key that never existed; `PATH` deletable while `getPath` consumers exist — `getPath` and `GET /api/parameters/path` then `""` (c08); a blank/blank key deletes only the blank/blank row; the other 19 rows byte-identical. No ghost row, no marker (CR-P2) |
| c06 F3 = F12 on the list (exit); F3 = F12 on `FMT02` / `FMT03` (back); F5 refresh; Page Down live while More…; dead indicators | **residual** | — | c03 / c06 (2) | P12, P18 | **residual explained**: stateless HTTP — every `GET` is an F5, Page Down is `?offset`, two consecutive `GET`s are identical; the PAR surface is exactly six routes (`GET` list, `GET` path, `GET` / `PUT` / `DELETE` key, `POST`) and `PATCH`, `POST` on a key, `PUT` / `DELETE` on the collection, a refresh route, a `/parameters` web page and any deeper path are 404 (CR-P2) |
| c07 PAR201: `GetParm2('PATH')` → `*TCAT '*'` → `WRKLNK`; `'<dir>/*'` vs `'<dir>*'`; blank → `'*'`; 100-char PATH loses the `*` | converted (pattern) / residual (panel) | `wrklnkPattern` | c07 (2) | P06, P07, P08 | match: 3 000 values equal the `*TCAT` oracle on a `*CHAR 100` — the two directory outcomes, blank → `'*'`, a 100-character `PATH` loses the `*`, 99 keeps it. The `WRKLNK` panel is not carried (no IFS access, no route — absence in P18 / P22). The **trailing-slash contract is stored and returned verbatim** (P08: `/home/sample/out` stays without a slash); nothing normalises it — **needs-SME data question stays open** |
| c08 Blank / missing `PATH` is silent end to end | converted (as-is, `inferred` kept) | `getPath`, `GET /api/parameters/path` | c08 (1) | P06, P17 | match: a missing row and a blank `PARM2` are indistinguishable — `""` (200) from both readers with no error; three blanks written by another tool are returned as stored; the PAR201 pattern becomes `'*'`. The `inferred` half (relative-name resolution, `CVTSPLPDF` / XML / XSS) is outside the tree and not modelled. **needs-SME (fail fast vs default) stays open** |
| c09 GetPARM1..5 over one private chain: buffer-keyed cache, miss → cleared buffer, miss never cached, blank key never reads, case-sensitive `10A`, USROPN never closed | converted (as-is) / residual | `getParm1` … `getParm5`, `normaliseParameterKey` (private `chain`) | c09 (6) | P01–P05 | match: 1 810 getter calls over 62 rows + 300 random keys equal the chain oracle (miss → blanks / zeros, nothing thrown; the blank/blank row's values never returned); 2 000 `(code, sub)` pairs equal the fixed-length-alpha oracle (cut to 10, trailing blanks ignored, exact case — `path` and `PATH` are two rows, `Path` misses); **0** SQL statements for five spellings of a blank/blank key × 5 getters with a blank/blank row present, a half-blank key reads. The **hit cache is not reproduced**: 5 reads for 5 calls vs the legacy machine's 1, a change / delete seen at once vs held until a key switch, a miss not cached in either (CR-P1, observed). **G-P4** below |
| c10 `EXPORT(*ALL)` exports the five getters only (corrected run 16); `ClosePARAMETER` has no export; no binder source | converted / n/a | `FParameter` methods; module exports | c10 / c12 (1) | P09 | match: instance keys are exactly `getParm1..getParm5` + `getPath`; nothing named `open*` / `close*` / `chain*` on the instance or among the module's exports; module exports are `createFParameter`, `normaliseParameterKey`, `wrklnkPattern`, `PATH_KEY`, `CLEARED_PARAMETER` and six width constants; `PATH_KEY = { PATH, "" }`. Signature / `BNDDIR` / `ACTGRP(*CALLER)` have no TS equivalent (CR-P7, explained) |
| c11 Only `('PATH', ' ')` is live; four literal readers; `GetPARM1/3/4/5` unused; implicit PATH contract | converted (as-is) / needs-SME | `getPath`, `GET /api/parameters/path` | c11 (2 + 2) | P08, P19, P21, P22 | match: `getPath` == `getParm2('PATH', ' ')` == `GET /api/parameters/path` on 7 stored values (no trailing slash, a leading blank, a 100-character value) — verbatim; `PARM1/3/4/5` of the `PATH` row carried (`X`, `YZ`, 1, 2) and **read by nobody** — `getParm1/3/4/5` have no caller in `modern/src` outside `shared/parm` (31 files scanned), `getPath` is called only by the PAR route, `features/order/**` makes no `PATH` read; `path` is the reader and never a one-segment key. **PATH-as-configuration is NOT decided**: one `CREATE TABLE`, no OpenAPI, no page; the table is the mapping, `getPath` the seam. **Room decision stays open** |
| c12 `PARAMETER`: `UNIQUE (PACODE, PASUBCODE)`, five typed columns, no delete flag / audit / LF / trigger, blank/blank legal, physical deletes | converted | `db/schema.sql` PAR section | c10 / c12 (1) | P10, P22 | match: `varchar(10/10/10/100/2)` + two `smallint`, all `NOT NULL`; PK is the only index; two `CHECK`s carry the zoned widths (±9, ±999 — the sign nibble allowed negatives); no trigger, no view; 22001 / 23514 / 23505 on the bounds, the widest legal row accepted; comments cite c11 and c07. The section is one `CREATE TABLE IF NOT EXISTS` + `COMMENT ON`, alters / drops nothing |
| c13 `LOG100` uses `PARAMETER`'s library as the log's location anchor | **residual** (LOG pack) | — | — | P10 | **residual explained**: the object exists in the modern schema; what `log-programs` does with "the application library" is the LOG pack's decision (`architecture/atu-merlin-log/**` deny-listed here; its `CONVERT_RECORD` records CR-L1). Nothing coded, nothing invented |

**Unexplained gaps: none.** Every card is either matched at the boundary or carries a reason that traces to the pack (`contract_paths: []`, "HTTP maintain UI only if Convert needs it", `anti_corruption` on order, sibling deny on `atu-merlin-log`, stateless server) or to a bind-time needs-SME.

### Schema — `parameter` mapping (pack mapping rule "PF PARM / PATH → postgres table or config surface (choose at convert; document choice; c11 open)")

| Check | Probe | Observed |
| --- | --- | --- |
| Column-for-column from `PARAMETER.PF` | P10 | `pacode` / `pasubcode` / `parm1 varchar(10)`, `parm2 varchar(100)`, `parm3 varchar(2)`, `parm4` / `parm5 smallint`, all `NOT NULL` with `''` / `0` defaults; composite PK = the PF `UNIQUE`; no delete flag (exists = row present, deletes physical, c05) |
| Field bounds | P10 | an 11-character key / `PARM1`, a 101-character `PARM2`, a 3-character `PARM3` → 22001; `PARM4` ±10, `PARM5` ±1000 → 23514; duplicate key → 23505; `ABCDEFGHIJ` / 100 × `x` / `AB` / −9 / −999 accepted |
| Nothing else | P10 | PK is the only index; no trigger, no view; comments cite c11 (one live row, disposition open) and c07 (trailing slash) |
| Additive | §2, P22 | first 332 lines of `schema.sql` byte-identical to `ae99393`; the PAR section is one `CREATE TABLE IF NOT EXISTS` + four `COMMENT ON`; the LOG section follows it untouched |
| Choice documented, disposition open | P08, P22 | table = mapping; `getPath()` / `GET /api/parameters/path` = the config surface consumers reuse; no `PATH` row seeded (installation data, c08) |

### Preserved (planted / known_risk / as-is), asserted, not fixed

| Behaviour | Where asserted | Observed |
| --- | --- | --- |
| Blank / missing `PATH` silent (c08) | P06, P17 | `""` from both readers, 200, nothing thrown; deleting `PATH` produces it at once |
| Trailing-slash contract verbatim (c07 / c11) | P07, P08 | `/home/sample/out` stored and returned without a slash; `wrklnkPattern` shows `out*` vs `out/*` |
| Blank/blank row creatable, listed first, unreachable by the getters (c02, c09) | P03, P11, P14, P21 | `POST {}` → 201; first row of the list; `/%20/%20` addresses it; 0 reads from the getters |
| Unconditional, unvalidated update, no audit (c04) | P16 | unchanged values still 200; any values within width accepted |
| Immediate unconfirmed delete of `PATH`, no in-use check (c05) | P17 | 204, `getPath` then `""` |
| `GetPARM1/3/4/5` exported, no consumer (c11) | P08, P09 | five methods present; 0 callers outside `shared/parm` |
| PATH-as-configuration not decided (c11) | P08, P22 | table + `getPath` seam; no configuration reader, no OpenAPI, no page |
| Last-key hit cache (c09) — **not** reproduced | P04 | recorded and observed as CR-P1; the c09 SME question (stale read acceptable?) decides whether anyone would notice, not whether the code should hold a stale value |
| No `WRKLNK` / IFS / web page / OpenAPI (c07 panel; `contract_paths: []`) | P18, P22 | six routes only; `openapi/` = `customer.yaml`, `order.yaml`; no `par.web.ts` |

### Observations found (not behaviour drift on any card for an input reachable through the modern writer)

| Id | Gap | Severity | Suggested owner |
| --- | --- | --- | --- |
| G-P1 | `modern/README.md` PAR section, CR-P3 "Why" column says "digits before upper before lower hold in both" byte order and the EBCDIC keyed order. They do not: in EBCDIC (CP037) lower-case letters sort before upper-case and digits after every letter. P13 shows fourteen keys in module / byte order `'' 1A 9Z A AA AZ BE PATH ZZ a aa az path zz` and in EBCDIC order `'' a aa az path zz A AA AZ BE PATH ZZ 1A 9Z`. Only "blank first", "shorter key first" and the relative order of upper-case-only keys agree. The delta itself (byte order, not EBCDIC) is correctly declared, tested and dispositioned below; the sentence describing where it is invisible is wrong — the same wording the COU verify run flagged as G-C1. In-tree keys are upper-case (the API folds), so the demo never shows the difference | low | Conversion documentation fix at the next README touch (one sentence, same fix as G-C1). Not this station's edit |
| G-P2 | Three input corners a 5250 field could not produce (P15): (a) `readChar` checks the width **before** the right-trim, so `'A' + ten blanks` (11 characters, would store as `A`) is refused `FIELD_TOO_LONG` rather than trimmed; (b) JavaScript `toUpperCase` can lengthen a string (`ß` → `SS`), so ten `ß` pass the 10-character check, fold to 20 characters and fail in the column with **500 INTERNAL** instead of a 400; (c) a JSON **array** body is read as an empty form (`asRecord`) and creates the blank/blank row (201). A 5250 field is fixed-width single-byte EBCDIC, so none of these existed on the box; none changes an answer on the cards' domain | low | Conversion at the next pack version, if wanted: trim before the width check; fold before the width check (or reject non-ASCII in the folded fields — the DDS fields are single-byte); reject a non-object body with 400. Three lines, no card behaviour changes |
| G-P3 | The keyed routes fold the URL key to upper case like the display (`parameterKeyOf`), so a row whose stored key is lower-case — possible only when written **outside** the API (c02 "another tool", c12 "mixed-case key … can exist if written by another tool") — is listed by `GET /api/parameters` and invisible to the case-sensitive getters (both as on the box), but cannot be opened, updated or deleted through the keyed routes: `GET` / `PUT` answer 404 for the folded key and `DELETE` is a silent 204 that removes nothing (P19). `PAR200` took the key for option 2 / 4 from the subfile row itself and could edit or delete such a row. The API is the only writer in modern and folds on create, so the case needs an out-of-band write | low | Conversion at the next pack version: fold on `POST` only (the "typed" key) and take the URL key verbatim, or offer a non-folding form. One line; the room's c11 decision may make the routes moot |
| G-P4 | Card c09's summary says a blank key "never reads"; its step 4 says the blank request equals the **cleared** key. Replaying the buffer machine (P05) shows the two differ after a hit: from a cleared buffer a blank/blank request does not read (0 reads — matches the card and the module), but after `GetParm2('PATH':' ')` the buffer key is `PATH`, a following blank/blank request differs, chains, and would return the blank/blank row's values (`HIDDEN` in the probe); the module short-circuits unconditionally and returns blanks with no read. No caller in the estate passes a blank key (c11), so the case is unreachable | low | Discovery: one clarifying sentence on c09 (not a harness bug — COMPARE was not blocked, so the card was **not** edited by this run). Conversion: nothing to change unless the room wants the exact legacy state machine, which would also mean reproducing the cache (CR-P1) |

## 5. CONTRACT_RISK CR-P1..CR-P7 and pack `known_risks` — accept-for-demo or defer

No line below claims an IBM i match. "Accept-for-demo" means the delta is acceptable for the pathfinder demo as documented in `modern/README.md`; "defer" means the room or an SME must decide before anything beyond a demo. No SME answer is invented here.

| Id | Delta (from `modern/README.md`) | Disposition | One sentence |
| --- | --- | --- | --- |
| CR-P1 | No last-key cache, no held `PARAMETER` open, nothing to close: every getter call reads `parameter`; a `PATH` change or delete is seen by the next call | **accept-for-demo** | The only observable difference is the read count (P04: 5 reads for 5 calls where the legacy machine reads once) and that an edited or deleted `PATH` is seen by the next call instead of by the next job — the direction any stateless target takes and the same stance as CR-8 / CR-O8 / CR-V1 / CR-C1; the open c09 SME question (stale read acceptable for one setting read once per job?) decides whether anyone would have noticed on the box, not whether the code should hold a stale value. |
| CR-P2 | No subfile snapshot, no panel state, no record locks: list re-read per call, edits seen at once, deleted rows disappear, no ghost row, no `'*** Deleted ****'` buffer, no `WAITRCD` / 01218; F3 / F5 / F12 / Page Down have no equivalent beyond `GET` and `?offset=` | **accept-for-demo** | The c03 / c04 / c05 / c06 quirks are properties of one long-lived program instance and its subfile (P18: 15 distinct keys after a create at Bottom, edited values shown, no ghost, six routes and nothing else), and reproducing them over HTTP would mean inventing session state the cards never asked for; the estate-wide list/edit **lock** question (CR-8, CR-O8, CR-P2 / CR-P5) and whether a server-rendered PAR200 page (`ui: simple-web`) is ever built stay with the room — the page explicitly with c11, since a screen the room may retire is the wrong bet. |
| CR-P3 | Key order is byte order of the stored text (`COLLATE "C"`), not EBCDIC | **accept-for-demo** | The module does exactly what it declares (P11: pages equal a `Buffer.compare` oracle on ten table sizes; P13: fourteen mixed keys in byte order) and every key the API can write is upper-case, so the demo shows no difference; the README's sentence about *where* the orders agree is wrong (G-P1) and should be corrected, but the correction does not change the disposition — a key starting with a digit or lower-case (out-of-band write only) would list in a different place than on the box. |
| CR-P4 | `pacode`, `pasubcode`, `parm1`, `parm3` upper-cased **by the service** on create / update; `parm2` keeps case | **accept-for-demo** | It is the only reason `GetParm2('PATH':' ')` finds a row typed `path` (P19: typed lower, stored `PATH`, found by both readers), it reproduces exactly what the 5250 device did for every field without `CHECK(LC)` (P14: 400 bodies equal the folding oracle) while the getters stay case-sensitive as on the box (P02), and its two edges — the URL fold making an out-of-band lower-case row unaddressable (G-P3) and Unicode folding lengthening past the column (G-P2b) — are recorded with no path to them through the modern writer. |
| CR-P5 | `PUT` on a vanished row → `404 PARAMETER_NOT_FOUND`; a lost `chain` / `write` race on `POST` → the same `400 DUPLICATE_KEY` | **accept-for-demo** | HTTP must answer something where the legacy raised unmonitored 01221 / 01021 into the session (P16: 404 with the key on `PUT` and `GET`; P20: one 201 and 23 identical duplicate errors from 24 concurrent creates, exactly one row), the answers are recorded as recorded-not-decided in the README (same stance as CR-4 / CR-O1), and the lock / ghost-row template question they belong to is the room's, listed under CR-P2. |
| CR-P6 | Over-long fields → `400 FIELD_TOO_LONG`; non-integer / out-of-width `parm4` / `parm5` → `400 FIELD_INVALID`; trailing blanks trimmed on store; negatives accepted | **accept-for-demo** | 5250 field widths made these inputs impossible and the modern boundary must decide — reject rather than truncate (P15: one error per offending field, nothing written; `-9` / `-999` and `' 7 '` / `'007'` accepted; `null` == absent == blank / zero as `clear fmt03`), the same as CR-5 / CR-O10; the three corners in G-P2 (width before trim, `ß`, array body) are unreachable from any 5250-shaped input and are one-line choices at the next pack version. |
| CR-P7 | No binder signature, no `BNDDIR`, no `ACTGRP(*CALLER)`, no `*LIBL`; the export surface is a TypeScript interface | **accept-for-demo** | A TypeScript import resolves the five getters at build time (P09: exactly five getters + `getPath`, nothing named `open*` / `close*` / `chain*`), which is stricter than the generated signature with no `*PRV` that c10 describes and correctly reflects the run-16 correction (five exports, `ClosePARAMETER` never had one); the activation-group scope of the cache is CR-P1's subject, and `LOG100`'s library anchor (c13) is the LOG pack's residual. |

Totals: 7 accept-for-demo, 0 deferred CONTRACT_RISKs. **Deferred to the room / SME** (not risks of the code, decisions the code waits on): c11 PATH-as-configuration; the c07 trailing-slash data fact; a PAR200 web page (with c11); the estate list/edit lock template (CR-P2 / CR-P5 family).

Pack `known_risks` (from `PACK.yaml`):

| Known risk | Disposition | Evidence / one sentence |
| --- | --- | --- |
| Pathfinder waiver: no IBM i goldens; COMPARE only at TypeScript API | accept-for-demo (structural) | This whole run; `PARITY.yaml` `legacy_replay: SKIPPED`, `ibm_i_parity: NOT_CLAIMED`. |
| PATH as config vs maintained table (c11) — do not invent disposition | **defer** (room) — verified not invented | P08 / P22: the table is the mapping, `getPath()` / `GET /api/parameters/path` the seam consumers reuse; no configuration reader, no OpenAPI, no page, no `PATH` seed. If the room says "configuration": a second `FParameter` behind the same interface and `features/par` (c01–c06) has nothing left to maintain — pack version bump + SUPERSEDE + re-bind, not an edit in place. |
| PATH trailing slash / filesystem semantics (c07) | accept-for-demo for the code (verbatim) — **defer** the data fact (SME) | P07 / P08: stored and returned exactly as written, `wrklnkPattern` shows the two outcomes, nothing normalises or defaults; whether the value on the box ends with `/` and which file system it names is installation data the SME brief still asks. |
| Unused GetPARM getters — do not invent consumers | accept-for-demo — verified preserved | P08: `getParm1/3/4/5` exported, 0 callers outside `shared/parm` in 31 files; `PARM1/3/4/5` of the `PATH` row carried and read by nobody. |
| PATH consumers ORD500 already converted — do not rewrite order feature; document interop as reuse of config | accept-for-demo — verified | §2: `features/order/**` and `openapi/order.yaml` untouched since `ae99393`; P08: `features/order/**` makes no `PATH` read (the PDF step is needs-SME under the ORD pack); interop is documented as reuse of `getPath()` / `GET /api/parameters/path` for a future ORD pack version. |
| Architecture DRAFT/BOUND does not authorize Convert; separate ROOM_OK required | n/a (procedural) | Chain honoured: BIND `ad697f3` → convert `ROOM_OK` (`ae99393`) → convert `19609fc` / `0724e00` / `cb8142d` → verification `ROOM_OK` (`ce4e68e`) → this run. |
| CUS and ORD modern already exist; accidental re-scope of those packs is a fail | accept-for-demo — verified | §2: `features/customer/**`, `features/order/**`, both OpenAPI files, `architecture/atu-merlin/**`, `architecture/atu-merlin-ord/**` untouched since `ae99393`; CUS / ORD suites 55 / 56 green without edit; `app.ts` diff is the additive PAR registration only. |
| `modern/db/**` additive only — do not reshape CUS or ORD schema | accept-for-demo — verified | §2 + P10 / P22: first 332 lines of `schema.sql` byte-identical to `ae99393`; the PAR section is one `CREATE TABLE IF NOT EXISTS` + comments and alters / drops nothing. |

## 6. Explicitly not claimed / refused

- IBM i parity, `PARITY=GREEN`, `REPLAY_GREEN`, `legacy_green`, `parity_green`, behaviour `status: verified` — none. `inventory/atu-merlin/APP_MANIFEST.yaml` was not touched (pack `edit_surface.deny` and Field Guide: under a waiver the maximum is `converted` + `parity: WAIVED`).
- `PACK.yaml` `quality_gates.verification: DEFERRED` was not edited — BOUND is immutable; this run is the evidence the room reads next to it.
- Whole-estate verification — CUS / ORD / VAT / DAT / COU / LOG not re-verified as this job's claim; ART untouched.
- PAR201's `WRKLNK` panel, a PAR200 web page, a `PATH` seed, an OpenAPI file, a configuration-backed `FParameter` — not modelled, not invented (the room's c11 decision comes first).
- "Repo fully migrated" or "PAR fully migrated" — no; see §1.
- Golden creation or RECORD — no `tests/characterization/**` exists and none was created.
- Discovery card edits — none: no harness bug blocked COMPARE (G-P4 is a clarification for Discovery, not a blocker).
- Fixing G-P1 … G-P4 or deciding c11 — not this station's edit; recorded for Conversion (G-P1 … G-P3), Discovery (G-P4) and the room (c11, the lock template, the page).
- Any edit under `ATU_SRC/**`, `discovery/**`, `inventory/**`, `architecture/**`, `modern/src/**`, `modern/db/**`, `modern/test/**`, `modern/openapi/**`. `modern/README.md` was touched for its two PAR status sentences only (the job body carries no STRICT line; VAT / DAT precedent), so the README stops saying "Verification deferred" for PAR.
- SME answers — none invented; `discovery/par-maintain/SME_BRIEF.md` stays unsigned with its open questions (c11 configuration vs row, c07 / c11 data, c08 blank stance, c09 cache, c05 delete, c02 / c04 lock template, c10 / c12 build-owner questions).

## 7. Reproduce

```bash
cd modern && npm ci && ./scripts/local-pg.sh start
export DATABASE_URL=$(./scripts/local-pg.sh url)
npm run typecheck && npm test
npx tsx ../verification/par-vertical/2026-09-09-r1/evidence/probe.mjs
```

The probe needs only a live `DATABASE_URL`: it builds a throw-away schema through `modern/test/helpers/db.ts` (schema + CUS seed + ORD seed), boots `buildApp()` against it, runs every case there and drops it. The page sweeps, bounds and route checks are deterministic; the random rows, keys, bodies and edits (P01, P02, P11, P14, P16, P17) are drawn fresh each run (`crypto.randomBytes`), so `results.json` counts are stable but the sampled values are not.

## 8. Follow-ups (not done here)

- Conversion (next README touch, documentation only): close G-P1 (correct the CR-P3 rationale — the same sentence as G-C1 under CR-C4).
- Conversion (next pack version, one to three lines each, no card behaviour changes): G-P2 (trim before the width check; fold before the width check or reject non-ASCII in folded fields; 400 on a non-object body), G-P3 (fold on `POST` only, URL key verbatim). Both may be moot after the room's c11 decision.
- Discovery: one clarifying sentence on `par-maintain-c09` for G-P4 ("never reads" is a cleared-buffer property).
- Room: decide c11 — `PATH` as target configuration (retire `PAR200` / `parameter`: pack version bump + SUPERSEDE + re-bind; `getPath()` is the seam so no consumer changes) or keep the maintained row (then decide whether a PAR200 page is built). Decide the estate list/edit lock template (CR-8 / CR-O8 / CR-P2 / CR-P5) once for CUS200 / ORD200 / ORD201 / PAR200.
- SME: sign off `discovery/par-maintain/SME_BRIEF.md` — c07 / c11 data (trailing slash, file system, other rows), c08 blank stance, c09 cache staleness, c05 unconfirmed delete, c02 / c04 lock template, c10 / c12 build-owner questions.
- ORD pack version: the ORD500 PDF step (`CVTSPLPDF`, `TODIR(&PATH)`) reusing `getPath()` — `ord-print-ord500-c02` / `c04` needs-SME first. PRO202 / PRO203 (`pro-interactive`, unbound): the other two `PATH` consumers. LOG pack: c13 anchor (CR-L1).
- Next per the job header: Verify LOG (its own `AGENT_JOB` RUN with its own `ROOM_OK`). Pack B night-residual queue held.
- If the room ever wants parity against IBM i: legacy RECORD on the box first (Test execution station), then a real COMPARE — this run cannot be upgraded into that.
